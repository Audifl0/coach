---
phase: 05-adaptive-coaching-and-safety-guardrails
plan: 05-02
type: execute
wave: 1
depends_on:
  - 05-01
files_modified:
  - src/lib/adaptive-coaching/policy.ts
  - src/lib/adaptive-coaching/confidence.ts
  - src/server/services/adaptive-coaching-policy.ts
  - tests/program/adaptive-coaching-policy.test.ts
  - tests/program/adaptive-coaching-fallback.test.ts
autonomous: true
requirements:
  - SAFE-01
  - SAFE-02
  - SAFE-03
must_haves:
  truths:
    - "Safety bounds are hard-enforced server-side and cannot be bypassed by LLM output or client payloads."
    - "Limitation/pain conflicts emit deterministic warning metadata without forcing automatic blocking."
    - "Low-confidence or invalid recommendations deterministically fall back to last conservative recommendation or conservative hold."
  artifacts:
    - path: src/lib/adaptive-coaching/policy.ts
      provides: "Central SAFE-01 and SAFE-02 policy engine with strict conservative clamps and conflict tagging."
      exports:
        - applyAdaptiveSafetyPolicy
        - detectLimitationConflict
    - path: src/lib/adaptive-coaching/confidence.ts
      provides: "SAFE-03 confidence gate and fallback selection helpers."
      exports:
        - evaluateRecommendationConfidence
        - selectConservativeFallback
    - path: tests/program/adaptive-coaching-policy.test.ts
      provides: "Policy test matrix for clamp and limitation-warning behavior."
  key_links:
    - from: src/lib/adaptive-coaching/policy.ts
      to: src/server/services/adaptive-coaching.ts
      via: "post-LLM guardrail enforcement"
      pattern: "applyAdaptiveSafetyPolicy"
    - from: src/lib/adaptive-coaching/confidence.ts
      to: src/server/services/adaptive-coaching.ts
      via: "fallback gate execution"
      pattern: "evaluateRecommendationConfidence|selectConservativeFallback"
---

<objective>
Implement deterministic safety guardrails as the authoritative decision gate before any adaptive recommendation is applied or surfaced.

Purpose: Guarantee conservative behavior under normal, conflicting, and degraded-confidence conditions.
Output: Policy/confidence modules with requirement-grade automated tests.
</objective>

<execution_context>
@/home/flo/.codex/get-shit-done/workflows/execute-plan.md
@/home/flo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-adaptive-coaching-and-safety-guardrails/05-01-PLAN.md
@.planning/phases/05-adaptive-coaching-and-safety-guardrails/05-CONTEXT.md
@.planning/phases/05-adaptive-coaching-and-safety-guardrails/05-RESEARCH.md
@src/lib/program/contracts.ts
@src/server/dal/program.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build SAFE-01 progression clamp policy with strict moderate bounds</name>
  <files>src/lib/adaptive-coaching/policy.ts, tests/program/adaptive-coaching-policy.test.ts</files>
  <behavior>
    - Test 1: `progress` proposals above configured load/rep bounds are clamped to conservative maximums.
    - Test 2: Negative regression beyond conservative floor is clamped for hold/deload consistency.
    - Test 3: `substitution` proposals retain action type while forcing bounded progression parameters.
  </behavior>
  <action>Define centralized numeric constants for conservative progression bounds and implement `applyAdaptiveSafetyPolicy` that clamps all progression deltas before persistence. Keep thresholds configurable in one module and never duplicated in routes or UI.</action>
  <verify>
    <automated>pnpm test tests/program/adaptive-coaching-policy.test.ts --runInBand</automated>
  </verify>
  <done>Every recommendation action is normalized into SAFE-01-compliant progression bounds with deterministic outputs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add SAFE-02 limitation conflict detector and warning envelope</name>
  <files>src/lib/adaptive-coaching/policy.ts, tests/program/adaptive-coaching-policy.test.ts</files>
  <behavior>
    - Test 1: Recommendation targeting flagged movement pattern produces `limitationConflict: true` with warning reason code.
    - Test 2: Conflict case returns warning metadata but recommendation remains user-actionable.
    - Test 3: No-conflict case emits empty warning envelope.
  </behavior>
  <action>Implement deterministic comparison between recommendation movement tags/equipment demands and athlete limitation or pain flags from profile context. Emit standardized warning payload fields consumed by service/UI without route-local reinterpretation.</action>
  <verify>
    <automated>pnpm test tests/program/adaptive-coaching-policy.test.ts --runInBand</automated>
  </verify>
  <done>SAFE-02 warning semantics are stable, explainable, and reusable across APIs/dashboard views.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add SAFE-03 confidence gate and conservative fallback selection</name>
  <files>src/lib/adaptive-coaching/confidence.ts, src/server/services/adaptive-coaching-policy.ts, tests/program/adaptive-coaching-fallback.test.ts</files>
  <behavior>
    - Test 1: Invalid or low-confidence recommendation triggers fallback path.
    - Test 2: Fallback prefers last applied recommendation only when it re-passes SAFE-01 bounds.
    - Test 3: If reuse candidate is unsafe/missing, fallback resolves to conservative `hold` with prudence flag.
  </behavior>
  <action>Implement confidence composition blending model confidence and deterministic context-quality checks, then apply fallback precedence exactly as locked in context: reuse only conservative prior recommendation, otherwise hold. Return explicit fallback reason codes for dashboard prudence rendering.</action>
  <verify>
    <automated>pnpm test tests/program/adaptive-coaching-fallback.test.ts --runInBand</automated>
  </verify>
  <done>SAFE-03 behavior is deterministic, fully test-covered, and provides explicit fallback metadata for downstream consumers.</done>
</task>

</tasks>

<verification>
Run policy and fallback suites to validate clamp, warning, and fallback behavior against requirement scenarios and edge cases.
</verification>

<success_criteria>
- Conservative progression bounds are hard-enforced for all recommendation actions.
- Limitation conflict warnings are produced consistently without blocking user agency.
- Low-confidence/invalid outputs always resolve to a conservative, explainable fallback.
</success_criteria>

<output>
After completion, create `.planning/phases/05-adaptive-coaching-and-safety-guardrails/05-02-SUMMARY.md`.
</output>
