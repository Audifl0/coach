---
phase: 05-adaptive-coaching-and-safety-guardrails
plan: 05-04
type: execute
wave: 3
depends_on:
  - 05-03
files_modified:
  - src/server/services/adaptive-coaching.ts
  - src/app/api/program/adaptation/[recommendationId]/confirm/route.ts
  - src/app/api/program/adaptation/[recommendationId]/reject/route.ts
  - src/app/(private)/dashboard/components/adaptive-confirmation-banner.tsx
  - tests/program/adaptive-coaching-confirm-route.test.ts
autonomous: true
requirements:
  - ADAP-03
  - SAFE-01
  - SAFE-03
must_haves:
  truths:
    - "High-impact actions (deload and substitution) are never auto-applied and always require explicit user confirmation."
    - "Confirmation validity is scoped to the target next planned session and expires outside that scope."
    - "Reject flow yields conservative safe outcome without bypassing SAFE policy constraints."
  artifacts:
    - path: src/app/api/program/adaptation/[recommendationId]/confirm/route.ts
      provides: "Endpoint to accept pending high-impact recommendation with session-scoped validity checks."
      exports:
        - POST
    - path: src/app/api/program/adaptation/[recommendationId]/reject/route.ts
      provides: "Endpoint to reject pending high-impact recommendation and apply conservative fallback outcome."
      exports:
        - POST
    - path: src/app/(private)/dashboard/components/adaptive-confirmation-banner.tsx
      provides: "Dashboard banner presenting reason, impact summary, and accept/reject actions for pending recommendation."
  key_links:
    - from: src/server/services/adaptive-coaching.ts
      to: src/app/api/program/adaptation/[recommendationId]/confirm/route.ts
      via: "confirmation-state transition validation"
      pattern: "confirmAdaptiveRecommendation"
    - from: src/server/services/adaptive-coaching.ts
      to: src/app/api/program/adaptation/[recommendationId]/reject/route.ts
      via: "rejection-to-conservative-outcome transition"
      pattern: "rejectAdaptiveRecommendation"
---

<objective>
Implement explicit confirmation governance for high-impact recommendations and expose a clear dashboard decision surface.

Purpose: Fulfill ADAP-03 while preserving conservative fallback semantics and session-scoped validity.
Output: confirm/reject APIs, service transitions, and confirmation banner UI with automated coverage.
</objective>

<execution_context>
@/home/flo/.codex/get-shit-done/workflows/execute-plan.md
@/home/flo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-adaptive-coaching-and-safety-guardrails/05-03-PLAN.md
@.planning/phases/05-adaptive-coaching-and-safety-guardrails/05-CONTEXT.md
@src/app/(private)/dashboard/page.tsx
@src/server/dal/adaptive-coaching.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add confirmation/rejection service transitions with one-session validity enforcement</name>
  <files>src/server/services/adaptive-coaching.ts, tests/program/adaptive-coaching-confirm-route.test.ts</files>
  <behavior>
    - Test 1: `deload` and `substitution` recommendations are saved as `pending_confirmation` and not `applied`.
    - Test 2: Confirm call fails when recommendation is expired, not pending, or no longer tied to next session.
    - Test 3: Reject call transitions to conservative outcome with explicit rejection reason trace.
  </behavior>
  <action>Implement `confirmAdaptiveRecommendation` and `rejectAdaptiveRecommendation` service methods enforcing ADAP-03 scope rules (`plannedSessionId` + `expiresAt`). Ensure both paths append decision-trace entries and keep SAFE compliance on resulting applied recommendation.</action>
  <verify>
    <automated>pnpm test tests/program/adaptive-coaching-confirm-route.test.ts --runInBand</automated>
  </verify>
  <done>High-impact recommendation lifecycle cannot be applied without explicit, session-valid confirmation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement confirm/reject API endpoints with ownership masking</name>
  <files>src/app/api/program/adaptation/[recommendationId]/confirm/route.ts, src/app/api/program/adaptation/[recommendationId]/reject/route.ts, tests/program/adaptive-coaching-confirm-route.test.ts</files>
  <behavior>
    - Test 1: Authenticated owner can confirm pending recommendation and receives updated state payload.
    - Test 2: Authenticated owner can reject pending recommendation and receives conservative applied state.
    - Test 3: Non-owner or missing recommendation responses are masked as not-found.
  </behavior>
  <action>Create both route handlers using parse-validated input, authenticated account scope, and service transition methods. Keep route behavior consistent with existing private program APIs (auth-first, ownership masking, strict contract outputs).</action>
  <verify>
    <automated>pnpm test tests/program/adaptive-coaching-confirm-route.test.ts --runInBand</automated>
  </verify>
  <done>ADAP-03 confirmation workflow is executable via API with secure ownership boundaries.</done>
</task>

<task type="auto">
  <name>Task 3: Add dashboard confirmation banner for pending high-impact recommendations</name>
  <files>src/app/(private)/dashboard/components/adaptive-confirmation-banner.tsx, src/app/(private)/dashboard/page.tsx</files>
  <action>Render an explicit dashboard banner when latest recommendation status is `pending_confirmation`, including concise reason, impact summary, one-session validity text, and accept/reject actions wired to new endpoints. Keep behavior deterministic and avoid applying changes locally before server confirmation succeeds.</action>
  <verify>
    <automated>pnpm test tests/program/adaptive-coaching-confirm-route.test.ts --runInBand</automated>
  </verify>
  <done>Users can clearly review and explicitly accept/reject high-impact recommendations from dashboard flow.</done>
</task>

</tasks>

<verification>
Run confirmation route tests to validate status transitions, expiry/session scoping, and ownership masking.
</verification>

<success_criteria>
- Deload/substitution remain pending until explicit user decision.
- Confirm/reject routes enforce next-session-only validity and secure ownership boundaries.
- Dashboard exposes a clear confirmation UX for high-impact adaptations.
</success_criteria>

<output>
After completion, create `.planning/phases/05-adaptive-coaching-and-safety-guardrails/05-04-SUMMARY.md`.
</output>
