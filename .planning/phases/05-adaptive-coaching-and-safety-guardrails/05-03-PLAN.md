---
phase: 05-adaptive-coaching-and-safety-guardrails
plan: 05-03
type: execute
wave: 2
depends_on:
  - 05-01
  - 05-02
files_modified:
  - src/lib/adaptive-coaching/evidence.ts
  - src/lib/adaptive-coaching/orchestrator.ts
  - src/server/services/adaptive-coaching.ts
  - src/app/api/program/adaptation/route.ts
  - tests/program/adaptive-coaching-service.test.ts
autonomous: true
requirements:
  - ADAP-01
  - ADAP-02
  - SAFE-03
must_haves:
  truths:
    - "Recommendation generation is LLM-first but always passes through deterministic server-side policy gates before persistence."
    - "Every surfaced recommendation includes 2-3 explicit user-facing reasons and short source references derived from local corpus evidence."
    - "Invalid model output and missing-evidence conditions route through SAFE-03 fallback instead of raw model responses."
  artifacts:
    - path: src/lib/adaptive-coaching/orchestrator.ts
      provides: "End-to-end recommendation orchestration pipeline (context assembly, retrieval, model call, policy/fallback gating)."
      exports:
        - generateAdaptiveRecommendation
    - path: src/lib/adaptive-coaching/evidence.ts
      provides: "Top-k local corpus retrieval and evidence-reference packaging for explanation payloads."
      exports:
        - retrieveAdaptiveEvidence
    - path: src/app/api/program/adaptation/route.ts
      provides: "Authenticated recommendation generation endpoint returning validated recommendation + explanation + warning/fallback flags."
      exports:
        - POST
  key_links:
    - from: src/lib/adaptive-coaching/orchestrator.ts
      to: src/lib/adaptive-coaching/policy.ts
      via: "fixed guardrail ordering enforcement"
      pattern: "applyAdaptiveSafetyPolicy"
    - from: src/server/services/adaptive-coaching.ts
      to: src/lib/adaptive-coaching/evidence.ts
      via: "evidence-backed explanation assembly"
      pattern: "retrieveAdaptiveEvidence"
---

<objective>
Deliver the adaptive recommendation engine and API path that produce explainable next-session actions from latest training context.

Purpose: Fulfill ADAP-01/ADAP-02 through a policy-governed LLM orchestration flow.
Output: service/orchestrator/evidence modules plus generation endpoint and automated integration tests.
</objective>

<execution_context>
@/home/flo/.codex/get-shit-done/workflows/execute-plan.md
@/home/flo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-adaptive-coaching-and-safety-guardrails/05-02-PLAN.md
@.planning/phases/05-adaptive-coaching-and-safety-guardrails/05-CONTEXT.md
@.planning/phases/05-adaptive-coaching-and-safety-guardrails/05-RESEARCH.md
@src/server/dal/program.ts
@src/lib/program/select-today-session.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build local-corpus evidence retrieval and explanation packaging</name>
  <files>src/lib/adaptive-coaching/evidence.ts, tests/program/adaptive-coaching-service.test.ts</files>
  <behavior>
    - Test 1: Retrieval returns top-k evidence snippets with stable short IDs and source class tags.
    - Test 2: Explanation envelope creation fails when fewer than 2 reasons or no evidence references are provided.
    - Test 3: Missing corpus hits lower confidence input to SAFE-03 gate.
  </behavior>
  <action>Implement retrieval from local versioned corpus index (guideline/review/expertise classes) and format evidence references for recommendation explanations. Keep retrieval deterministic and bounded (`top-k`) with explicit priority ordering to avoid random explanation drift.</action>
  <verify>
    <automated>pnpm test tests/program/adaptive-coaching-service.test.ts --runInBand</automated>
  </verify>
  <done>Service can attach traceable evidence references and explanation primitives to every recommendation candidate.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement LLM-first orchestration pipeline with fixed guardrail ordering</name>
  <files>src/lib/adaptive-coaching/orchestrator.ts, src/server/services/adaptive-coaching.ts, tests/program/adaptive-coaching-service.test.ts</files>
  <behavior>
    - Test 1: Orchestrator executes steps in order: parse -> integrity checks -> SAFE-01/02 -> SAFE-03 -> status assignment.
    - Test 2: Valid model proposal produces `progress|hold|deload|substitution` recommendation with 2-3 reasons.
    - Test 3: Invalid structured output enters fallback path and still returns contract-valid recommendation payload.
  </behavior>
  <action>Assemble athlete context (profile constraints, limitation flags, recent logs/history, readiness/fatigue, adherence), call LLM using strict structured output, parse through contracts, then apply policy/fallback modules in the fixed order from research. Persist final recommendation state through adaptive DAL and include decision-trace rows.</action>
  <verify>
    <automated>pnpm test tests/program/adaptive-coaching-service.test.ts --runInBand && pnpm test tests/program/adaptive-coaching-fallback.test.ts --runInBand</automated>
  </verify>
  <done>Recommendation generation is end-to-end operational with deterministic safety governance and auditable traces.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Expose authenticated adaptation generation endpoint</name>
  <files>src/app/api/program/adaptation/route.ts, tests/program/adaptive-coaching-service.test.ts</files>
  <behavior>
    - Test 1: Authenticated request returns latest validated recommendation with explanation and warning/fallback metadata.
    - Test 2: Cross-account request path is ownership-masked as not-found.
    - Test 3: Endpoint response schema remains parse-validated and rejects malformed service payloads.
  </behavior>
  <action>Create `POST /api/program/adaptation` route that derives account scope from authenticated session, invokes adaptive coaching service, and returns parse-validated recommendation payload for dashboard consumption. Preserve existing authorization and not-found masking standards used by program routes.</action>
  <verify>
    <automated>pnpm test tests/program/adaptive-coaching-service.test.ts --runInBand</automated>
  </verify>
  <done>ADAP-01 recommendation generation is available via authenticated API with explicit ADAP-02 explanation payloads.</done>
</task>

</tasks>

<verification>
Run adaptive service integration tests and fallback tests to verify generation pipeline correctness, ordering, and explainability outputs.
</verification>

<success_criteria>
- Authenticated users can request next-session recommendations generated from latest logs/context.
- Responses always include explicit reasons and short source references.
- Invalid/low-confidence model responses are converted into conservative contract-valid outputs.
</success_criteria>

<output>
After completion, create `.planning/phases/05-adaptive-coaching-and-safety-guardrails/05-03-SUMMARY.md`.
</output>
