---
phase: 05-adaptive-coaching-and-safety-guardrails
plan: 05-05
type: execute
wave: 4
depends_on:
  - 05-03
  - 05-04
files_modified:
  - src/lib/adaptive-coaching/forecast.ts
  - src/app/(private)/dashboard/components/adaptive-forecast-card.tsx
  - src/app/(private)/dashboard/page.tsx
  - .planning/knowledge/adaptive-coaching/index.json
  - .planning/knowledge/adaptive-coaching/principles.json
  - scripts/adaptive-knowledge/refresh-corpus.ts
  - tests/program/dashboard-adaptive-forecast.test.ts
autonomous: true
requirements:
  - DASH-03
  - ADAP-02
  - SAFE-02
  - SAFE-03
must_haves:
  truths:
    - "Dashboard always shows an upcoming-session forecast derived from the latest validated adaptive recommendation state."
    - "When warning or fallback flags are active, dashboard renders explicit `Prevision prudente` state with traceable rationale."
    - "Local scientific corpus remains versioned, refreshable, and quality-checked through automated ingestion checks."
  artifacts:
    - path: src/lib/adaptive-coaching/forecast.ts
      provides: "Forecast view-model builder that converts recommendation states to dashboard-ready projections."
      exports:
        - buildAdaptiveForecastViewModel
    - path: src/app/(private)/dashboard/components/adaptive-forecast-card.tsx
      provides: "Dashboard card rendering recommendation action, reasons, source tags, and prudence messaging."
    - path: scripts/adaptive-knowledge/refresh-corpus.ts
      provides: "Automated corpus refresh script with metadata validation and conflict checks."
  key_links:
    - from: src/lib/adaptive-coaching/forecast.ts
      to: src/app/(private)/dashboard/components/adaptive-forecast-card.tsx
      via: "forecast projection contract"
      pattern: "buildAdaptiveForecastViewModel"
    - from: .planning/knowledge/adaptive-coaching/index.json
      to: src/lib/adaptive-coaching/evidence.ts
      via: "retrieval metadata source of truth"
      pattern: "source_type|evidence_level|freshness_days"
---

<objective>
Finalize adaptive coaching user visibility and governance with a robust forecast surface and auditable knowledge-refresh workflow.

Purpose: Fulfill DASH-03 and make safety/fallback reasoning explicit in daily dashboard usage.
Output: forecast projection/rendering, prudent-card behavior, and corpus automation checks.
</objective>

<execution_context>
@/home/flo/.codex/get-shit-done/workflows/execute-plan.md
@/home/flo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-adaptive-coaching-and-safety-guardrails/05-03-PLAN.md
@.planning/phases/05-adaptive-coaching-and-safety-guardrails/05-04-PLAN.md
@.planning/phases/05-adaptive-coaching-and-safety-guardrails/05-CONTEXT.md
@.planning/phases/05-adaptive-coaching-and-safety-guardrails/05-RESEARCH.md
@src/app/(private)/dashboard/page.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement forecast projection builder from adaptive recommendation state</name>
  <files>src/lib/adaptive-coaching/forecast.ts, tests/program/dashboard-adaptive-forecast.test.ts</files>
  <behavior>
    - Test 1: Applied recommendation yields upcoming-session forecast with action, bounded progression, reasons, and source tags.
    - Test 2: Pending confirmation yields forecast marked as awaiting user decision.
    - Test 3: Warning or fallback flags force `previsionPrudente` display state with explicit prudence reason message.
  </behavior>
  <action>Create a dedicated forecast view-model builder that transforms recommendation lifecycle state into a stable dashboard payload. Ensure warning/fallback metadata is preserved and surfaced as a first-class state rather than implicit UI inference.</action>
  <verify>
    <automated>pnpm test tests/program/dashboard-adaptive-forecast.test.ts --runInBand</automated>
  </verify>
  <done>Dashboard consumers have one canonical forecast projection contract including prudence semantics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add dashboard adaptive forecast card with prudent-state rendering</name>
  <files>src/app/(private)/dashboard/components/adaptive-forecast-card.tsx, src/app/(private)/dashboard/page.tsx, tests/program/dashboard-adaptive-forecast.test.ts</files>
  <behavior>
    - Test 1: Dashboard displays forecast card for next session when adaptive recommendation exists.
    - Test 2: `Prevision prudente` card variant appears when fallback or limitation warning is active.
    - Test 3: Forecast card displays 2-3 reasons plus short evidence source tags.
  </behavior>
  <action>Integrate adaptive forecast card into dashboard composition using server-provided projection data only. Keep rendering compact/actionable and include explicit microcopy for prudence state while preserving existing dashboard route behavior.</action>
  <verify>
    <automated>pnpm test tests/program/dashboard-adaptive-forecast.test.ts --runInBand</automated>
  </verify>
  <done>DASH-03 is delivered with explicit prudent-state UX aligned with SAFE-02/SAFE-03 transparency requirements.</done>
</task>

<task type="auto">
  <name>Task 3: Add automated local-corpus refresh script with quality gates</name>
  <files>.planning/knowledge/adaptive-coaching/index.json, .planning/knowledge/adaptive-coaching/principles.json, scripts/adaptive-knowledge/refresh-corpus.ts</files>
  <action>Create a refresh script that updates local corpus metadata and principles from curated inputs while enforcing required fields (`url`, `date`, `source_type`, `evidence_level`), duplicate detection, and contradiction warnings. Persist results in versioned JSON artifacts consumed by evidence retrieval modules.</action>
  <verify>
    <automated>pnpm tsx scripts/adaptive-knowledge/refresh-corpus.ts --check</automated>
  </verify>
  <done>Scientific corpus governance is automated, auditable, and aligned with locked requirement for regular quality verification.</done>
</task>

</tasks>

<verification>
Run forecast integration tests and corpus refresh checks to confirm dashboard state fidelity and governance automation behavior.
</verification>

<success_criteria>
- Upcoming adaptive forecast is visible on dashboard and reflects latest recommendation state.
- `Prevision prudente` appears whenever warning/fallback conditions are active.
- Corpus refresh pipeline validates metadata quality and produces versioned local artifacts for retrieval.
</success_criteria>

<output>
After completion, create `.planning/phases/05-adaptive-coaching-and-safety-guardrails/05-05-SUMMARY.md`.
</output>
