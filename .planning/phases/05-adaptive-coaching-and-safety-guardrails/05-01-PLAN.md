---
phase: 05-adaptive-coaching-and-safety-guardrails
plan: 05-01
type: execute
wave: 0
depends_on: []
files_modified:
  - prisma/schema.prisma
  - prisma/migrations/*_adaptive_recommendation_foundation/migration.sql
  - src/lib/adaptive-coaching/contracts.ts
  - src/lib/adaptive-coaching/types.ts
  - src/server/dal/adaptive-coaching.ts
  - tests/program/adaptive-coaching-contracts.test.ts
autonomous: true
requirements:
  - ADAP-01
  - ADAP-02
  - ADAP-03
  - SAFE-03
must_haves:
  truths:
    - "Adaptive recommendation payloads are contract-validated with strict enums for action/state and explicit explanation fields."
    - "Persistence primitives exist for recommendation lifecycle, confirmation scope, warnings, confidence, and fallback auditability."
    - "High-impact confirmation state can be represented as pending and bounded to a single target planned session."
  artifacts:
    - path: prisma/schema.prisma
      provides: "Adaptive recommendation and decision-trace models linked to account-scoped program entities."
      contains: "model AdaptiveRecommendation"
    - path: src/lib/adaptive-coaching/contracts.ts
      provides: "Shared Zod schemas and parse helpers for LLM proposal, validated recommendation, and confirmation payloads."
      exports:
        - parseAdaptiveRecommendationProposal
        - parseAdaptiveRecommendation
        - parseAdaptiveConfirmationInput
    - path: src/server/dal/adaptive-coaching.ts
      provides: "Account-scoped CRUD/state-transition DAL primitives for adaptive recommendations."
      exports:
        - createAdaptiveRecommendation
        - getAdaptiveRecommendationById
        - updateAdaptiveRecommendationStatus
  key_links:
    - from: src/lib/adaptive-coaching/contracts.ts
      to: src/server/services/adaptive-coaching.ts
      via: "service boundary parse/serialize contract"
      pattern: "parseAdaptiveRecommendation|parseAdaptiveRecommendationProposal"
    - from: src/server/dal/adaptive-coaching.ts
      to: src/app/api/program/adaptation/route.ts
      via: "route persistence entrypoint"
      pattern: "createAdaptiveRecommendation|listLatestAdaptiveRecommendation"
---

<objective>
Create the foundational contract and persistence layer for adaptive coaching so every later plan builds on explicit, test-backed interfaces.

Purpose: Lock stable recommendation data shapes and lifecycle states before policy/LLM/UI wiring.
Output: Schema migration, shared contracts, DAL skeleton, and contract tests.
</objective>

<execution_context>
@/home/flo/.codex/get-shit-done/workflows/execute-plan.md
@/home/flo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-adaptive-coaching-and-safety-guardrails/05-CONTEXT.md
@.planning/phases/05-adaptive-coaching-and-safety-guardrails/05-RESEARCH.md
@prisma/schema.prisma
@src/lib/program/contracts.ts
@src/server/dal/program.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add adaptive recommendation persistence models and migration</name>
  <files>prisma/schema.prisma, prisma/migrations/*_adaptive_recommendation_foundation/migration.sql</files>
  <action>Add `AdaptiveRecommendation` and append-only `AdaptiveRecommendationDecision` models with fields for `actionType`, `status`, progression deltas, substitution target, confidence values, warning/fallback flags, reasons/evidence JSON payloads, and `expiresAt` for one-session confirmation validity. Link records to account-scoped program entities (`userId`, `plannedSessionId`) and add indexes supporting latest-recommendation reads and state transitions.</action>
  <verify>
    <automated>pnpm prisma migrate dev --name adaptive_recommendation_foundation --create-only</automated>
  </verify>
  <done>Database schema can store the complete recommendation lifecycle, including pending confirmation and fallback traceability.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Define adaptive coaching contracts and parse helpers</name>
  <files>src/lib/adaptive-coaching/contracts.ts, src/lib/adaptive-coaching/types.ts, tests/program/adaptive-coaching-contracts.test.ts</files>
  <behavior>
    - Test 1: Proposal parser accepts only `progress|hold|deload|substitution` actions and rejects unknown action/state values.
    - Test 2: Recommendation parser requires 2-3 user-facing reasons and at least one short evidence reference tag.
    - Test 3: Confirmation parser enforces explicit `accept|reject` decision and target `recommendationId`.
    - Test 4: Pending-confirmation payload requires `expiresAt` and `plannedSessionId` linkage.
  </behavior>
  <action>Write failing tests first, then implement Zod contracts and parse helpers for proposal, validated recommendation, forecast projection payload, and confirmation input. Keep strict parsing behavior aligned with existing route contract patterns (throwing on invalid boundaries).</action>
  <verify>
    <automated>pnpm test tests/program/adaptive-coaching-contracts.test.ts --runInBand</automated>
  </verify>
  <done>Adaptive coaching contracts provide the canonical typed boundary for service, routes, and dashboard payloads.</done>
</task>

<task type="auto">
  <name>Task 3: Create account-scoped adaptive recommendation DAL scaffolding</name>
  <files>src/server/dal/adaptive-coaching.ts</files>
  <action>Implement DAL primitives for create/read/update of adaptive recommendations with account ownership filters and not-found masking semantics matching existing program DAL conventions. Include dedicated methods for status transitions (`validated`, `pending_confirmation`, `applied`, `rejected`, `fallback_applied`) and append decision-trace rows for policy/execution steps.</action>
  <verify>
    <automated>pnpm test tests/program/adaptive-coaching-contracts.test.ts --runInBand</automated>
  </verify>
  <done>Downstream services can persist and transition recommendations without bypassing account scope or lifecycle constraints.</done>
</task>

</tasks>

<verification>
Generate migration artifacts and run adaptive contract tests to confirm the foundation supports strict lifecycle and explanation requirements.
</verification>

<success_criteria>
- Recommendation lifecycle states and confirmation expiry primitives exist in persistence.
- Shared adaptive contract parsers enforce explicit reasons/evidence and strict action enums.
- DAL scaffolding exposes account-scoped create/read/transition primitives for Phase 05 services.
</success_criteria>

<output>
After completion, create `.planning/phases/05-adaptive-coaching-and-safety-guardrails/05-01-SUMMARY.md`.
</output>
