---
phase: 01-platform-foundation-security-and-authentication
plan: 3
type: execute
wave: 4
depends_on:
  - "01-02"
files_modified:
  - scripts/admin-reset-password.ts
  - src/lib/auth/admin-reset.ts
  - src/server/dal/account-scope.ts
  - tests/auth/admin-reset.test.ts
  - tests/security/account-isolation.test.ts
  - docs/operations/auth-recovery.md
  - docs/security/phase-1-baseline.md
autonomous: true
requirements:
  - AUTH-03
  - PLAT-03
must_haves:
  truths:
    - "Account access can be restored through a manual admin reset flow."
    - "Password reset invalidates active sessions so old tokens cannot continue."
    - "Protected data queries are rejected when account context is missing or mismatched."
    - "Operational recovery and security baseline are documented for repeatable use."
  artifacts:
    - path: "scripts/admin-reset-password.ts"
      provides: "Admin CLI entrypoint for manual password reset"
      contains: "username"
    - path: "src/lib/auth/admin-reset.ts"
      provides: "Reset service revoking sessions after password update"
      exports: ["resetPasswordByAdmin"]
    - path: "docs/operations/auth-recovery.md"
      provides: "Recovery runbook for personal VPS operations"
      contains: "reset steps"
  key_links:
    - from: "scripts/admin-reset-password.ts"
      to: "src/lib/auth/admin-reset.ts"
      via: "CLI calls reset service"
      pattern: "resetPasswordByAdmin"
    - from: "src/lib/auth/admin-reset.ts"
      to: "session store"
      via: "session revocation after hash update"
      pattern: "revoke"
---

<objective>
Deliver the manual admin recovery flow and account-isolation hardening needed to safely operate phase-1 authentication in production-like usage.

Purpose: Close recovery and security gaps that are critical for baseline safety while keeping the solution pragmatic for personal use.
Output: Admin reset CLI/service, session revocation on reset, isolation regression tests, and operator/security documentation.
</objective>

<execution_context>
@/home/flo/.codex/get-shit-done/workflows/execute-plan.md
@/home/flo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-platform-foundation-security-and-authentication/01-CONTEXT.md
@.planning/phases/01-platform-foundation-security-and-authentication/01-RESEARCH.md
@.planning/phases/01-platform-foundation-security-and-authentication/01-02-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement admin password reset service with session revocation</name>
  <files>src/lib/auth/admin-reset.ts, scripts/admin-reset-password.ts, tests/auth/admin-reset.test.ts</files>
  <behavior>
    - Admin reset updates password hash for the target username.
    - All active sessions for that account are revoked immediately.
    - Reset command does not leak whether non-existent usernames are present.
  </behavior>
  <action>Build a manual reset service and CLI script for server-side operation. Require explicit operator input and clear audit-safe logs (no plaintext secrets). Ensure reset always revokes active sessions tied to the account and follows generic error policy.</action>
  <verify>
    <automated>pnpm test tests/auth/admin-reset.test.ts</automated>
  </verify>
  <done>Manual reset workflow is executable from CLI and proven by tests to rotate credentials plus invalidate prior sessions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce and validate strict account isolation at DAL boundary</name>
  <files>src/server/dal/account-scope.ts, tests/security/account-isolation.test.ts</files>
  <behavior>
    - Data access without an authenticated account context is rejected.
    - Data access with mismatched account context is rejected.
    - Valid account context allows only account-owned data queries.
  </behavior>
  <action>Strengthen the account-scope guard introduced in plan 01-01 to enforce fail-closed behavior for missing/mismatched context. Add tests covering allowed and denied query paths so future feature plans inherit safe defaults.</action>
  <verify>
    <automated>pnpm test tests/security/account-isolation.test.ts</automated>
  </verify>
  <done>DAL account isolation rules are explicit, fail-closed, and protected by regression tests.</done>
</task>

<task type="auto">
  <name>Task 3: Document recovery and baseline security operations</name>
  <files>docs/operations/auth-recovery.md, docs/security/phase-1-baseline.md</files>
  <action>Write concise operator docs for manual password reset and phase-1 baseline controls (transport security expectations, password hashing policy, session cookie flags, account isolation). Keep scope aligned with deferred items: no email reset and no advanced brute-force monitoring in this phase.</action>
  <verify>
    <automated>pnpm exec markdownlint docs/operations/auth-recovery.md docs/security/phase-1-baseline.md</automated>
  </verify>
  <done>Docs provide reproducible operational steps and clearly state the implemented security baseline boundaries.</done>
</task>

</tasks>

<verification>
Recovery and isolation are complete when reset + isolation tests pass and docs match implemented behavior without scope drift.
</verification>

<success_criteria>
- AUTH-03 is satisfied via tested manual admin reset workflow.
- Session revocation on password reset is enforced and test-proven.
- Account-level isolation guardrails are present before later user-data phases.
</success_criteria>

<output>
After completion, create `.planning/phases/01-platform-foundation-security-and-authentication/01-03-SUMMARY.md`
</output>
