---
phase: 01-platform-foundation-security-and-authentication
plan: 2
type: execute
wave: 3
depends_on:
  - "01-05"
files_modified:
  - src/lib/auth/auth.ts
  - src/lib/auth/password.ts
  - src/app/api/auth/signup/route.ts
  - src/app/api/auth/login/route.ts
  - tests/auth/session-lifecycle.test.ts
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - PLAT-03
must_haves:
  truths:
    - "User can create an account with username and password."
    - "Valid login issues a persistent secure session suitable for revisit flow."
    - "Login failures return generic errors that do not leak account existence."
  artifacts:
    - path: "src/app/api/auth/signup/route.ts"
      provides: "Signup API with validation and hash persistence"
      exports: ["POST"]
    - path: "src/app/api/auth/login/route.ts"
      provides: "Login API with persistent secure session cookie issuance"
      exports: ["POST"]
    - path: "tests/auth/session-lifecycle.test.ts"
      provides: "Automated backend auth lifecycle coverage"
      contains: "signup and login"
  key_links:
    - from: "src/app/api/auth/login/route.ts"
      to: "src/lib/auth/auth.ts"
      via: "session creation"
      pattern: "createSession"
---

<objective>
Implement backend account creation and sign-in behavior for username/password authentication with persistent sessions.

Purpose: Deliver the core auth service and API boundary before UI and middleware wiring.
Output: Auth/password services, signup/login routes, and backend lifecycle tests.
</objective>

<execution_context>
@/home/flo/.codex/get-shit-done/workflows/execute-plan.md
@/home/flo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-platform-foundation-security-and-authentication/01-CONTEXT.md
@.planning/phases/01-platform-foundation-security-and-authentication/01-RESEARCH.md
@.planning/phases/01-platform-foundation-security-and-authentication/01-05-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement username/password auth services and API handlers</name>
  <files>src/lib/auth/auth.ts, src/lib/auth/password.ts, src/app/api/auth/signup/route.ts, src/app/api/auth/login/route.ts</files>
  <behavior>
    - Valid signup creates account with hashed password and rejects duplicate username.
    - Valid login returns authenticated session context and secure cookie metadata.
    - Invalid credentials always return a generic auth error message.
  </behavior>
  <action>Implement auth service and password hashing helpers with minimal password policy (minimum length only). Build signup/login route handlers with Zod validation and generic failure responses. Configure session expiry target around 30 days and rolling update policy consistent with locked decisions.</action>
  <verify>
    <automated>pnpm test tests/auth/session-lifecycle.test.ts -t "signup and login"</automated>
  </verify>
  <done>Signup/login endpoints are operational, security-relevant responses stay generic, and tests pass for success/failure cases.</done>
</task>

<task type="auto">
  <name>Task 2: Lock backend auth lifecycle regressions</name>
  <files>tests/auth/session-lifecycle.test.ts</files>
  <action>Expand backend-focused lifecycle coverage for signup, duplicate username rejection, generic login failure messaging, and persistent session issuance semantics. Keep runtime short and deterministic for repeated local execution.</action>
  <verify>
    <automated>pnpm test tests/auth/session-lifecycle.test.ts</automated>
  </verify>
  <done>Automated tests enforce backend auth lifecycle behavior and prevent regressions before UI integration.</done>
</task>

</tasks>

<verification>
Run lifecycle tests to confirm backend signup/login semantics, generic failure policy, and persistent session issuance.
</verification>

<success_criteria>
- AUTH-01 backend behavior is demonstrably satisfied through passing automated tests.
- AUTH-02 backend session issuance behavior is implemented with persistent-cookie intent.
- Auth backend follows locked decisions (username-first, no email verification, generic errors).
</success_criteria>

<output>
After completion, create `.planning/phases/01-platform-foundation-security-and-authentication/01-02-SUMMARY.md`
</output>
