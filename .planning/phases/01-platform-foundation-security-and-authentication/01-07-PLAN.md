---
phase: 01-platform-foundation-security-and-authentication
plan: 7
type: execute
wave: 5
depends_on:
  - "01-02"
  - "01-06"
files_modified:
  - src/lib/auth/session-gate.ts
  - src/app/(private)/dashboard/page.tsx
  - src/middleware.ts
  - tests/auth/session-gate.test.ts
  - tests/auth/session-lifecycle.test.ts
autonomous: true
gap_closure: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - PLAT-01
  - PLAT-03
must_haves:
  truths:
    - "Private dashboard access requires an active, non-revoked, non-expired server-side session record."
    - "Forged, expired, and revoked session cookies are rejected and redirected to login with next path preservation."
    - "Current-session logout keeps other concurrent sessions valid while invalidating the logged-out session for private route access."
    - "HTTPS/VPS routing behavior for dashboard endpoints remains intact while auth enforcement is corrected."
  artifacts:
    - path: "src/lib/auth/session-gate.ts"
      provides: "Authoritative session validation from cookie token hash to active session record"
      contains: "sessionTokenHash"
    - path: "src/app/(private)/dashboard/page.tsx"
      provides: "Server-side authenticated dashboard gate using validated session context"
      contains: "redirect('/login?next=/dashboard')"
    - path: "src/middleware.ts"
      provides: "Anonymous prefilter + next preservation without treating cookie presence as authentication proof"
      contains: "matcher"
    - path: "tests/auth/session-gate.test.ts"
      provides: "Regression coverage for forged/revoked/expired/private-route gating scenarios"
      contains: "revoked"
  key_links:
    - from: "src/app/(private)/dashboard/page.tsx"
      to: "src/lib/auth/session-gate.ts"
      via: "server-side session validation before rendering private content"
      pattern: "validate"
    - from: "src/app/api/auth/logout/route.ts"
      to: "src/lib/auth/session-gate.ts"
      via: "revocation effect reflected in subsequent private-route access checks"
      pattern: "revokedAt"
---

<objective>
Close the verified auth-gating gap by replacing cookie-presence checks with authoritative session validation for private access.

Purpose: Ensure protected routes are gated by real session state (hash match + not revoked + not expired), not by a raw cookie string.
Output: Session-gate module, dashboard enforcement update, middleware prefilter hardening, and regression tests proving gap closure.
</objective>

<execution_context>
@/home/flo/.codex/get-shit-done/workflows/execute-plan.md
@/home/flo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-platform-foundation-security-and-authentication/01-CONTEXT.md
@.planning/phases/01-platform-foundation-security-and-authentication/01-RESEARCH.md
@.planning/phases/01-platform-foundation-security-and-authentication/VERIFICATION.md
@.planning/phases/01-platform-foundation-security-and-authentication/01-06-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build authoritative session gate and lock in failure-mode tests first</name>
  <files>src/lib/auth/session-gate.ts, tests/auth/session-gate.test.ts</files>
  <behavior>
    - A cookie token is authenticated only when its hash maps to a session row with revokedAt = null and expiresAt > now.
    - Missing/forged token returns unauthenticated without leaking account existence details.
    - Revoked and expired tokens return unauthenticated even if cookie is present.
  </behavior>
  <action>Create a reusable session-gate module that reads `coach_session`, hashes token with existing auth helper, and validates active session state through Prisma-backed lookup. Write tests first for valid, missing, forged, revoked, and expired token scenarios, then implement minimum code to pass.</action>
  <verify>
    <automated>corepack pnpm test tests/auth/session-gate.test.ts</automated>
  </verify>
  <done>Session authenticity is proven by persisted active session state, not by cookie presence, with explicit regression tests for each invalid state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce validated session gate in private route flow and update lifecycle regressions</name>
  <files>src/app/(private)/dashboard/page.tsx, src/middleware.ts, tests/auth/session-lifecycle.test.ts</files>
  <behavior>
    - Dashboard redirects to `/login?next=/dashboard` when session-gate validation fails.
    - Middleware remains an anonymous prefilter (missing cookie redirects) but does not claim cookie presence equals authenticated state.
    - After current-session logout, that token cannot access dashboard while another concurrent session can.
  </behavior>
  <action>Refactor dashboard gate to depend on the new session-gate module before rendering private content. Keep middleware lightweight for UX redirect behavior and preserve `next` path, but remove implied trust in raw cookie presence as authentication. Extend lifecycle tests to cover revoked-token denial and surviving concurrent-session access.</action>
  <verify>
    <automated>corepack pnpm test tests/auth/session-lifecycle.test.ts tests/auth/session-gate.test.ts</automated>
  </verify>
  <done>Private access is enforced by validated session state and the previously verified auth-gating gap is closed with automated coverage.</done>
</task>

</tasks>

<verification>
Automated tests must prove that revoked, expired, forged, or missing cookies are denied for private dashboard access, while a valid concurrent session remains accepted after current-session logout.
</verification>

<success_criteria>
- AUTH-02 private-route access reflects real persisted session validity instead of cookie existence.
- AUTH-03 reset/logout revocation semantics are reflected immediately in private-route gating.
- PLAT-03 auth boundary no longer allows stale/forged cookie bypass of protected pages.
- Existing VPS/HTTPS routing behavior for dashboard entrypoints remains operational (PLAT-01 unaffected).
</success_criteria>

<output>
After completion, create `.planning/phases/01-platform-foundation-security-and-authentication/01-07-SUMMARY.md`
</output>
