---
phase: 01-platform-foundation-security-and-authentication
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-lock.yaml
  - next.config.ts
  - tsconfig.json
  - .env.example
  - src/lib/db/prisma.ts
autonomous: true
requirements:
  - PLAT-03
must_haves:
  truths:
    - "Application can boot with a reproducible local foundation stack."
    - "Core runtime configuration for server-side auth and database access is established once."
    - "Environment template provides only baseline keys needed for phase-1 execution."
  artifacts:
    - path: "package.json"
      provides: "Pinned runtime/tooling dependencies for app foundation"
      contains: "scripts"
    - path: "src/lib/db/prisma.ts"
      provides: "Reusable Prisma client bootstrap with singleton handling"
      exports: ["prisma"]
  key_links:
    - from: "src/lib/db/prisma.ts"
      to: ".env.example"
      via: "DATABASE_URL runtime expectation"
      pattern: "DATABASE_URL"
---

<objective>
Create the minimal runtime and configuration foundation for phase-1 auth and platform work.

Purpose: Ensure all downstream plans build on a deterministic Next.js + TypeScript + Prisma baseline instead of re-creating setup.
Output: Bootstrapped project tooling/config, baseline env template, and shared Prisma client bootstrap.
</objective>

<execution_context>
@/home/flo/.codex/get-shit-done/workflows/execute-plan.md
@/home/flo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-platform-foundation-security-and-authentication/01-CONTEXT.md
@.planning/phases/01-platform-foundation-security-and-authentication/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bootstrap project runtime and baseline configuration</name>
  <files>package.json, pnpm-lock.yaml, next.config.ts, tsconfig.json, .env.example, src/lib/db/prisma.ts</files>
  <action>Initialize a Next.js + TypeScript project configured for server-side auth work and Prisma client bootstrapping. Keep dependencies minimal and aligned with research guidance (Better Auth, Prisma, Zod, pino). Include only baseline environment keys needed by this phase and avoid deferred hardening features.</action>
  <verify>
    <automated>pnpm install && pnpm exec tsc --noEmit</automated>
  </verify>
  <done>Project installs cleanly, TypeScript compiles, and environment template is sufficient to run subsequent phase-1 plans.</done>
</task>

</tasks>

<verification>
Foundation is valid when install and compile checks pass with deterministic project configuration.
</verification>

<success_criteria>
- Executors can run and build the project without redefining baseline tooling.
- Shared Prisma bootstrap and environment contract are available for subsequent plans.
- Scope remains strictly foundational with no auth business logic implemented yet.
</success_criteria>

<output>
After completion, create `.planning/phases/01-platform-foundation-security-and-authentication/01-01-SUMMARY.md`
</output>
