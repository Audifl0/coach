---
phase: 01-platform-foundation-security-and-authentication
plan: 5
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - prisma/schema.prisma
  - prisma/migrations/0001_init_auth/migration.sql
  - src/lib/auth/contracts.ts
  - src/server/dal/account-scope.ts
  - tests/auth/contracts.test.ts
autonomous: true
requirements:
  - AUTH-01
  - PLAT-03
must_haves:
  truths:
    - "Account identity is modeled as username plus hashed password, not email-first."
    - "Protected data access has an account-scope contract before feature logic is added."
    - "Auth payload/session contracts are explicit and type-checked at boundaries."
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "Initial auth-related data model"
      contains: "model User"
    - path: "src/lib/auth/contracts.ts"
      provides: "Typed auth payload and session contracts"
      exports: ["SignupInput", "LoginInput", "SessionContext"]
    - path: "src/server/dal/account-scope.ts"
      provides: "Central account-bound query guard"
      exports: ["requireAccountScope"]
  key_links:
    - from: "src/server/dal/account-scope.ts"
      to: "prisma/schema.prisma"
      via: "User id/account filter assumptions"
      pattern: "userId"
    - from: "tests/auth/contracts.test.ts"
      to: "src/lib/auth/contracts.ts"
      via: "Contract shape assertions"
      pattern: 'describe\("auth contracts"'
---

<objective>
Define the initial auth data model and server-side contracts required by downstream auth endpoint work.

Purpose: Isolate schema and contract decisions into a focused plan so application logic plans can consume stable interfaces.
Output: Prisma auth schema/migration, typed auth/session contracts, account-scope helper, and contract tests.
</objective>

<execution_context>
@/home/flo/.codex/get-shit-done/workflows/execute-plan.md
@/home/flo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-platform-foundation-security-and-authentication/01-CONTEXT.md
@.planning/phases/01-platform-foundation-security-and-authentication/01-RESEARCH.md
@.planning/phases/01-platform-foundation-security-and-authentication/01-01-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define username-first auth schema and migration</name>
  <files>prisma/schema.prisma, prisma/migrations/0001_init_auth/migration.sql</files>
  <behavior>
    - User model enforces username uniqueness.
    - Password hash is stored; plaintext password is never persisted.
    - Session model supports long-lived concurrent sessions.
  </behavior>
  <action>Create initial Prisma models for user/session with username-first credentials and migration SQL. Keep schema minimal and aligned with locked decisions (no email verification flow, no brute-force tables in this phase).</action>
  <verify>
    <automated>pnpm exec prisma validate</automated>
  </verify>
  <done>Schema and migration encode username/password identity with session support for phase-1 auth flows.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add auth contracts and account-scope guard with tests</name>
  <files>src/lib/auth/contracts.ts, src/server/dal/account-scope.ts, tests/auth/contracts.test.ts</files>
  <behavior>
    - Contract rejects missing username/password fields at boundary validation.
    - Session context type captures authenticated account identity.
    - Account-scope helper requires authenticated user context before data access.
  </behavior>
  <action>Implement typed auth contracts and a reusable DAL account-scope guard that downstream code must call before protected queries. Add tests that fail without required contract fields and pass with valid payloads.</action>
  <verify>
    <automated>pnpm test tests/auth/contracts.test.ts</automated>
  </verify>
  <done>Contracts and scope guard compile and tests confirm expected boundary behavior.</done>
</task>

</tasks>

<verification>
Schema validation and contract tests must pass before auth API implementation begins.
</verification>

<success_criteria>
- AUTH-01 prerequisites are codified as stable schema and API contracts.
- PLAT-03 baseline is reinforced through explicit account-scope boundary enforcement.
- Downstream plans can implement behavior without redefining auth interfaces.
</success_criteria>

<output>
After completion, create `.planning/phases/01-platform-foundation-security-and-authentication/01-05-SUMMARY.md`
</output>
