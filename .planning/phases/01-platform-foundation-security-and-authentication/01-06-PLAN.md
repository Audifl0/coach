---
phase: 01-platform-foundation-security-and-authentication
plan: 6
type: execute
wave: 4
depends_on:
  - "01-02"
files_modified:
  - src/app/(public)/signup/page.tsx
  - src/app/(public)/login/page.tsx
  - src/app/(private)/dashboard/page.tsx
  - src/app/api/auth/logout/route.ts
  - src/middleware.ts
  - tests/auth/session-lifecycle.test.ts
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - PLAT-03
must_haves:
  truths:
    - "Public auth screens allow account creation and sign-in using username/password."
    - "Authenticated session survives refresh/revisit and gates private routes."
    - "Logout invalidates only the current session while other sessions remain valid."
  artifacts:
    - path: "src/app/(public)/signup/page.tsx"
      provides: "Signup screen wired to auth API"
      contains: "username"
    - path: "src/app/(public)/login/page.tsx"
      provides: "Login screen wired to auth API"
      contains: "password"
    - path: "src/app/api/auth/logout/route.ts"
      provides: "Current-session logout endpoint"
      exports: ["POST"]
    - path: "src/middleware.ts"
      provides: "Authenticated route protection"
      contains: "matcher"
  key_links:
    - from: "src/middleware.ts"
      to: "src/app/(private)/dashboard/page.tsx"
      via: "route protection"
      pattern: "matcher"
    - from: "src/app/api/auth/logout/route.ts"
      to: "src/lib/auth/auth.ts"
      via: "current session revocation"
      pattern: "logout"
---

<objective>
Wire authentication UX, route protection, and current-device logout on top of backend auth APIs.

Purpose: Complete the user-visible auth lifecycle while keeping UI and middleware concerns isolated from backend API implementation.
Output: Signup/login pages, protected dashboard gate, logout route, middleware policy, and integration tests for persistence/logout behavior.
</objective>

<execution_context>
@/home/flo/.codex/get-shit-done/workflows/execute-plan.md
@/home/flo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-platform-foundation-security-and-authentication/01-CONTEXT.md
@.planning/phases/01-platform-foundation-security-and-authentication/01-RESEARCH.md
@.planning/phases/01-platform-foundation-security-and-authentication/01-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement auth UI screens and protected dashboard shell</name>
  <files>src/app/(public)/signup/page.tsx, src/app/(public)/login/page.tsx, src/app/(private)/dashboard/page.tsx, src/middleware.ts</files>
  <action>Create pragmatic signup/login pages wired to existing APIs and a minimal authenticated dashboard shell. Add middleware protection for private routes so anonymous access is redirected/denied while authenticated sessions proceed.</action>
  <verify>
    <automated>pnpm test tests/auth/session-lifecycle.test.ts -t "persistence"</automated>
  </verify>
  <done>User can access auth screens, authenticate, and reach protected dashboard routes with session persistence across refresh simulation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement current-session logout and finalize integration coverage</name>
  <files>src/app/api/auth/logout/route.ts, tests/auth/session-lifecycle.test.ts</files>
  <behavior>
    - Logout revokes only the active session token.
    - Logout clears session cookie for current device.
    - Other concurrent sessions remain valid.
  </behavior>
  <action>Implement logout endpoint semantics aligned with locked decision (current session only). Extend lifecycle tests to validate persistence plus current-session-only logout behavior end-to-end.</action>
  <verify>
    <automated>pnpm test tests/auth/session-lifecycle.test.ts -t "persistence and current-session logout"</automated>
  </verify>
  <done>Logout behavior and persistence guardrails are test-proven and aligned with low-friction multi-device usage.</done>
</task>

</tasks>

<verification>
Integration tests must prove protected-route gating, session persistence on revisit, and current-device-only logout semantics.
</verification>

<success_criteria>
- AUTH-01 and AUTH-02 user-visible flows are complete and validated through lifecycle tests.
- Session policy matches locked decisions (long-lived, multi-device allowed, current-session logout).
- PLAT-03 transport/session baseline is reinforced through secure route gating and cookie handling.
</success_criteria>

<output>
After completion, create `.planning/phases/01-platform-foundation-security-and-authentication/01-06-SUMMARY.md`
</output>
