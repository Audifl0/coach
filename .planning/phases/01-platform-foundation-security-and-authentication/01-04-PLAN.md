---
phase: 01-platform-foundation-security-and-authentication
plan: 4
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - Dockerfile
  - docker-compose.yml
  - infra/caddy/Caddyfile
  - infra/scripts/deploy.sh
  - infra/scripts/smoke-test-https.sh
  - infra/scripts/backup.sh
  - infra/scripts/restore.sh
  - docs/operations/vps-deploy.md
  - docs/operations/data-protection.md
autonomous: true
requirements:
  - PLAT-01
  - PLAT-03
must_haves:
  truths:
    - "Application is deployable to VPS behind HTTPS with automatic TLS lifecycle."
    - "Production traffic path enforces encrypted transport for auth and app routes."
    - "At-rest protection controls and backup/restore operations are documented and scriptable."
    - "Deployment has an automated smoke check validating HTTPS availability."
  artifacts:
    - path: "docker-compose.yml"
      provides: "VPS service topology (app, db, reverse proxy)"
      contains: "caddy"
    - path: "infra/caddy/Caddyfile"
      provides: "HTTPS termination and secure proxy routing"
      contains: "reverse_proxy"
    - path: "infra/scripts/smoke-test-https.sh"
      provides: "Automated endpoint verification over TLS"
      contains: "https://"
    - path: "docs/operations/data-protection.md"
      provides: "At-rest and backup protection baseline"
      contains: "encryption"
  key_links:
    - from: "infra/caddy/Caddyfile"
      to: "docker-compose.yml"
      via: "reverse proxy service reference"
      pattern: "app:"
    - from: "infra/scripts/deploy.sh"
      to: "infra/scripts/smoke-test-https.sh"
      via: "post-deploy verification step"
      pattern: "smoke-test-https"
---

<objective>
Establish VPS deployment and transport/storage protection baseline for phase-1 production access.

Purpose: Satisfy HTTPS reachability and baseline data protection requirements without overbuilding operations for a personal-use project.
Output: Containerized deployment stack, HTTPS reverse proxy config, deploy/smoke/backup scripts, and operations docs.
</objective>

<execution_context>
@/home/flo/.codex/get-shit-done/workflows/execute-plan.md
@/home/flo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-platform-foundation-security-and-authentication/01-CONTEXT.md
@.planning/phases/01-platform-foundation-security-and-authentication/01-RESEARCH.md
@.planning/phases/01-platform-foundation-security-and-authentication/01-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build containerized VPS runtime and HTTPS ingress</name>
  <files>Dockerfile, docker-compose.yml, infra/caddy/Caddyfile</files>
  <action>Create a production-oriented container setup for app and PostgreSQL behind Caddy automatic HTTPS on dedicated subdomain. Enforce TLS path by default and include secure proxy headers suitable for auth cookies and server logging.</action>
  <verify>
    <automated>docker compose config && docker compose build</automated>
  </verify>
  <done>Compose and Caddy configs are valid and produce deployable images with HTTPS ingress wiring.</done>
</task>

<task type="auto">
  <name>Task 2: Add deploy, smoke, backup, and restore scripts</name>
  <files>infra/scripts/deploy.sh, infra/scripts/smoke-test-https.sh, infra/scripts/backup.sh, infra/scripts/restore.sh</files>
  <action>Implement scripts for deterministic deploy/update, HTTPS smoke test, and encrypted backup/restore handling for VPS operations. Keep scripts minimal, idempotent where practical, and compatible with personal single-server usage.</action>
  <verify>
    <automated>bash -n infra/scripts/deploy.sh infra/scripts/smoke-test-https.sh infra/scripts/backup.sh infra/scripts/restore.sh</automated>
  </verify>
  <done>Operational scripts execute with valid syntax and cover routine deploy plus recovery workflows.</done>
</task>

<task type="auto">
  <name>Task 3: Document VPS deployment and data-protection baseline</name>
  <files>docs/operations/vps-deploy.md, docs/operations/data-protection.md</files>
  <action>Document end-to-end deployment steps, expected DNS/subdomain setup, TLS behavior, and concrete at-rest protection controls (database volume strategy, encrypted backups, secret handling). Include practical operator checks that correspond to scripts and avoid enterprise-only controls deferred for later phases.</action>
  <verify>
    <automated>pnpm exec markdownlint docs/operations/vps-deploy.md docs/operations/data-protection.md</automated>
  </verify>
  <done>Docs are actionable and aligned with scripts so a single operator can deploy and verify baseline protections.</done>
</task>

</tasks>

<verification>
Platform baseline is complete when compose config validates, scripts pass syntax checks, and documentation supports repeatable HTTPS deployment and protected data handling.
</verification>

<success_criteria>
- PLAT-01 is satisfied with HTTPS-capable VPS deployment artifacts and smoke verification.
- PLAT-03 baseline controls are implemented across transport, credential/session handling expectations, and at-rest backup strategy.
- Ops runbooks allow reproducible deployment and recovery without manual guesswork.
</success_criteria>

<output>
After completion, create `.planning/phases/01-platform-foundation-security-and-authentication/01-04-SUMMARY.md`
</output>
