---
phase: 02-athlete-profile-and-constraints-onboarding
plan: 3
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - src/components/profile/profile-form.tsx
  - src/app/(private)/onboarding/page.tsx
  - src/app/(private)/profile/page.tsx
autonomous: true
requirements:
  - PROF-01
  - PROF-02
  - PROF-03
  - PROF-04
must_haves:
  truths:
    - "Onboarding is a single page with sections (not a multi-step wizard)."
    - "Critical fields are required during first onboarding pass, while profile edit remains available later."
    - "Onboarding save success redirects authenticated user to dashboard."
  artifacts:
    - path: "src/components/profile/profile-form.tsx"
      provides: "Reusable sectioned profile form component for onboarding and profile edit flows"
      exports: ["ProfileForm"]
    - path: "src/app/(private)/onboarding/page.tsx"
      provides: "First-run onboarding page consuming shared profile form"
      contains: "/api/profile"
    - path: "src/app/(private)/profile/page.tsx"
      provides: "Post-onboarding editable profile page using same form contracts"
      contains: "edit"
  key_links:
    - from: "src/components/profile/profile-form.tsx"
      to: "src/lib/profile/contracts.ts"
      via: "Shared value sets and payload shape used by client form serialization"
      pattern: "goal"
    - from: "src/app/(private)/onboarding/page.tsx"
      to: "src/app/api/profile/route.ts"
      via: "Submit first-pass onboarding payload to shared profile endpoint"
      pattern: "PUT /api/profile"
---

<objective>
Build the user-facing onboarding and profile-edit screens on top of the shared profile contracts.

Purpose: Deliver the locked UX flow (single-page sections, required first pass, later editability) with one reusable form implementation.
Output: reusable profile form component, private onboarding page, and private editable profile page.
</objective>

<execution_context>
@/home/flo/.codex/get-shit-done/workflows/execute-plan.md
@/home/flo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-athlete-profile-and-constraints-onboarding/02-CONTEXT.md
@.planning/phases/02-athlete-profile-and-constraints-onboarding/02-RESEARCH.md
@src/app/(public)/login/page.tsx
@src/app/(public)/signup/page.tsx
@.planning/phases/02-athlete-profile-and-constraints-onboarding/02-01-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement reusable sectioned profile form for onboarding and edit modes</name>
  <files>src/components/profile/profile-form.tsx</files>
  <behavior>
    - Form renders sections for goal, constraints (frequency/duration/equipment), and limitations.
    - Form supports mode-aware required fields (strict on first pass, editable later).
    - Existing profile values prefill controls in edit mode and on repeated onboarding visits.
  </behavior>
  <action>Create a client `ProfileForm` component with `mode` (`onboarding` or `edit`) and explicit submit handler props. Reuse shared contract constants for options to avoid enum drift. Keep UX feedback consistent with existing login/signup pages (pending state + inline error).</action>
  <verify>
    <automated>pnpm exec tsc --noEmit</automated>
  </verify>
  <done>One form component supports both first-run and edit flows while preserving locked field structure and mode-specific required behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add private onboarding and profile pages that consume shared form and API route</name>
  <files>src/app/(private)/onboarding/page.tsx, src/app/(private)/profile/page.tsx, src/components/profile/profile-form.tsx</files>
  <behavior>
    - Onboarding page loads current profile state and requires completion of critical fields before submit.
    - Successful onboarding submit redirects to `/dashboard`.
    - Profile page allows later edits and surfaces success/error feedback without leaving page.
  </behavior>
  <action>Implement both private pages with server-side session validation and client form submission to `/api/profile`. Keep onboarding as a single page with sections and no step wizard. Preserve existing values on edit and send explicit section payloads to avoid implicit resets.</action>
  <verify>
    <automated>pnpm exec tsc --noEmit && pnpm test tests/profile/profile-route.test.ts -t "first-pass save"</automated>
  </verify>
  <done>Onboarding/profile screens are wired to the shared API contract, and onboarding success path redirects to dashboard as required.</done>
</task>

</tasks>

<verification>
Typecheck must pass and route-level tests for first-pass save behavior must remain green after UI integration.
</verification>

<success_criteria>
- PROF-01/02/03 are captured in a locked single-page onboarding UX with sectioned inputs.
- PROF-04 is supported via dedicated editable profile page that reuses the same data shape.
- Form option values stay aligned with shared contracts to prevent payload drift.
</success_criteria>

<output>
After completion, create `.planning/phases/02-athlete-profile-and-constraints-onboarding/02-03-SUMMARY.md`
</output>
