---
phase: 02-athlete-profile-and-constraints-onboarding
plan: 2
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - src/server/dal/profile.ts
  - src/app/api/profile/route.ts
  - tests/profile/profile-route.test.ts
autonomous: true
requirements:
  - PROF-01
  - PROF-02
  - PROF-03
  - PROF-04
must_haves:
  truths:
    - "Authenticated user can create/read/update only their own profile and cannot access another account's profile."
    - "Onboarding save enforces required critical fields; subsequent edits keep explicit merge semantics and do not reset omitted sections."
    - "Profile API returns structured profile data queryable by later recommendation safety logic."
  artifacts:
    - path: "src/server/dal/profile.ts"
      provides: "Account-scoped profile repository with upsert/read and explicit patch merge behavior"
      exports: ["createProfileDal", "upsertProfile", "getProfileByUserId"]
    - path: "src/app/api/profile/route.ts"
      provides: "GET/PUT profile API using contract parsing and account scope guard"
      exports: ["GET", "PUT"]
    - path: "tests/profile/profile-route.test.ts"
      provides: "Route-level tests for first-pass validation, account isolation, and explicit update semantics"
      contains: "profile route"
  key_links:
    - from: "src/app/api/profile/route.ts"
      to: "src/server/dal/profile.ts"
      via: "Route handlers delegate account-scoped persistence to DAL"
      pattern: "requireAccountScope"
    - from: "src/server/dal/profile.ts"
      to: "src/lib/profile/contracts.ts"
      via: "DAL consumes validated profile shapes only"
      pattern: "Profile"
---

<objective>
Implement server-side profile persistence and API boundaries with strict account isolation and edit semantics.

Purpose: Deliver one authoritative backend path shared by onboarding and profile-edit UI flows.
Output: profile DAL, `/api/profile` GET+PUT route, and backend tests covering safety-critical behaviors.
</objective>

<execution_context>
@/home/flo/.codex/get-shit-done/workflows/execute-plan.md
@/home/flo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-athlete-profile-and-constraints-onboarding/02-CONTEXT.md
@.planning/phases/02-athlete-profile-and-constraints-onboarding/02-RESEARCH.md
@src/server/dal/account-scope.ts
@src/lib/auth/session-gate.ts
@src/app/api/auth/login/route.ts
@.planning/phases/02-athlete-profile-and-constraints-onboarding/02-01-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build account-scoped profile DAL with explicit merge behavior</name>
  <files>src/server/dal/profile.ts, tests/profile/profile-route.test.ts</files>
  <behavior>
    - All DAL queries/writes are scoped by authenticated account context.
    - Existing limitation/equipment data remains unchanged when omitted in patch payloads.
    - Update operations require explicit payload values for changed sections; no implicit reset.
  </behavior>
  <action>Create profile DAL methods that use existing account-scope helpers and encapsulate profile upsert/read/patch behavior. Write DAL-focused tests first inside the route test suite (or helper functions) to prove unchanged fields survive partial updates and cross-account data access is rejected.</action>
  <verify>
    <automated>pnpm test tests/profile/profile-route.test.ts -t "account isolation and merge semantics"</automated>
  </verify>
  <done>Profile persistence logic is isolated, scoped, and proven to preserve existing sections unless explicitly updated.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement `/api/profile` GET and PUT handlers with contract-first parsing</name>
  <files>src/app/api/profile/route.ts, tests/profile/profile-route.test.ts</files>
  <behavior>
    - GET returns authenticated user's profile payload and completion state.
    - PUT validates payload against shared contracts and returns 400 on malformed input.
    - PUT supports first-pass onboarding save and later edits through one endpoint.
  </behavior>
  <action>Add route handlers that validate session with existing gate patterns, parse JSON payloads with profile contracts, call DAL methods, and return explicit HTTP status responses. Include tests for malformed payloads, unauthorized access, first-pass save success, and edit preservation of untouched data.</action>
  <verify>
    <automated>pnpm test tests/profile/profile-route.test.ts</automated>
  </verify>
  <done>Route handlers are fully test-covered for validation, account scope, and update semantics required by ongoing profile edits.</done>
</task>

</tasks>

<verification>
Route tests must demonstrate authenticated account isolation, first-pass required-field enforcement, and non-destructive profile edits.
</verification>

<success_criteria>
- PROF-01/02/03 backend persistence and retrieval are available behind authenticated route boundaries.
- PROF-04 backend edit path is explicit and non-destructive for omitted sections.
- Profile data remains account-isolated and queryable for later safety workflows.
</success_criteria>

<output>
After completion, create `.planning/phases/02-athlete-profile-and-constraints-onboarding/02-02-SUMMARY.md`
</output>
