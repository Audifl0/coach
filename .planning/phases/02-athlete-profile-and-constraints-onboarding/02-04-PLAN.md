---
phase: 02-athlete-profile-and-constraints-onboarding
plan: 4
type: execute
wave: 3
depends_on:
  - "02-02"
  - "02-03"
files_modified:
  - src/lib/profile/completeness.ts
  - src/app/(private)/dashboard/page.tsx
  - src/middleware.ts
  - tests/profile/onboarding-gate.test.ts
autonomous: true
requirements:
  - PROF-04
must_haves:
  truths:
    - "First authenticated access with incomplete profile is redirected to onboarding."
    - "Users with complete profile can access dashboard without onboarding loop."
    - "Profile completion check is server-side and based on canonical persisted fields."
  artifacts:
    - path: "src/lib/profile/completeness.ts"
      provides: "Single canonical predicate for profile completion used by private route gates"
      exports: ["isProfileComplete"]
    - path: "src/app/(private)/dashboard/page.tsx"
      provides: "Dashboard entry gate enforcing onboarding-first flow for incomplete profile"
      contains: "redirect('/onboarding')"
    - path: "tests/profile/onboarding-gate.test.ts"
      provides: "Automated regression tests for onboarding redirect and no-loop behavior"
      contains: "onboarding gate"
  key_links:
    - from: "src/app/(private)/dashboard/page.tsx"
      to: "src/lib/profile/completeness.ts"
      via: "Private route gate uses canonical completeness predicate"
      pattern: "isProfileComplete"
    - from: "src/lib/profile/completeness.ts"
      to: "src/app/api/profile/route.ts"
      via: "Completion checks rely on the same persisted critical fields used by API contracts"
      pattern: "goal"
---

<objective>
Enforce onboarding-first routing logic using one canonical server-side profile-completion predicate.

Purpose: Make first authenticated experience deterministic and avoid redirect loops while preserving later edit access.
Output: profile completeness helper, dashboard/private gate integration, and onboarding-gate regression tests.
</objective>

<execution_context>
@/home/flo/.codex/get-shit-done/workflows/execute-plan.md
@/home/flo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-athlete-profile-and-constraints-onboarding/02-CONTEXT.md
@.planning/phases/02-athlete-profile-and-constraints-onboarding/02-RESEARCH.md
@src/lib/auth/session-gate.ts
@src/app/(private)/dashboard/page.tsx
@src/middleware.ts
@.planning/phases/02-athlete-profile-and-constraints-onboarding/02-02-PLAN.md
@.planning/phases/02-athlete-profile-and-constraints-onboarding/02-03-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add canonical profile completeness helper and tests for completion states</name>
  <files>src/lib/profile/completeness.ts, tests/profile/onboarding-gate.test.ts</files>
  <behavior>
    - Completeness requires critical first-pass fields only.
    - Explicit "no limitation" state is considered complete, ambiguous null state is not.
    - Predicate is deterministic across onboarding and dashboard gates.
  </behavior>
  <action>Create `isProfileComplete` helper that evaluates persisted profile payload against locked first-pass requirements. Write tests first covering incomplete, complete, and explicit-none limitation states to prevent gate regressions.</action>
  <verify>
    <automated>pnpm test tests/profile/onboarding-gate.test.ts -t "profile completeness"</automated>
  </verify>
  <done>Completion logic is centralized, deterministic, and protected by automated tests for edge states.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire dashboard/private entry gating to onboarding completion flow</name>
  <files>src/app/(private)/dashboard/page.tsx, src/middleware.ts, tests/profile/onboarding-gate.test.ts</files>
  <behavior>
    - Incomplete authenticated profile redirects to `/onboarding` on dashboard entry.
    - Complete authenticated profile reaches dashboard.
    - Middleware keeps UX prefilter role and does not become authoritative completion checker.
  </behavior>
  <action>Integrate completeness checks into private dashboard server logic using validated session context and profile retrieval. Keep middleware lightweight for anonymous redirects and path preservation, but avoid logic that can cause onboarding/dashboard loops. Extend tests to cover first-login redirect and post-completion dashboard access.</action>
  <verify>
    <automated>pnpm test tests/profile/onboarding-gate.test.ts && pnpm test tests/auth/session-gate.test.ts</automated>
  </verify>
  <done>First-run onboarding trigger is enforced server-side, loop-free, and regression-locked while preserving existing auth guard behavior.</done>
</task>

</tasks>

<verification>
Onboarding-gate tests must prove incomplete users are routed to onboarding once, then can reach dashboard after profile completion without loop regressions.
</verification>

<success_criteria>
- First authenticated entry behavior matches locked decision: onboarding triggers when profile is incomplete.
- Profile completion routing remains stable and server-authoritative.
- PROF-04 lifecycle is complete: users can return later to edit constraints without breaking dashboard access.
</success_criteria>

<output>
After completion, create `.planning/phases/02-athlete-profile-and-constraints-onboarding/02-04-SUMMARY.md`
</output>
