---
phase: 02-athlete-profile-and-constraints-onboarding
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - prisma/migrations/0002_athlete_profile_constraints/migration.sql
  - src/lib/profile/contracts.ts
  - tests/profile/profile-contracts.test.ts
autonomous: true
requirements:
  - PROF-01
  - PROF-02
  - PROF-03
must_haves:
  truths:
    - "Profile contract accepts only goal values hypertrophy, strength, or recomposition."
    - "Profile contract enforces structured constraints for weekly frequency, predefined duration bucket, and equipment categories."
    - "Physical limitations are persisted in a structured shape with zone, severity, and temporality for future safety checks."
  artifacts:
    - path: "src/lib/profile/contracts.ts"
      provides: "Authoritative profile payload schemas and exported types shared by API and UI"
      exports: ["profileGoalValues", "sessionDurationValues", "profileInputSchema", "profilePatchSchema"]
    - path: "prisma/schema.prisma"
      provides: "Athlete profile and limitation persistence model linked to User"
      contains: "model AthleteProfile"
    - path: "tests/profile/profile-contracts.test.ts"
      provides: "Automated contract regression coverage for allowed values and invalid payload rejection"
      contains: "profile contracts"
  key_links:
    - from: "src/lib/profile/contracts.ts"
      to: "prisma/schema.prisma"
      via: "Enum-like constrained value sets mirrored in persisted model fields"
      pattern: "goal"
    - from: "tests/profile/profile-contracts.test.ts"
      to: "src/lib/profile/contracts.ts"
      via: "Schema parsing assertions for invalid/valid onboarding inputs"
      pattern: "safeParse"
---

<objective>
Define the profile domain contracts and data model needed for safe onboarding and later profile editing.

Purpose: Lock one shared source of truth for profile shape before API/UI implementation to avoid value drift.
Output: Prisma profile schema+migration, shared profile contracts/types, and contract tests.
</objective>

<execution_context>
@/home/flo/.codex/get-shit-done/workflows/execute-plan.md
@/home/flo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-athlete-profile-and-constraints-onboarding/02-CONTEXT.md
@.planning/phases/02-athlete-profile-and-constraints-onboarding/02-RESEARCH.md
@src/lib/auth/contracts.ts
@prisma/schema.prisma
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define profile contracts with locked value sets and first-pass requirements</name>
  <files>src/lib/profile/contracts.ts, tests/profile/profile-contracts.test.ts</files>
  <behavior>
    - Goal accepts exactly `hypertrophy`, `strength`, `recomposition`.
    - First-pass profile input requires goal, weekly session target, duration bucket, and structured equipment categories.
    - Limitation entries accept zone + severity (`none`, `mild`, `moderate`, `severe`) + temporality (`temporary`, `chronic`).
  </behavior>
  <action>Create a dedicated profile contract module exporting constants, Zod schemas, and inferred types for full input and patch input. Add tests first for invalid enum values, missing critical first-pass fields, and valid structured limitation payloads. Keep label wording out of contracts; contracts enforce only domain correctness.</action>
  <verify>
    <automated>pnpm test tests/profile/profile-contracts.test.ts</automated>
  </verify>
  <done>Profile contracts are executable and test-locked, and all locked decision value sets are represented in one shared module.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add Prisma profile persistence model aligned with contracts</name>
  <files>prisma/schema.prisma, prisma/migrations/0002_athlete_profile_constraints/migration.sql, tests/profile/profile-contracts.test.ts</files>
  <behavior>
    - A user owns at most one athlete profile.
    - Profile supports persisted goal, availability target, session duration bucket, equipment categories, and limitations structure.
    - DB shape supports future query usage for safety checks without free-form parsing.
  </behavior>
  <action>Extend Prisma schema with `AthleteProfile` (1:1 to User) and a structured limitation representation suitable for server-side querying/validation. Create an explicit migration SQL file and update tests to assert contract values map cleanly to persistence fields. Avoid introducing program-generation tables in this phase.</action>
  <verify>
    <automated>pnpm exec prisma validate && pnpm test tests/profile/profile-contracts.test.ts</automated>
  </verify>
  <done>Schema validates, migration exists, and contract tests confirm model-compatible value constraints.</done>
</task>

</tasks>

<verification>
Profile contracts and Prisma schema must agree on constrained values and required first-pass fields, with automated tests proving invalid payload rejection.
</verification>

<success_criteria>
- PROF-01 goal choices are explicitly constrained and reusable across server/client code.
- PROF-02 profile constraints are modeled in a structured, typed, and persisted form.
- PROF-03 limitation flags are modeled in a machine-queryable shape for later safety phases.
</success_criteria>

<output>
After completion, create `.planning/phases/02-athlete-profile-and-constraints-onboarding/02-01-SUMMARY.md`
</output>
