---
phase: 05.2-pipeline-web-autonome-corpus-scientifique-veille-synth-se-validation-continue
plan: 05.2-01
type: execute
wave: 0
depends_on: []
files_modified:
  - scripts/adaptive-knowledge/contracts.ts
  - scripts/adaptive-knowledge/config.ts
  - scripts/adaptive-knowledge/refresh-corpus.ts
  - tests/program/adaptive-knowledge-contracts.test.ts
  - tests/program/adaptive-knowledge-config.test.ts
autonomous: true
requirements:
  - ADAP-01
  - ADAP-02
  - SAFE-02
  - SAFE-03
must_haves:
  truths:
    - "Pipeline accepts only whitelisted guideline/review/expertise sources and rejects records outside approved domains."
    - "Default freshness policy is five years (1825 days) with bounded backfill and explicit override fields in one shared config contract."
    - "Source ingestion can stay EN, but synthesis contracts must carry FR output fields with explicit source provenance IDs."
  artifacts:
    - path: scripts/adaptive-knowledge/contracts.ts
      provides: "Canonical schemas/types for normalized source records, principle synthesis payloads, snapshot manifests, and run reports."
      exports:
        - parseNormalizedEvidenceRecord
        - parseCorpusPrinciple
        - parseCorpusSnapshotManifest
        - parseCorpusRunReport
    - path: scripts/adaptive-knowledge/config.ts
      provides: "Single runtime config parser for whitelist, freshness window, retry policy, and weekly schedule metadata."
      exports:
        - parseAdaptiveKnowledgePipelineConfig
        - DEFAULT_PIPELINE_FRESHNESS_DAYS
        - DEFAULT_PIPELINE_RETRY_COUNT
  key_links:
    - from: scripts/adaptive-knowledge/contracts.ts
      to: scripts/adaptive-knowledge/refresh-corpus.ts
      via: "refresh entrypoint consumes strict parsers for report/manifest compatibility"
      pattern: "parseCorpusRunReport|parseCorpusSnapshotManifest|parseCorpusPrinciple"
    - from: scripts/adaptive-knowledge/config.ts
      to: scripts/adaptive-knowledge/refresh-corpus.ts
      via: "shared perimeter/freshness policy is read at CLI entry boundary"
      pattern: "parseAdaptiveKnowledgePipelineConfig|freshnessWindowDays|allowedDomains"
---

<objective>
Lock Phase 05.2 contracts before implementation so every downstream stage (connectors, synthesis, publish, runtime load) shares one deterministic schema/config boundary.

Purpose: remove ambiguity from placeholder roadmap scope and encode locked 05.2 decisions into executable contracts.
Output: typed contracts + config parser + tests proving strict source perimeter and freshness/retry defaults.
</objective>

<execution_context>
@/home/flo/.codex/get-shit-done/workflows/execute-plan.md
@/home/flo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05.2-pipeline-web-autonome-corpus-scientifique-veille-synth-se-validation-continue/05.2-CONTEXT.md
@.planning/phases/05.2-pipeline-web-autonome-corpus-scientifique-veille-synth-se-validation-continue/05.2-RESEARCH.md
@scripts/adaptive-knowledge/refresh-corpus.ts
@.planning/knowledge/adaptive-coaching/index.json
@.planning/knowledge/adaptive-coaching/principles.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create canonical corpus pipeline contracts and strict parsers</name>
  <files>scripts/adaptive-knowledge/contracts.ts, tests/program/adaptive-knowledge-contracts.test.ts</files>
  <behavior>
    - Test 1: Normalized source schema accepts only `guideline|review|expertise` and requires URL/date/source domain/provenance IDs.
    - Test 2: Principle synthesis schema requires FR summary fields and non-empty provenance references to source records.
    - Test 3: Snapshot manifest/run report schemas reject unknown fields and invalid stage status transitions.
  </behavior>
  <action>Write failing tests first, then add strict Zod parsers and exported types for normalized evidence records, synthesized principles, snapshot metadata, and run report structures. Keep schema strict (`additionalProperties` disallowed) to prevent drift between pipeline stages.</action>
  <verify>
    <automated>pnpm test tests/program/adaptive-knowledge-contracts.test.ts --runInBand</automated>
  </verify>
  <done>All pipeline data exchanged between stages is parse-validated and version-safe.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add shared runtime config parser for perimeter/freshness/retry policy</name>
  <files>scripts/adaptive-knowledge/config.ts, tests/program/adaptive-knowledge-config.test.ts</files>
  <behavior>
    - Test 1: Defaults resolve to `freshnessWindowDays=1825`, bounded retry count, and weekly cadence metadata.
    - Test 2: Whitelist parser rejects non-approved domains and malformed URL hosts.
    - Test 3: Invalid freshness/retry overrides fail fast with deterministic validation errors.
  </behavior>
  <action>Implement one parser that returns immutable config for source allowlist, freshness policy, backfill window limits, and bounded retries/timeouts. Include explicit fields required by locked decisions so later tasks cannot bypass the perimeter.</action>
  <verify>
    <automated>pnpm test tests/program/adaptive-knowledge-config.test.ts --runInBand</automated>
  </verify>
  <done>Perimeter and reliability knobs are centrally enforced and test-covered.</done>
</task>

<task type="auto">
  <name>Task 3: Refactor refresh entrypoint to consume new contracts/config boundary</name>
  <files>scripts/adaptive-knowledge/refresh-corpus.ts, scripts/adaptive-knowledge/contracts.ts, scripts/adaptive-knowledge/config.ts</files>
  <action>Update `refresh-corpus.ts` to use new parsers instead of ad-hoc inline types, while preserving current `--check` compatibility behavior. Keep this script as the stable entrypoint for future cron invocation, but do not introduce manual-only execution paths in nominal mode logic.</action>
  <verify>
    <automated>pnpm test tests/program/adaptive-knowledge-contracts.test.ts tests/program/adaptive-knowledge-config.test.ts --runInBand</automated>
  </verify>
  <done>Current corpus workflow remains usable and now sits on the same strict contract layer as the new 05.2 pipeline.</done>
</task>

</tasks>

<verification>
Run contracts/config suites to confirm source perimeter, five-year freshness default, and strict schema compatibility are enforced before connector or publish work begins.
</verification>

<success_criteria>
- 05.2 pipeline contracts are explicit, strict, and shared across stages.
- Whitelist and freshness defaults are encoded and tested in one parser.
- Existing refresh/check entrypoint is migrated without behavior regression.
</success_criteria>

<output>
After completion, create `.planning/phases/05.2-pipeline-web-autonome-corpus-scientifique-veille-synth-se-validation-continue/05.2-01-SUMMARY.md`.
</output>
