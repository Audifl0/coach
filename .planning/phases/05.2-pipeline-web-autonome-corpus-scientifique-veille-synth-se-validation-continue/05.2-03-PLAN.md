---
phase: 05.2-pipeline-web-autonome-corpus-scientifique-veille-synth-se-validation-continue
plan: 05.2-03
type: execute
wave: 2
depends_on:
  - 05.2-01
  - 05.2-02
files_modified:
  - scripts/adaptive-knowledge/quality-gates.ts
  - scripts/adaptive-knowledge/publish.ts
  - scripts/adaptive-knowledge/pipeline-run.ts
  - src/lib/adaptive-coaching/evidence.ts
  - src/lib/adaptive-coaching/evidence-corpus.ts
  - tests/program/adaptive-knowledge-publish.test.ts
  - tests/program/adaptive-evidence-corpus-loader.test.ts
  - infra/scripts/install-adaptive-corpus-cron.sh
autonomous: true
requirements:
  - ADAP-01
  - ADAP-02
  - SAFE-02
  - SAFE-03
must_haves:
  truths:
    - "Publication is blocked when composite quality score is below threshold or critical contradiction is detected."
    - "Active corpus is switched only via atomic pointer swap to a fully validated snapshot, with one-step rollback to previous snapshot metadata."
    - "Runtime evidence retrieval reads only the currently active validated snapshot and never candidate/partial files."
    - "Nominal execution mode is cron-triggered weekly; operations instructions include install/verify/rollback commands."
  artifacts:
    - path: scripts/adaptive-knowledge/quality-gates.ts
      provides: "Composite quality and contradiction evaluation deciding publishable vs blocked candidate state."
      exports:
        - evaluateCorpusQualityGate
    - path: scripts/adaptive-knowledge/publish.ts
      provides: "Atomic active-pointer promotion and rollback pointer management for validated snapshots."
      exports:
        - promoteCandidateSnapshot
        - rollbackCorpusSnapshot
    - path: src/lib/adaptive-coaching/evidence-corpus.ts
      provides: "Runtime loader resolving active snapshot corpus payloads with safe fallback to last valid pointers."
      exports:
        - loadActiveAdaptiveEvidenceCorpus
  key_links:
    - from: scripts/adaptive-knowledge/quality-gates.ts
      to: scripts/adaptive-knowledge/publish.ts
      via: "only publishable candidates are eligible for atomic promotion"
      pattern: "publishable|criticalContradictions"
    - from: scripts/adaptive-knowledge/publish.ts
      to: src/lib/adaptive-coaching/evidence-corpus.ts
      via: "active/rollback pointer JSON contract"
      pattern: "active.json|rollback.json"
    - from: src/lib/adaptive-coaching/evidence-corpus.ts
      to: src/lib/adaptive-coaching/evidence.ts
      via: "retrieval uses active snapshot corpus as primary source"
      pattern: "loadActiveAdaptiveEvidenceCorpus|retrieveAdaptiveEvidence"
---

<objective>
Finalize Phase 05.2 with strict validation gate, atomic publish/rollback, runtime activation safety, and cron-only operationalization.

Purpose: ensure autonomous weekly updates can never expose low-quality or partial corpus state to adaptive coaching runtime.
Output: quality gate module, publish/rollback module, runtime snapshot loader integration, cron install script, and ops runbook.
</objective>

<execution_context>
@/home/flo/.codex/get-shit-done/workflows/execute-plan.md
@/home/flo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05.2-pipeline-web-autonome-corpus-scientifique-veille-synth-se-validation-continue/05.2-02-PLAN.md
@.planning/phases/05.2-pipeline-web-autonome-corpus-scientifique-veille-synth-se-validation-continue/05.2-CONTEXT.md
@.planning/phases/05.2-pipeline-web-autonome-corpus-scientifique-veille-synth-se-validation-continue/05.2-RESEARCH.md
@src/lib/adaptive-coaching/evidence.ts
@infra/scripts/backup.sh
@docs/operations/data-protection.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement composite quality gate and contradiction blocking before publish</name>
  <files>scripts/adaptive-knowledge/quality-gates.ts, scripts/adaptive-knowledge/pipeline-run.ts, tests/program/adaptive-knowledge-publish.test.ts</files>
  <behavior>
    - Test 1: Candidate with score below configured threshold is marked non-publishable and active pointer remains unchanged.
    - Test 2: Candidate with critical contradiction is blocked even when recency/source-type score is high.
    - Test 3: Gate output emits deterministic reasons (`score_below_threshold`, `critical_contradiction`) for run-report observability.
  </behavior>
  <action>Create a quality evaluator that combines source class weight, recency score, required-field completeness, and contradiction severity into one publish decision. Integrate it into stage runner so publish is impossible without explicit pass result.</action>
  <verify>
    <automated>pnpm test tests/program/adaptive-knowledge-publish.test.ts --runInBand</automated>
  </verify>
  <done>Publish gate is deterministic and blocks low-quality or contradictory corpus candidates.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add atomic publish and rollback pointers with no in-place active file mutation</name>
  <files>scripts/adaptive-knowledge/publish.ts, scripts/adaptive-knowledge/pipeline-run.ts, tests/program/adaptive-knowledge-publish.test.ts</files>
  <behavior>
    - Test 1: Successful publish writes candidate snapshot artifacts then atomically swaps `active.json` pointer.
    - Test 2: Previous active pointer is persisted into `rollback.json` during promotion.
    - Test 3: Rollback command restores previous active pointer atomically and writes corresponding run-report event.
  </behavior>
  <action>Implement pointer-based publish API with same-filesystem atomic rename semantics (`active.tmp.json -> active.json`) and explicit rollback function. Ensure the stage runner never edits `.planning/knowledge/adaptive-coaching/index.json` or `principles.json` in place during publish.</action>
  <verify>
    <automated>pnpm test tests/program/adaptive-knowledge-publish.test.ts --runInBand</automated>
  </verify>
  <done>Snapshot activation/rollback is safe, atomic, and auditable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire runtime corpus loader to active snapshot and add cron install helper</name>
  <files>src/lib/adaptive-coaching/evidence-corpus.ts, src/lib/adaptive-coaching/evidence.ts, tests/program/adaptive-evidence-corpus-loader.test.ts, infra/scripts/install-adaptive-corpus-cron.sh</files>
  <behavior>
    - Test 1: Evidence retrieval loads entries/principles from active snapshot pointer when available.
    - Test 2: Missing/invalid active pointer fails closed to last valid snapshot and never reads candidate directories.
    - Test 3: Cron install script registers one weekly job invoking `corepack pnpm tsx scripts/adaptive-knowledge/refresh-corpus.ts`.
  </behavior>
  <action>Add a dedicated runtime loader used by `retrieveAdaptiveEvidence` to source corpus data from active snapshot metadata. Provide an infra helper script enforcing weekly cron-only nominal triggering.</action>
  <verify>
    <automated>pnpm test tests/program/adaptive-evidence-corpus-loader.test.ts tests/program/adaptive-knowledge-publish.test.ts --runInBand</automated>
  </verify>
  <done>Adaptive runtime consumes only validated active snapshots, and VPS operations have a concrete weekly-cron + rollback procedure.</done>
</task>

</tasks>

<verification>
Run publish and loader test suites to prove gate-blocking, atomic pointer semantics, rollback safety, and active-only runtime corpus reads.
</verification>

<success_criteria>
- Non-publishable candidates never become active.
- Active/rollback snapshot pointers are updated atomically and test-covered.
- Runtime evidence retrieval is decoupled from candidate writes and relies on active validated snapshots.
- Cron-only weekly operations are scriptable.
</success_criteria>

<output>
After completion, create `.planning/phases/05.2-pipeline-web-autonome-corpus-scientifique-veille-synth-se-validation-continue/05.2-03-SUMMARY.md`.
</output>
