---
phase: 06-trends-and-operational-reliability
plan: 06-03
type: execute
wave: 1
depends_on: []
files_modified:
  - infra/scripts/backup.sh
  - infra/scripts/restore.sh
  - infra/scripts/run-restore-drill.sh
  - infra/systemd/coach-restore-drill.service
  - infra/systemd/coach-restore-drill.timer
  - docs/operations/restore-drill-runbook.md
  - tests/ops/restore-drill.test.ts
autonomous: true
requirements:
  - PLAT-02
user_setup:
  - "Create a dedicated restore target database on VPS (for example `coach_restore_drill`) and provide it through env file used by drill script."
  - "Install/enable `infra/systemd/coach-restore-drill.service` and `.timer` on VPS host (`systemctl daemon-reload && systemctl enable --now coach-restore-drill.timer`)."
must_haves:
  truths:
    - "Backup restore drill can run monthly on the same VPS using a dedicated test database without mutating production primary data."
    - "Restore execution fails fast on SQL errors and cannot silently succeed with partial state."
    - "Each drill run leaves timestamped evidence with restore outcome plus smoke verification of login and dashboard reachability."
  artifacts:
    - path: infra/scripts/run-restore-drill.sh
      provides: "Single-command monthly drill orchestration over backup + restore + smoke checks + evidence log write."
    - path: infra/systemd/coach-restore-drill.timer
      provides: "Monthly persistent scheduler for restore drill cadence."
    - path: docs/operations/restore-drill-runbook.md
      provides: "Timestamped step-by-step incident-ready restore runbook with verification and rollback steps."
  key_links:
    - from: infra/scripts/run-restore-drill.sh
      to: infra/scripts/restore.sh
      via: "drill wrapper restores encrypted backup into dedicated test database"
      pattern: "restore.sh"
    - from: infra/scripts/restore.sh
      to: docs/operations/restore-drill-runbook.md
      via: "runbook execution steps and expected outputs map to strict restore command semantics"
      pattern: "ON_ERROR_STOP|single-transaction|restore target db"
    - from: infra/systemd/coach-restore-drill.timer
      to: infra/scripts/run-restore-drill.sh
      via: "monthly timer triggers oneshot drill service with persistent catch-up"
      pattern: "OnCalendar=monthly|Persistent=true|ExecStart="
---

<objective>
Finalize PLAT-02 by making backup and restore validation operational, repeatable, and auditable on VPS.

Purpose: prove recoverability, not only backup file creation, via strict restore semantics and monthly restore drills.
Output: hardened restore scripts, automated drill wrapper, systemd scheduler units, and an incident-grade runbook with evidence logging.
</objective>

<execution_context>
@/home/flo/.codex/get-shit-done/workflows/execute-plan.md
@/home/flo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/06-trends-and-operational-reliability/06-CONTEXT.md
@.planning/phases/06-trends-and-operational-reliability/06-RESEARCH.md
@infra/scripts/backup.sh
@infra/scripts/restore.sh
@docs/operations/data-protection.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Harden backup/restore scripts for strict restore safety and dedicated drill DB targeting</name>
  <files>infra/scripts/backup.sh, infra/scripts/restore.sh, tests/ops/restore-drill.test.ts</files>
  <behavior>
    - Test 1: Restore command includes strict failure semantics (`ON_ERROR_STOP`) and transaction safety for SQL replay.
    - Test 2: Restore script enforces explicit target database variable for drill mode and blocks accidental production target.
    - Test 3: Backup/restore scripts preserve encrypted-flow contract and fail clearly when required env variables are missing.
  </behavior>
  <action>Refactor scripts to support dedicated restore drill target DB while retaining current encrypted backup format. Update restore command flags to fail fast on SQL errors and prevent partial-success ambiguity.</action>
  <verify>
    <automated>pnpm test tests/ops/restore-drill.test.ts && bash -n infra/scripts/backup.sh infra/scripts/restore.sh</automated>
  </verify>
  <done>Backup and restore primitives are safe enough for repeatable PLAT-02 drills.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add drill orchestration script and monthly systemd units</name>
  <files>infra/scripts/run-restore-drill.sh, infra/systemd/coach-restore-drill.service, infra/systemd/coach-restore-drill.timer, tests/ops/restore-drill.test.ts</files>
  <behavior>
    - Test 1: Drill script runs backup selection + restore + smoke checks in deterministic order and exits non-zero on any failed stage.
    - Test 2: Timer unit is monthly with `Persistent=true` and points to oneshot service.
    - Test 3: Service unit executes drill wrapper script and writes predictable evidence output location.
  </behavior>
  <action>Create a wrapper script orchestrating restore drill flow end-to-end with timestamped evidence output. Add systemd service/timer templates under `infra/systemd/` for monthly cadence and missed-run catch-up behavior.</action>
  <verify>
    <automated>pnpm test tests/ops/restore-drill.test.ts && bash -n infra/scripts/run-restore-drill.sh</automated>
  </verify>
  <done>VPS operators can schedule monthly restore drills with a reproducible automation path.</done>
</task>

<task type="auto">
  <name>Task 3: Publish incident-ready restore runbook and evidence checklist</name>
  <files>docs/operations/restore-drill-runbook.md, docs/operations/data-protection.md, tests/ops/restore-drill.test.ts</files>
  <action>Write a timestamped checklist runbook for restore drills: prerequisites, command sequence, smoke verification for login/dashboard, expected outputs, and rollback/failure handling. Cross-link from existing data-protection docs and align naming conventions with script outputs.</action>
  <verify>
    <automated>pnpm test tests/ops/restore-drill.test.ts</automated>
  </verify>
  <done>PLAT-02 operational procedure is documented, executable, and evidence-driven for incident response.</done>
</task>

</tasks>

<verification>
Run ops test suite and shell syntax checks to verify strict restore semantics, scheduler templates, and runbook/script alignment.
</verification>

<success_criteria>
- Monthly restore drill can be scheduled and executed against dedicated test DB on same VPS.
- Restore flow is fail-fast and auditable, with no silent partial restore behavior.
- Runbook provides step-by-step incident execution plus smoke proof for login and dashboard access.
</success_criteria>

<output>
After completion, create `.planning/phases/06-trends-and-operational-reliability/06-03-SUMMARY.md`.
</output>
