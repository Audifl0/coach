---
phase: 06-trends-and-operational-reliability
plan: 06-02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - package.json
  - pnpm-lock.yaml
  - src/app/(private)/dashboard/page.tsx
  - src/app/(private)/dashboard/_components/trends-summary-card.tsx
  - src/app/(private)/dashboard/_components/trends-drilldown.tsx
  - tests/program/dashboard-trends-surface.test.ts
autonomous: true
requirements:
  - DASH-02
must_haves:
  truths:
    - "Dashboard shows a compact trends block with exactly three mini line charts and KPI values for volume, intensity, and adherence."
    - "Default horizon on first render is 30 days, and user can switch inline using 7/30/90 toggles."
    - "Trend interpretation remains visual-only (line + value) with no arrows, deltas, or extra interpretation badges."
    - "Exercise-level reps/load evolution is available in a drilldown view, not in the main trends summary block."
  artifacts:
    - path: src/app/(private)/dashboard/_components/trends-summary-card.tsx
      provides: "Client component rendering compact KPI + mini-line trend cards and horizon toggles."
      exports:
        - TrendsSummaryCard
    - path: src/app/(private)/dashboard/_components/trends-drilldown.tsx
      provides: "Client drilldown component rendering per-exercise reps and load evolution charts."
      exports:
        - TrendsDrilldown
    - path: src/app/(private)/dashboard/page.tsx
      provides: "Server composition placing trends below adaptive forecast and above history."
  key_links:
    - from: src/app/(private)/dashboard/page.tsx
      to: /api/program/trends
      via: "server-side fetch with no-store for initial 30d trend payload"
      pattern: "fetch.*api/program/trends\\?period=30d"
    - from: src/app/(private)/dashboard/_components/trends-summary-card.tsx
      to: /api/program/trends
      via: "inline 7/30/90 toggle reloads summary dataset"
      pattern: "period=7d|period=30d|period=90d"
    - from: src/app/(private)/dashboard/_components/trends-drilldown.tsx
      to: /api/program/trends/[exerciseKey]
      via: "exercise drilldown requests reps/load history for selected exercise"
      pattern: "/api/program/trends/"
---

<objective>
Render the full DASH-02 dashboard experience using the Phase 06 trend API foundation.

Purpose: deliver compact, action-first trend visibility on dashboard while keeping detailed per-exercise evolution in a dedicated drilldown view.
Output: dashboard trends summary UI, drilldown UI, and placement/wiring updates with focused surface tests.
</objective>

<execution_context>
@/home/flo/.codex/get-shit-done/workflows/execute-plan.md
@/home/flo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/06-trends-and-operational-reliability/06-CONTEXT.md
@.planning/phases/06-trends-and-operational-reliability/06-RESEARCH.md
@.planning/phases/06-trends-and-operational-reliability/06-01-PLAN.md
@src/app/(private)/dashboard/page.tsx
@src/app/(private)/dashboard/_components/session-history-card.tsx
@src/app/(private)/dashboard/components/adaptive-forecast-card.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add chart dependency and implement compact trends summary card component</name>
  <files>package.json, pnpm-lock.yaml, src/app/(private)/dashboard/_components/trends-summary-card.tsx, tests/program/dashboard-trends-surface.test.ts</files>
  <behavior>
    - Test 1: Summary component renders exactly three metric cards (volume/intensity/adherence) with mini line charts and KPI value labels.
    - Test 2: Default client state uses `30d` period and exposes only `7d|30d|90d` inline toggles.
    - Test 3: View helpers do not emit delta arrows or interpretation badges in output state.
  </behavior>
  <action>Install `recharts` and create a focused client component for compact line charts plus KPI values. Keep card UI minimal and deterministic, with visual-only interpretation and no secondary computed delta language.</action>
  <verify>
    <automated>pnpm test tests/program/dashboard-trends-surface.test.ts</automated>
  </verify>
  <done>Dashboard has a reusable trends summary component that matches locked visual constraints.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build per-exercise trends drilldown for reps/load evolution</name>
  <files>src/app/(private)/dashboard/_components/trends-drilldown.tsx, src/app/(private)/dashboard/_components/trends-summary-card.tsx, tests/program/dashboard-trends-surface.test.ts</files>
  <behavior>
    - Test 1: Drilldown renders separate line series for reps and load evolution for selected exercise.
    - Test 2: Main dashboard summary remains compact and does not embed per-exercise charts inline.
    - Test 3: Drilldown fetch requests are tied to selected horizon and selected exercise key.
  </behavior>
  <action>Create a drilldown component opened from the summary block to show exercise-level load/reps lines. Keep drilldown behavior deterministic and aligned to the same 7/30/90 horizon state.</action>
  <verify>
    <automated>pnpm test tests/program/dashboard-trends-surface.test.ts</automated>
  </verify>
  <done>Users can inspect reps/load evolution per exercise without bloating the dashboard main surface.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire dashboard page placement and data loading order for trends section</name>
  <files>src/app/(private)/dashboard/page.tsx, src/app/(private)/dashboard/_components/trends-summary-card.tsx, tests/program/dashboard-trends-surface.test.ts</files>
  <behavior>
    - Test 1: Dashboard composition places trends block below adaptive forecast and above session history.
    - Test 2: Initial server load fetches trends with `period=30d` and `cache: no-store`.
    - Test 3: If trends API fails, dashboard keeps other cards functional and trends section degrades gracefully.
  </behavior>
  <action>Update dashboard server composition to load initial 30d trends payload and pass it to the client summary component. Maintain current auth/onboarding routing behavior and existing session/history surfaces unchanged.</action>
  <verify>
    <automated>pnpm test tests/program/dashboard-trends-surface.test.ts tests/program/dashboard-today-surface.test.ts</automated>
  </verify>
  <done>Dashboard trend UX is fully wired with correct section order and resilient loading behavior.</done>
</task>

</tasks>

<verification>
Run dashboard trends and dashboard surface tests to confirm correct placement, horizon switching, and drilldown behavior.
</verification>

<success_criteria>
- Dashboard renders compact 3-chart trend block below adaptive forecast and above history.
- First-load horizon is 30d, with working 7/30/90 toggles.
- Per-exercise reps/load evolution is available through drilldown, not the summary surface.
</success_criteria>

<output>
After completion, create `.planning/phases/06-trends-and-operational-reliability/06-02-SUMMARY.md`.
</output>
