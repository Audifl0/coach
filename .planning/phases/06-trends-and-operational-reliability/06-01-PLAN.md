---
phase: 06-trends-and-operational-reliability
plan: 06-01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/program/contracts.ts
  - src/lib/program/trends.ts
  - src/server/dal/program.ts
  - src/app/api/program/trends/route.ts
  - src/app/api/program/trends/[exerciseKey]/route.ts
  - tests/program/program-trends-contracts.test.ts
  - tests/program/program-dal-trends.test.ts
  - tests/program/program-trends-route.test.ts
autonomous: true
requirements:
  - DASH-02
must_haves:
  truths:
    - "Authenticated user can request 7/30/90 trend summaries and receive volume, intensity, and adherence points over the selected horizon."
    - "Trend response always uses deterministic metric definitions: volume = tonnage, intensity = average key-exercise load, adherence = completed/planned sessions."
    - "Exercise drilldown exposes line-ready reps/load points for a selected exercise key without leaking cross-account data."
  artifacts:
    - path: src/lib/program/trends.ts
      provides: "Shared trend query contracts, period helpers, and projection utilities."
      exports:
        - parseProgramTrendQueryInput
        - parseProgramTrendsSummaryResponse
        - parseProgramTrendsExerciseResponse
    - path: src/server/dal/program.ts
      provides: "Account-scoped trend aggregation and exercise drilldown data access."
      exports:
        - getTrendSummary
        - getExerciseTrendSeries
    - path: src/app/api/program/trends/route.ts
      provides: "Authenticated summary endpoint returning compact chart payload."
      exports:
        - GET
  key_links:
    - from: src/app/api/program/trends/route.ts
      to: src/server/dal/program.ts
      via: "route resolves session and delegates to account-scoped DAL trend query"
      pattern: "getTrendSummary"
    - from: src/app/api/program/trends/[exerciseKey]/route.ts
      to: src/server/dal/program.ts
      via: "drilldown endpoint delegates to exercise series query with ownership scope"
      pattern: "getExerciseTrendSeries"
    - from: src/lib/program/trends.ts
      to: src/app/api/program/trends/route.ts
      via: "route payload is parse-validated before response"
      pattern: "parseProgramTrendsSummaryResponse|parseProgramTrendQueryInput"
---

<objective>
Build the Phase 06 trend-data foundation and authenticated API surface before dashboard rendering.

Purpose: make DASH-02 data deterministic, account-scoped, and reusable by both compact dashboard cards and per-exercise drilldown.
Output: strict trend contracts, DAL aggregations, and `/api/program/trends` + `/api/program/trends/[exerciseKey]` endpoints with tests.
</objective>

<execution_context>
@/home/flo/.codex/get-shit-done/workflows/execute-plan.md
@/home/flo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-trends-and-operational-reliability/06-CONTEXT.md
@.planning/phases/06-trends-and-operational-reliability/06-RESEARCH.md
@src/lib/program/contracts.ts
@src/server/dal/program.ts
@src/app/api/program/history/route.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add strict trends contracts and period parsing helpers</name>
  <files>src/lib/program/trends.ts, src/lib/program/contracts.ts, tests/program/program-trends-contracts.test.ts</files>
  <behavior>
    - Test 1: Query parser accepts only `7d|30d|90d` and rejects `custom` for dashboard trends horizon toggles.
    - Test 2: Summary response schema requires `volume|intensity|adherence` metrics with line-ready points and KPI values.
    - Test 3: Drilldown response schema requires per-date reps/load points and deterministic exercise identity fields.
  </behavior>
  <action>Write failing contract tests first, then implement shared trend contracts/parsers and export helpers from `src/lib/program/contracts.ts` for route consumption. Keep schemas strict and aligned to locked metric definitions (tonnage, key-exercise average load, completion ratio).</action>
  <verify>
    <automated>pnpm test tests/program/program-trends-contracts.test.ts</automated>
  </verify>
  <done>Trend query and payload contracts are centralized, strict, and reusable by both API routes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement account-scoped trend aggregation and exercise drilldown in Program DAL</name>
  <files>src/server/dal/program.ts, tests/program/program-dal-trends.test.ts</files>
  <behavior>
    - Test 1: Summary aggregation returns deterministic 7/30/90 buckets and zero-fills missing periods for stable line charts.
    - Test 2: Intensity metric uses deterministic key-exercise subset logic and ignores sessions outside the selected horizon.
    - Test 3: Drilldown returns ordered reps/load evolution for one exercise key and masks cross-account records.
  </behavior>
  <action>Extend `createProgramDal` with trend summary and per-exercise drilldown methods, using account-scoped filters and deterministic ordering. Keep aggregation logic server-side, not UI-side, to avoid timezone/formula drift.</action>
  <verify>
    <automated>pnpm test tests/program/program-dal-trends.test.ts</automated>
  </verify>
  <done>Program DAL can serve all trend metrics and drilldown series with ownership guarantees.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add authenticated trends API routes for summary and exercise drilldown</name>
  <files>src/app/api/program/trends/route.ts, src/app/api/program/trends/[exerciseKey]/route.ts, tests/program/program-trends-route.test.ts</files>
  <behavior>
    - Test 1: Unauthenticated requests return 401 on both routes.
    - Test 2: Valid `period` query returns parse-validated summary payload for volume/intensity/adherence.
    - Test 3: Invalid query or missing exercise drilldown data returns deterministic 400/404 behavior without leaking ownership details.
  </behavior>
  <action>Implement GET handlers using existing session-gate patterns from history/today routes. Parse query inputs with new trend parsers, call DAL methods, and parse response payloads before returning JSON.</action>
  <verify>
    <automated>pnpm test tests/program/program-trends-route.test.ts tests/program/program-dal-trends.test.ts tests/program/program-trends-contracts.test.ts</automated>
  </verify>
  <done>Dashboard can consume authenticated summary and drilldown trend APIs with stable contracts.</done>
</task>

</tasks>

<verification>
Run trend contract, DAL, and route tests to confirm strict schemas, deterministic metric computation, and account isolation.
</verification>

<success_criteria>
- `/api/program/trends?period=30d` returns strict volume/intensity/adherence payload for authenticated users.
- `/api/program/trends/{exerciseKey}?period=30d` returns ordered reps/load series for drilldown charts.
- Query and payload contracts reject malformed input/output before route response.
</success_criteria>

<output>
After completion, create `.planning/phases/06-trends-and-operational-reliability/06-01-SUMMARY.md`.
</output>
