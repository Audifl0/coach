---
phase: 06-trends-and-operational-reliability
plan: 06-04
type: execute
wave: 3
depends_on:
  - 06-02
files_modified:
  - src/app/(private)/dashboard/_components/trends-summary-card.tsx
  - tests/program/dashboard-trends-surface.test.ts
autonomous: true
gap_closure: true
requirements:
  - DASH-02
must_haves:
  truths:
    - "Switching 30d -> 7d|90d -> 30d always displays true 30d data, never stale prior-period data."
    - "When a user taps any 7/30/90 toggle, the charts and KPI values always update to that exact selected horizon."
    - "Users can trust horizon toggles because visible trend values never mismatch the currently highlighted period."
  artifacts:
    - path: src/app/(private)/dashboard/_components/trends-summary-card.tsx
      provides: "Deterministic period-state data loading that keeps selected period and rendered dataset aligned."
      exports:
        - TrendsSummaryCard
    - path: tests/program/dashboard-trends-surface.test.ts
      provides: "Regression coverage for period switch sequence including return-to-30d behavior."
  key_links:
    - from: src/app/(private)/dashboard/_components/trends-summary-card.tsx
      to: /api/program/trends
      via: "period toggle handler/effect enforces selected period data parity"
      pattern: "period=7d|period=30d|period=90d"
    - from: tests/program/dashboard-trends-surface.test.ts
      to: src/app/(private)/dashboard/_components/trends-summary-card.tsx
      via: "UI sequence test validates stale-state regression is fixed"
      pattern: "30d.*7d.*30d|30d.*90d.*30d"
---

<objective>
Close the remaining DASH-02 verification gap where the dashboard horizon toggle can show stale data when switching back to 30d.

Purpose: ensure trend toggles are trustworthy and deterministic for decision support.
Output: corrected trends summary period-state logic plus regression tests.
</objective>

<execution_context>
@/home/flo/.codex/get-shit-done/workflows/execute-plan.md
@/home/flo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-trends-and-operational-reliability/06-VERIFICATION.md
@.planning/phases/06-trends-and-operational-reliability/06-02-SUMMARY.md
@src/app/(private)/dashboard/_components/trends-summary-card.tsx
@tests/program/dashboard-trends-surface.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix summary-card period state so selected horizon always matches rendered dataset</name>
  <files>src/app/(private)/dashboard/_components/trends-summary-card.tsx, tests/program/dashboard-trends-surface.test.ts</files>
  <behavior>
    - Test 1: Sequence `30d -> 7d -> 30d` renders 30d dataset after returning to 30d.
    - Test 2: Sequence `30d -> 90d -> 30d` renders 30d dataset after returning to 30d.
    - Test 3: In-flight request handling cannot leave state with `period=30d` while displaying non-30d series.
  </behavior>
  <action>Update `TrendsSummaryCard` period transition logic to guarantee data-period parity. When switching to the initial period, either restore `initialData` explicitly or fetch based on actual loaded data-period mismatch so stale non-30d data cannot persist under a 30d selection label.</action>
  <verify>
    <automated>pnpm test tests/program/dashboard-trends-surface.test.ts</automated>
  </verify>
  <done>Returning to 30d after other horizons always shows true 30d metrics and line series.</done>
</task>

<task type="auto">
  <name>Task 2: Run focused verification sweep for dashboard trend toggle behavior</name>
  <files>tests/program/dashboard-trends-surface.test.ts</files>
  <action>Run targeted verification for the fixed dashboard trend surface to confirm the return-to-30d stale-data regression is closed and no toggle behavior regresses.</action>
  <verify>
    <automated>pnpm test tests/program/dashboard-trends-surface.test.ts</automated>
  </verify>
  <done>Gap closure is validated and trend surface toggle behavior remains stable across horizon switches.</done>
</task>

</tasks>

<verification>
Confirm the verified `30d -> 7d|90d -> 30d` stale-state regression is closed and horizon toggles always render period-matching data.
</verification>

<success_criteria>
- Switching back to 30d never shows stale 7d/90d data.
- Dashboard trends surface tests include explicit regression coverage for return-to-30d sequences.
- Focused verification run passes for dashboard trend toggle behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/06-trends-and-operational-reliability/06-04-SUMMARY.md`.
</output>
