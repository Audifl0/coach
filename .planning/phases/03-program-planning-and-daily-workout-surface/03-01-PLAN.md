---
phase: 03-program-planning-and-daily-workout-surface
plan: 03-01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - prisma/migrations/*_program_planning_init/migration.sql
  - src/lib/program/contracts.ts
  - src/lib/program/types.ts
  - src/lib/program/catalog.ts
  - src/server/dal/program.ts
autonomous: true
requirements:
  - PROG-01
  - PROG-02
must_haves:
  truths:
    - "An authenticated user can receive a 7-day training plan with clear per-exercise prescriptions (sets, reps, load, rest range)."
    - "Invalid program payloads are rejected with deterministic validation errors before any plan data is persisted or returned."
    - "A user can access and modify only their own program data; another account's program is never visible or mutable."
  artifacts:
    - path: prisma/schema.prisma
      provides: "ProgramPlan, PlannedSession, and PlannedExercise models linked to User."
      contains: "model ProgramPlan"
    - path: src/lib/program/contracts.ts
      provides: "Zod schemas and parse helpers for program DTOs/commands."
      exports:
        - parseProgramGenerateInput
        - parseProgramTodayResponse
    - path: src/server/dal/program.ts
      provides: "Account-scoped DAL for creating and reading planned program data."
      exports:
        - createProgramDal
  key_links:
    - from: src/server/dal/program.ts
      to: prisma/schema.prisma
      via: "Prisma client model usage"
      pattern: "prisma\\.(programPlan|plannedSession|plannedExercise)"
    - from: src/lib/program/contracts.ts
      to: src/app/api/program/*
      via: "route input/output parsing"
      pattern: "parseProgram"
---

<objective>
Create the program-domain foundation (schema, contracts, catalog, DAL) required by all Phase 3 execution plans.

Purpose: Establish strict, reusable contracts and persistence boundaries before feature routes/UI are implemented.
Output: Migratable Prisma models, typed/Zod program contracts, deterministic catalog scaffolding, and account-scoped DAL.
</objective>

<execution_context>
@/home/flo/.codex/get-shit-done/workflows/execute-plan.md
@/home/flo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-program-planning-and-daily-workout-surface/03-CONTEXT.md
@.planning/phases/03-program-planning-and-daily-workout-surface/03-RESEARCH.md
@src/lib/profile/contracts.ts
@src/server/dal/account-scope.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add program planning persistence models and migration</name>
  <files>prisma/schema.prisma, prisma/migrations/*_program_planning_init/migration.sql</files>
  <action>Add ProgramPlan, PlannedSession, and PlannedExercise models with user ownership, scheduled-date indexing, and prescription fields (`sets`, fixed `targetReps`, `targetLoad`, `restMinSec`, `restMaxSec`). Keep continuity metadata (`isSubstituted`, `originalExerciseKey`) on planned exercises. Create migration SQL matching schema changes.</action>
  <verify>
    <automated>pnpm prisma migrate dev --name program_planning_init --create-only</automated>
  </verify>
  <done>Schema and migration define all core Phase 3 entities with correct relations and constraints for account-scoped access.</done>
</task>

<task type="auto">
  <name>Task 2: Define program contracts, internal types, and deterministic catalog primitives</name>
  <files>src/lib/program/contracts.ts, src/lib/program/types.ts, src/lib/program/catalog.ts</files>
  <action>Create Zod schemas and parse helpers for program-generation commands and today/session responses. Define explicit enums/types for movement patterns, equipment tags, and session states. Add an in-code exercise catalog + strict substitution compatibility metadata (no fuzzy matching, no client-owned safety flags).</action>
  <verify>
    <automated>pnpm test tests/program/contracts.test.ts --runInBand</automated>
  </verify>
  <done>Program contracts and catalog types are exported and enforce fixed-rep/target-load/rest-range response shapes.</done>
</task>

<task type="auto">
  <name>Task 3: Implement account-scoped Program DAL entry points</name>
  <files>src/server/dal/program.ts</files>
  <action>Implement DAL methods to create/replace active plans, fetch today/next session candidates, resolve planned exercise ownership, and apply single-row substitution updates. Enforce ownership through existing account-scope patterns and keep all methods session-user derived (never caller-provided userId trust).</action>
  <verify>
    <automated>pnpm test tests/program/program-dal.test.ts --runInBand</automated>
  </verify>
  <done>Program DAL supports downstream generation/today/substitution flows with explicit user boundary checks.</done>
</task>

</tasks>

<verification>
Run migration generation and focused program-domain tests to confirm models, contracts, and DAL align and compile together.
</verification>

<success_criteria>
- Program schema supports a 7-day plan with per-exercise prescription fields required by PROG-01/PROG-02.
- Contracts expose parse helpers consumed by routes without ad-hoc validation.
- DAL methods required by generation/today/substitution plans exist and enforce account ownership.
</success_criteria>

<output>
After completion, create `.planning/phases/03-program-planning-and-daily-workout-surface/03-01-SUMMARY.md`.
</output>
