---
phase: 03-program-planning-and-daily-workout-surface
plan: 03-03
type: execute
wave: 3
depends_on:
  - 03-02
files_modified:
  - src/lib/program/select-today-session.ts
  - src/app/api/program/today/route.ts
  - src/app/api/program/sessions/[sessionId]/route.ts
  - src/app/(private)/dashboard/page.tsx
  - src/app/(private)/dashboard/_components/today-workout-card.tsx
  - tests/program/dashboard-today-surface.test.ts
autonomous: true
requirements:
  - DASH-01
  - PROG-02
must_haves:
  truths:
    - "Dashboard shows a compact Today Workout card as the primary top block."
    - "If no workout exists today, dashboard shows the next planned session instead of an empty state."
    - "User can open exercise-level detail from the compact card and see prescription fields."
    - "Primary dashboard action is `Commencer seance` from the daily workout surface."
  artifacts:
    - path: src/app/api/program/today/route.ts
      provides: "Today-or-next workout API projection."
      exports:
        - GET
    - path: src/app/(private)/dashboard/_components/today-workout-card.tsx
      provides: "Compact dashboard UI with detail expansion and CTA."
    - path: src/app/(private)/dashboard/page.tsx
      provides: "Dashboard composition placing today workout as top-priority block."
  key_links:
    - from: src/app/(private)/dashboard/page.tsx
      to: /api/program/today
      via: "dashboard data fetch"
      pattern: "/api/program/today"
    - from: src/app/(private)/dashboard/_components/today-workout-card.tsx
      to: /api/program/sessions/:sessionId
      via: "detail expansion fetch"
      pattern: "/api/program/sessions/"
---

<objective>
Ship the daily workout dashboard surface so users immediately see what to do today, with fallback to next planned session.

Purpose: Fulfill DASH-01 with an action-first single-view experience aligned to compact + expandable UI decision.
Output: Today/next APIs, dashboard integration, and deterministic rendering behavior tests.
</objective>

<execution_context>
@/home/flo/.codex/get-shit-done/workflows/execute-plan.md
@/home/flo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-program-planning-and-daily-workout-surface/03-CONTEXT.md
@src/app/(private)/dashboard/page.tsx
@src/app/api/profile/route.ts
@src/lib/program/contracts.ts
@src/server/dal/program.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement today/next selection API and session-detail API</name>
  <files>src/lib/program/select-today-session.ts, src/app/api/program/today/route.ts, src/app/api/program/sessions/[sessionId]/route.ts, tests/program/dashboard-today-surface.test.ts</files>
  <behavior>
    - Test 1: `GET /api/program/today` returns `todaySession` when there is a planned session for current date.
    - Test 2: `GET /api/program/today` returns `todaySession: null` and populated `nextSession` when none exists today.
    - Test 3: Both routes return 401 without valid session.
    - Test 4: Session-detail route returns only account-owned exercises with prescription values.
  </behavior>
  <action>Write route/service tests first, then implement account-scoped today/next selection plus session detail endpoint. Keep response contracts parse-validated and include `primaryAction: "start_workout"` in today response.</action>
  <verify>
    <automated>pnpm test tests/program/dashboard-today-surface.test.ts --runInBand</automated>
  </verify>
  <done>Program read endpoints provide the exact data needed for compact dashboard rendering and detail expansion.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate compact daily workout block into dashboard with expandable details</name>
  <files>src/app/(private)/dashboard/page.tsx, src/app/(private)/dashboard/_components/today-workout-card.tsx</files>
  <action>Update dashboard to render the today workout block first, showing compact session summary and CTA `Commencer seance`. Add detail expansion to fetch/show exercise prescriptions (sets, fixed reps, target load, rest range). If no today session exists, render next planned session card while keeping one-page single-view UX.</action>
  <verify>
    <automated>pnpm test tests/program/dashboard-today-surface.test.ts --runInBand</automated>
  </verify>
  <done>Dashboard presents today/next session in a compact, action-first layout with on-demand exercise detail.</done>
</task>

</tasks>

<verification>
Execute dashboard surface tests and ensure route + UI behavior matches today-first and next-session fallback decisions.
</verification>

<success_criteria>
- Daily workout block is visibly top-priority on dashboard.
- Today/next fallback behavior is deterministic and covered by tests.
- Compact view plus detail expansion exposes required exercise prescription fields.
</success_criteria>

<output>
After completion, create `.planning/phases/03-program-planning-and-daily-workout-surface/03-03-SUMMARY.md`.
</output>

