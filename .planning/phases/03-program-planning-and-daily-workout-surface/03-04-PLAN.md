---
phase: 03-program-planning-and-daily-workout-surface
plan: 03-04
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - src/lib/program/substitution.ts
  - src/app/api/program/exercises/[plannedExerciseId]/substitutions/route.ts
  - src/app/api/program/exercises/[plannedExerciseId]/substitute/route.ts
  - tests/program/substitution.test.ts
autonomous: true
requirements:
  - PROG-03
must_haves:
  truths:
    - "Each planned exercise can return a strict-safe Top 3 substitution list."
    - "Substitution candidates always respect limitations, available equipment, and movement pattern compatibility."
    - "Applying a substitution is allowed only for today's session and mutates only the targeted planned exercise row."
  artifacts:
    - path: src/lib/program/substitution.ts
      provides: "Strict filter and ranking logic for substitution candidates."
      exports:
        - getSubstitutionCandidates
        - applyPlannedExerciseSubstitution
    - path: src/app/api/program/exercises/[plannedExerciseId]/substitutions/route.ts
      provides: "Authenticated candidate-list endpoint capped at Top 3."
      exports:
        - GET
    - path: src/app/api/program/exercises/[plannedExerciseId]/substitute/route.ts
      provides: "Authenticated substitution-apply endpoint for one planned exercise row."
      exports:
        - POST
  key_links:
    - from: src/app/api/program/exercises/[plannedExerciseId]/substitutions/route.ts
      to: src/lib/program/substitution.ts
      via: "candidate computation"
      pattern: "getSubstitutionCandidates"
    - from: src/app/api/program/exercises/[plannedExerciseId]/substitute/route.ts
      to: src/server/dal/program.ts
      via: "single-row substitution persistence"
      pattern: "apply.*Substitution|update"
---

<objective>
Implement strict-safe exercise replacement workflow with Top 3 candidates and today-only substitution application semantics.

Purpose: Satisfy PROG-03 while preserving safety boundaries and preventing unintended global program rewrites.
Output: Substitution logic module, candidate/apply API routes, and behavior tests.
</objective>

<execution_context>
@/home/flo/.codex/get-shit-done/workflows/execute-plan.md
@/home/flo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-program-planning-and-daily-workout-surface/03-CONTEXT.md
@.planning/phases/03-program-planning-and-daily-workout-surface/03-RESEARCH.md
@src/lib/program/catalog.ts
@src/lib/program/contracts.ts
@src/server/dal/program.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add substitution behavior tests then implement strict filtering/ranking logic</name>
  <files>tests/program/substitution.test.ts, src/lib/program/substitution.ts</files>
  <behavior>
    - Test 1: Candidate endpoint logic returns at most 3 substitutions.
    - Test 2: Any candidate violating limitation constraints is excluded.
    - Test 3: Any candidate requiring unavailable equipment is excluded.
    - Test 4: Any candidate with incompatible movement pattern is excluded.
  </behavior>
  <action>Write failing substitution tests first, then implement deterministic filtering and ranking. Use explicit compatibility metadata from catalog/rules only; do not use fuzzy text similarity or ad-hoc client criteria.</action>
  <verify>
    <automated>pnpm test tests/program/substitution.test.ts --runInBand</automated>
  </verify>
  <done>Substitution selection logic enforces strict safety filters and returns stable Top 3 candidates.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement authenticated candidate/apply substitution routes</name>
  <files>src/app/api/program/exercises/[plannedExerciseId]/substitutions/route.ts, src/app/api/program/exercises/[plannedExerciseId]/substitute/route.ts, tests/program/substitution.test.ts</files>
  <behavior>
    - Test 1: Unauthorized requests return 401.
    - Test 2: Candidate route returns strict-safe Top 3 for account-owned planned exercise only.
    - Test 3: Apply route rejects invalid replacement keys with 400.
    - Test 4: Apply route rejects non-today `plannedExerciseId` (including owned future/past sessions) and returns 400.
    - Test 5: Apply route updates only today's targeted planned exercise row, preserving the rest of today's session unchanged.
  </behavior>
  <action>Implement routes with session validation, ownership checks, contract parsing, and DAL-backed update. Explicitly enforce today-only substitution semantics by validating the targeted planned exercise belongs to the user's session scheduled for today's date; reject non-today rows before mutation. Ensure apply semantics remain row-local (`isSubstituted=true`, keep `originalExerciseKey`) and do not trigger global plan regeneration.</action>
  <verify>
    <automated>pnpm test tests/program/substitution.test.ts --runInBand</automated>
  </verify>
  <done>Substitution workflow is secure, strictly filtered, rejects non-today plannedExerciseIds, and applies only to a single planned exercise row in today's session.</done>
</task>

</tasks>

<verification>
Run substitution tests end-to-end to validate candidate quality gates, today-only enforcement, and apply-route mutation scope.
</verification>

<success_criteria>
- Replacement candidates are always strict-safe and limited to Top 3.
- Substitution apply operation is authenticated, account-scoped, single-row only, and rejects non-today plannedExerciseIds.
- No substitution path can bypass equipment/limitation/movement-pattern constraints.
</success_criteria>

<output>
After completion, create `.planning/phases/03-program-planning-and-daily-workout-surface/03-04-SUMMARY.md`.
</output>
