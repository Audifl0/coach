---
phase: 03-program-planning-and-daily-workout-surface
plan: 03-02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - src/lib/program/planner.ts
  - src/server/services/program-generation.ts
  - src/app/api/program/generate/route.ts
  - tests/program/planner.test.ts
  - tests/program/program-generate-route.test.ts
autonomous: true
requirements:
  - PROG-01
  - PROG-02
must_haves:
  truths:
    - "Authenticated user can generate a deterministic 7-day sliding weekly split from profile constraints."
    - "Generated planned exercises include sets, fixed reps, target load, and rest range."
    - "Regeneration preserves core exercise continuity rather than random day-to-day churn."
  artifacts:
    - path: src/lib/program/planner.ts
      provides: "Deterministic split and prescription generation logic."
      exports:
        - buildWeeklyProgramPlan
    - path: src/app/api/program/generate/route.ts
      provides: "Authenticated generation endpoint with validated payload and scoped persistence."
      exports:
        - POST
    - path: tests/program/planner.test.ts
      provides: "Behavior coverage for constraints, safety filtering, and continuity."
  key_links:
    - from: src/app/api/program/generate/route.ts
      to: src/lib/program/planner.ts
      via: "planner invocation in POST handler"
      pattern: "buildWeeklyProgramPlan"
    - from: src/app/api/program/generate/route.ts
      to: src/server/dal/program.ts
      via: "persist generated sessions/exercises"
      pattern: "programDal"
---

<objective>
Implement deterministic weekly program generation and expose it through a validated authenticated API endpoint.

Purpose: Deliver the core behavior behind PROG-01 and PROG-02 so users always get actionable, constrained sessions.
Output: Planner/service implementation, `/api/program/generate` route, and deterministic tests.
</objective>

<execution_context>
@/home/flo/.codex/get-shit-done/workflows/execute-plan.md
@/home/flo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-program-planning-and-daily-workout-surface/03-CONTEXT.md
@.planning/phases/03-program-planning-and-daily-workout-surface/03-RESEARCH.md
@src/lib/program/contracts.ts
@src/lib/program/catalog.ts
@src/server/dal/program.ts
@src/lib/profile/contracts.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add planner tests then implement deterministic split/prescription generation</name>
  <files>tests/program/planner.test.ts, src/lib/program/planner.ts</files>
  <behavior>
    - Test 1: A profile with `weeklySessionTarget` N generates exactly N planned sessions in a rolling 7-day window.
    - Test 2: Exercises violating equipment or limitation constraints are excluded from generated sessions.
    - Test 3: Regeneration keeps core base exercise keys stable unless constraints force change.
    - Test 4: Each planned exercise returns fixed `targetReps`, explicit `targetLoad`, and rest min/max values.
  </behavior>
  <action>Write failing tests first, then implement deterministic plan assembly using catalog + profile constraints. Keep rules-first logic (no LLM calls) and include light continuity logic keyed by previous active plan when present.</action>
  <verify>
    <automated>pnpm test tests/program/planner.test.ts --runInBand</automated>
  </verify>
  <done>Planner logic is deterministic, constraint-safe, and produces prescriptions matching PROG-02 shape.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement authenticated generate endpoint with scoped persistence</name>
  <files>src/server/services/program-generation.ts, src/app/api/program/generate/route.ts, tests/program/program-generate-route.test.ts</files>
  <behavior>
    - Test 1: Unauthenticated generate request returns 401.
    - Test 2: Invalid payload fails contract validation with 400.
    - Test 3: Valid request persists active plan/session/exercise rows for only the authenticated account.
    - Test 4: Response returns generated sessions summary ready for dashboard consumption.
  </behavior>
  <action>Implement route + service wiring with session validation, profile load checks, contract parsing, planner invocation, and DAL persistence in a transaction-safe flow. Return explicit API errors with existing project pattern.</action>
  <verify>
    <automated>pnpm test tests/program/program-generate-route.test.ts --runInBand</automated>
  </verify>
  <done>`POST /api/program/generate` is fully functional, validated, and account-scoped.</done>
</task>

</tasks>

<verification>
Run planner and generate-route tests together to prove deterministic behavior and route-level auth/validation/persistence correctness.
</verification>

<success_criteria>
- Program generation always returns valid 7-day sliding split output from profile constraints.
- Exercise prescription contract includes all required fields for every returned planned exercise.
- Generation endpoint behavior is fully covered for auth, validation, and successful persistence.
</success_criteria>

<output>
After completion, create `.planning/phases/03-program-planning-and-daily-workout-surface/03-02-SUMMARY.md`.
</output>

