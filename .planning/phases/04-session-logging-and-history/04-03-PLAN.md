---
phase: 04-session-logging-and-history
plan: 04-03
type: execute
wave: 3
depends_on:
  - 04-02
files_modified:
  - src/app/api/program/sessions/[sessionId]/route.ts
  - src/app/api/program/sessions/[sessionId]/complete/route.ts
  - src/app/api/program/sessions/[sessionId]/duration/route.ts
  - src/app/api/program/sessions/[sessionId]/note/route.ts
  - src/app/api/program/sessions/[sessionId]/exercises/[plannedExerciseId]/sets/route.ts
  - src/app/api/program/sessions/[sessionId]/exercises/[plannedExerciseId]/skip/route.ts
  - src/app/api/program/history/route.ts
  - tests/program/program-session-logging-route.test.ts
autonomous: true
requirements:
  - LOG-01
  - LOG-02
  - LOG-03
  - LOG-04
must_haves:
  truths:
    - "Authenticated users can autosave and edit logged sets through API endpoints with strict payload validation."
    - "Exercise skip and session note endpoints enforce required/optional fields exactly as decided."
    - "Session completion requires fatigue/readiness feedback and exposes bounded duration correction."
    - "History endpoint returns period-filtered summaries and account-scoped detail drilldown data."
  artifacts:
    - path: src/app/api/program/sessions/[sessionId]/exercises/[plannedExerciseId]/sets/route.ts
      provides: "Authenticated set logging create/update endpoint(s) with immediate autosave semantics."
      exports:
        - POST
        - PATCH
    - path: src/app/api/program/sessions/[sessionId]/complete/route.ts
      provides: "Explicit completion endpoint storing fatigue/readiness/comment and end timestamp."
      exports:
        - POST
    - path: src/app/api/program/history/route.ts
      provides: "Authenticated history list endpoint with 7d/30d/90d/custom filter parsing."
      exports:
        - GET
    - path: tests/program/program-session-logging-route.test.ts
      provides: "Route-level auth/validation/ownership/behavior coverage for new Phase 4 endpoints."
  key_links:
    - from: src/app/api/program/sessions/[sessionId]/exercises/[plannedExerciseId]/sets/route.ts
      to: src/server/services/session-logging.ts
      via: "service call for set autosave lifecycle"
      pattern: "createSessionLoggingService|logSet"
    - from: src/app/api/program/history/route.ts
      to: src/server/dal/program.ts
      via: "history list query and projection"
      pattern: "get.*History"
    - from: src/app/api/program/sessions/[sessionId]/route.ts
      to: src/lib/program/select-today-session.ts
      via: "session detail projection with logged sets"
      pattern: "buildSessionDetailProjection"
---

<objective>
Expose Phase 4 logging and history capabilities through authenticated, contract-validated API routes aligned with current program route patterns.

Purpose: Deliver complete backend API surface for set logging, skip flow, completion feedback, duration correction, and recent history retrieval.
Output: New/extended route handlers with dependency injection and comprehensive route behavior tests.
</objective>

<execution_context>
@/home/flo/.codex/get-shit-done/workflows/execute-plan.md
@/home/flo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-session-logging-and-history/04-CONTEXT.md
@.planning/phases/04-session-logging-and-history/04-RESEARCH.md
@src/app/api/program/today/route.ts
@src/app/api/program/sessions/[sessionId]/route.ts
@src/lib/auth/session-gate.ts
@src/lib/program/contracts.ts
@src/server/dal/program.ts
@src/server/services/session-logging.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement authenticated mutation routes for sets, skip, notes, completion, and duration correction</name>
  <files>src/app/api/program/sessions/[sessionId]/complete/route.ts, src/app/api/program/sessions/[sessionId]/duration/route.ts, src/app/api/program/sessions/[sessionId]/note/route.ts, src/app/api/program/sessions/[sessionId]/exercises/[plannedExerciseId]/sets/route.ts, src/app/api/program/sessions/[sessionId]/exercises/[plannedExerciseId]/skip/route.ts, tests/program/program-session-logging-route.test.ts</files>
  <behavior>
    - Test 1: Unauthenticated requests return 401 for every new mutation route.
    - Test 2: Malformed payloads return 400 using shared parse helpers.
    - Test 3: Account mismatch returns 404/403-equivalent not-found behavior without leaking ownership.
    - Test 4: Happy-path set autosave, skip apply/revert, note update, completion feedback, and duration correction return 200/201 with expected body shape.
  </behavior>
  <action>Create dependency-injected route factories that resolve session via existing session-gate pattern, parse payloads with program contracts, and delegate business rules to session-logging service methods. Preserve deterministic error mapping and avoid duplicating lifecycle logic in handlers.</action>
  <verify>
    <automated>pnpm test tests/program/program-session-logging-route.test.ts --runInBand</automated>
  </verify>
  <done>All Phase 4 mutation actions are reachable through secure, validated API endpoints ready for dashboard autosave UX.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add history list endpoint and enrich session-detail endpoint for history drilldown payloads</name>
  <files>src/app/api/program/history/route.ts, src/app/api/program/sessions/[sessionId]/route.ts, tests/program/program-session-logging-route.test.ts</files>
  <behavior>
    - Test 1: `GET /api/program/history` validates period filters and rejects invalid custom ranges with 400.
    - Test 2: `GET /api/program/history` returns date/duration/exerciseCount/totalLoad rows scoped to authenticated account.
    - Test 3: `GET /api/program/sessions/:sessionId` includes grouped logged sets, skip metadata, and feedback-ready session state fields.
    - Test 4: Unauthorized or non-owned session detail requests remain blocked.
  </behavior>
  <action>Implement history query route with query parsing and DAL projection mapping. Extend session detail route projection payload to include execution-log fields needed by dashboard drilldown while preserving account-owned filtering logic.</action>
  <verify>
    <automated>pnpm test tests/program/program-session-logging-route.test.ts --runInBand</automated>
  </verify>
  <done>Backend API now provides complete logging and history payloads for the Phase 4 UI with security boundaries intact.</done>
</task>

</tasks>

<verification>
Run dedicated logging-route tests to confirm auth, validation, ownership boundaries, and end-to-end route behavior for all new endpoints.
</verification>

<success_criteria>
- API surface includes all required mutation endpoints and history retrieval for LOG-01 through LOG-04.
- Route handlers remain thin and deterministic by relying on shared contracts + service/DAL layers.
- Session detail and history payloads contain all fields required for logging review in dashboard UI.
</success_criteria>

<output>
After completion, create `.planning/phases/04-session-logging-and-history/04-03-SUMMARY.md`.
</output>
