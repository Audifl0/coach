---
phase: 04-session-logging-and-history
plan: 04-04
type: execute
wave: 4
depends_on:
  - 04-03
files_modified:
  - src/app/(private)/dashboard/page.tsx
  - src/app/(private)/dashboard/_components/today-workout-card.tsx
  - src/app/(private)/dashboard/_components/session-logger.tsx
  - src/app/(private)/dashboard/_components/session-history-card.tsx
  - tests/program/dashboard-today-surface.test.ts
  - tests/program/session-history-surface.test.ts
autonomous: true
requirements:
  - LOG-01
  - LOG-02
  - LOG-03
  - LOG-04
must_haves:
  truths:
    - "User can log sets inline (weight/reps/optional RPE) with immediate autosave and editable entries during the workout."
    - "User can skip an exercise only with required reason and can revert skip before workout completion."
    - "User sees a visible running timer that starts on first logged set, stops on explicit completion, and supports post-completion duration correction."
    - "User can submit fatigue/readiness feedback at completion and browse recent history with 7d/30d/90d/custom filters plus session detail."
  artifacts:
    - path: src/app/(private)/dashboard/_components/session-logger.tsx
      provides: "Set-by-set logging interaction, timer display, skip reason flow, note input, and completion feedback prompt."
    - path: src/app/(private)/dashboard/_components/session-history-card.tsx
      provides: "Recent history list with period filters and detail drilldown trigger."
    - path: src/app/(private)/dashboard/_components/today-workout-card.tsx
      provides: "Integration point that routes primary action into active session logging/resume state."
    - path: tests/program/session-history-surface.test.ts
      provides: "Deterministic tests for history filter modes and detail rendering decisions."
  key_links:
    - from: src/app/(private)/dashboard/_components/session-logger.tsx
      to: /api/program/sessions/:sessionId/exercises/:plannedExerciseId/sets
      via: "autosave logging fetch calls"
      pattern: "/api/program/sessions/.*/sets"
    - from: src/app/(private)/dashboard/_components/session-logger.tsx
      to: /api/program/sessions/:sessionId/complete
      via: "explicit completion submission"
      pattern: "/api/program/sessions/.*/complete"
    - from: src/app/(private)/dashboard/_components/session-history-card.tsx
      to: /api/program/history
      via: "history list retrieval with period/custom query params"
      pattern: "/api/program/history\\?"
---

<objective>
Integrate Phase 4 logging and history behaviors into the private dashboard with low-friction workout flow and reviewable recent history.

Purpose: Deliver the user-facing outcomes for reliable daily adherence tracking and session review.
Output: Dashboard logging/history components wired to new APIs, with deterministic UI behavior tests.
</objective>

<execution_context>
@/home/flo/.codex/get-shit-done/workflows/execute-plan.md
@/home/flo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-session-logging-and-history/04-CONTEXT.md
@src/app/(private)/dashboard/page.tsx
@src/app/(private)/dashboard/_components/today-workout-card.tsx
@src/lib/program/contracts.ts
@src/app/api/program/history/route.ts
@src/app/api/program/sessions/[sessionId]/route.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build in-session logger UI with autosave sets, skip reason flow, timer lifecycle, and completion feedback</name>
  <files>src/app/(private)/dashboard/_components/session-logger.tsx, src/app/(private)/dashboard/_components/today-workout-card.tsx, tests/program/dashboard-today-surface.test.ts</files>
  <behavior>
    - Test 1: Set entry posts weight/reps/optional RPE immediately and reflects saved values for edit continuity.
    - Test 2: Skip action requires reason selection before submission and supports same-session revert.
    - Test 3: Timer state starts on first successful set save and remains visible until explicit completion.
    - Test 4: Completion flow requires fatigue/readiness values (1..5) and supports optional comment submission.
  </behavior>
  <action>Create a dedicated `SessionLogger` component and wire it from `TodayWorkoutCard` primary action/resume path. Keep optional note field non-blocking, enforce 280-char limit client-side + server-side, and integrate duration correction affordance after completion (within API-enforced window).</action>
  <verify>
    <automated>pnpm test tests/program/dashboard-today-surface.test.ts --runInBand</automated>
  </verify>
  <done>Daily workout surface supports full session execution logging lifecycle with minimal friction and explicit completion semantics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add recent history panel with period filters and session detail drilldown</name>
  <files>src/app/(private)/dashboard/_components/session-history-card.tsx, src/app/(private)/dashboard/page.tsx, tests/program/session-history-surface.test.ts</files>
  <behavior>
    - Test 1: History filter controls request 7d/30d/90d/custom ranges with deterministic query generation.
    - Test 2: History list rows render date, duration, exercise count, and total load values.
    - Test 3: Selecting a history row loads session detail and shows exercises with grouped logged sets.
    - Test 4: Empty/error states are explicit without breaking active workout surface.
  </behavior>
  <action>Add a `SessionHistoryCard` to dashboard below the today-workout block, fetch history data from `/api/program/history`, and provide drilldown to session detail route data. Keep layout compact and action-first while preserving current dashboard auth/onboarding routing behavior.</action>
  <verify>
    <automated>pnpm test tests/program/session-history-surface.test.ts --runInBand</automated>
  </verify>
  <done>Users can browse recent sessions with required summary fields and inspect detailed logged performance from the same dashboard context.</done>
</task>

</tasks>

<verification>
Run dashboard surface tests plus new history-surface tests to verify logging interactions, completion feedback flow, and history filter/detail behaviors.
</verification>

<success_criteria>
- Dashboard enables complete set logging, skip reasons, optional notes, explicit completion feedback, and timer visibility aligned to locked decisions.
- History browsing supports 7d/30d/90d/custom filtering with row summaries and detail drilldown.
- User-facing behaviors for LOG-01/02/03/04 are test-covered and integrated in one private surface.
</success_criteria>

<output>
After completion, create `.planning/phases/04-session-logging-and-history/04-04-SUMMARY.md`.
</output>
