---
phase: 04-session-logging-and-history
plan: 04-01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - prisma/migrations/*_session_logging_init/migration.sql
  - src/lib/program/contracts.ts
  - src/lib/program/select-today-session.ts
  - tests/program/contracts.test.ts
autonomous: true
requirements:
  - LOG-01
  - LOG-02
  - LOG-03
  - LOG-04
must_haves:
  truths:
    - "Session logging contracts accept set-by-set autosave payloads with required weight/reps and optional RPE."
    - "Skip payloads require a structured reason while keeping optional free text."
    - "History query contracts support 7d, 30d, 90d, and custom range filters with strict validation."
  artifacts:
    - path: prisma/schema.prisma
      provides: "Execution persistence fields and LoggedSet relation model tied to planned sessions/exercises."
      contains: "model LoggedSet"
    - path: src/lib/program/contracts.ts
      provides: "Zod parse helpers for logging, skip, completion, duration correction, and history query payloads."
      exports:
        - parseLoggedSetInput
        - parseExerciseSkipInput
        - parseSessionCompleteInput
        - parseHistoryQueryInput
    - path: src/lib/program/select-today-session.ts
      provides: "Projection helpers for history rows and session detail responses with grouped logged sets."
  key_links:
    - from: src/lib/program/contracts.ts
      to: src/app/api/program/sessions/*
      via: "route request/query parsing"
      pattern: "parse.*(Set|Skip|Complete|History)"
    - from: src/lib/program/select-today-session.ts
      to: src/app/api/program/history/route.ts
      via: "history response projection"
      pattern: "build.*History|parseProgram"
---

<objective>
Create the phase foundation for session execution logging by extending persistence schema and API contracts before DAL/routes/UI work starts.

Purpose: Ensure all downstream implementation uses stable data shapes and validated constraints from day one.
Output: Prisma schema + migration, logging/history contracts, and projection helpers verified by contract tests.
</objective>

<execution_context>
@/home/flo/.codex/get-shit-done/workflows/execute-plan.md
@/home/flo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-session-logging-and-history/04-CONTEXT.md
@.planning/phases/04-session-logging-and-history/04-RESEARCH.md
@prisma/schema.prisma
@src/lib/program/contracts.ts
@src/lib/program/select-today-session.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add execution persistence schema and migration for logs, skip metadata, and completion feedback</name>
  <files>prisma/schema.prisma, prisma/migrations/*_session_logging_init/migration.sql</files>
  <action>Extend `PlannedSession` with execution lifecycle fields (`startedAt`, `completedAt`, `effectiveDurationSec`, `durationCorrectedAt`, note/comment/feedback fields), extend `PlannedExercise` with skip metadata, and add `LoggedSet` with unique `(plannedExerciseId, setIndex)` plus account/session indexes. Keep relations account-scoped and aligned with existing `ProgramPlan -> PlannedSession -> PlannedExercise` ownership model.</action>
  <verify>
    <automated>pnpm prisma migrate dev --name session_logging_init --create-only</automated>
  </verify>
  <done>Schema and migration define all storage primitives required for LOG-01 through LOG-04 without introducing a parallel workout domain.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend program contracts for logging, skip, completion, duration correction, and history filters</name>
  <files>src/lib/program/contracts.ts, tests/program/contracts.test.ts</files>
  <behavior>
    - Test 1: Logged-set payload requires positive `weight`, integer `reps >= 1`, and supports optional bounded `rpe`.
    - Test 2: Skip payload rejects missing `reasonCode` while allowing optional trimmed `reasonText`.
    - Test 3: Session completion payload enforces `fatigue`/`readiness` scales from 1 to 5.
    - Test 4: History query parsing accepts 7d/30d/90d and validates `custom` ranges (`from` and `to` required and ordered).
  </behavior>
  <action>Write failing contract tests first, then add Zod schemas and parse helpers for all new request/query boundaries, including note max-length and duration-correction payload validation. Keep existing contract naming conventions and deterministic parse failure behavior.</action>
  <verify>
    <automated>pnpm test tests/program/contracts.test.ts --runInBand</automated>
  </verify>
  <done>All new logging/history boundaries are parse-validated by shared helpers used directly by route handlers.</done>
</task>

<task type="auto">
  <name>Task 3: Add projection helpers for history summaries and detailed grouped set logs</name>
  <files>src/lib/program/select-today-session.ts</files>
  <action>Extend projection utilities to return typed history list rows (`date`, `duration`, `exerciseCount`, `totalLoad`) and session detail structures grouped by exercise with logged sets and skip markers. Keep mapping deterministic and contract-validated through existing parse helpers.</action>
  <verify>
    <automated>pnpm test tests/program/contracts.test.ts --runInBand</automated>
  </verify>
  <done>Shared projection layer exists for both history route output and dashboard detail rendering with no route-local shape drift.</done>
</task>

</tasks>

<verification>
Validate migration generation and run contracts tests to confirm the schema/contracts/projections layer is complete and stable for dependent plans.
</verification>

<success_criteria>
- Data model now supports persisted set logs, skip reasons, completion feedback, and duration correction metadata.
- Route-ready parse helpers exist for every new logging and history endpoint input/query.
- History/session-detail response projection helpers are available for API and dashboard consumers.
</success_criteria>

<output>
After completion, create `.planning/phases/04-session-logging-and-history/04-01-SUMMARY.md`.
</output>
