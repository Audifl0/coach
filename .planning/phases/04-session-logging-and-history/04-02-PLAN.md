---
phase: 04-session-logging-and-history
plan: 04-02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - src/server/dal/program.ts
  - src/server/services/session-logging.ts
  - tests/program/program-dal.test.ts
autonomous: true
requirements:
  - LOG-01
  - LOG-02
  - LOG-03
  - LOG-04
must_haves:
  truths:
    - "Set autosave upserts are account-scoped and editable during an active session."
    - "Skip and unskip behavior enforces required reason data and blocks invalid post-completion edits."
    - "Completion and duration correction flows enforce explicit finish plus 24-hour correction window."
    - "History list/detail queries return only account-owned sessions and exercise logs."
  artifacts:
    - path: src/server/dal/program.ts
      provides: "Account-scoped persistence methods for logged sets, skip/revert, notes, completion, duration correction, and history projection reads."
      exports:
        - createProgramDal
    - path: src/server/services/session-logging.ts
      provides: "Execution-rule service for timer start/stop invariants and mutation guardrails."
      exports:
        - createSessionLoggingService
    - path: tests/program/program-dal.test.ts
      provides: "Behavior and security coverage for logging DAL and execution service invariants."
  key_links:
    - from: src/server/services/session-logging.ts
      to: src/server/dal/program.ts
      via: "service orchestration of scoped DAL writes"
      pattern: "programDal\\.(upsert|mark|complete|correct|getHistory)"
    - from: src/server/dal/program.ts
      to: prisma/schema.prisma
      via: "LoggedSet/PlannedSession/PlannedExercise model operations"
      pattern: "prisma\\.(loggedSet|plannedSession|plannedExercise)"
---

<objective>
Implement the account-scoped logging domain behavior in DAL and service layers, including timer/completion rules and history retrieval.

Purpose: Centralize execution invariants so routes/UI stay thin and safe by default.
Output: Expanded `program` DAL, new session-logging service module, and deterministic DAL/service tests.
</objective>

<execution_context>
@/home/flo/.codex/get-shit-done/workflows/execute-plan.md
@/home/flo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-session-logging-and-history/04-CONTEXT.md
@.planning/phases/04-session-logging-and-history/04-RESEARCH.md
@src/server/dal/program.ts
@src/server/dal/account-scope.ts
@src/lib/program/contracts.ts
@prisma/schema.prisma
@tests/program/program-dal.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add account-scoped DAL methods for logging mutations and history reads</name>
  <files>src/server/dal/program.ts, tests/program/program-dal.test.ts</files>
  <behavior>
    - Test 1: Upsert by `(plannedExerciseId, setIndex)` updates existing set rows instead of creating duplicates.
    - Test 2: Skip apply requires reason code, skip revert is allowed only while session is not completed.
    - Test 3: Completion persists feedback and marks session complete once; subsequent logging edits are rejected.
    - Test 4: History queries return only account-owned sessions and include aggregated `totalLoad` + grouped sets.
  </behavior>
  <action>Write failing DAL tests first, then extend `createProgramDal` with typed methods for set upsert/edit, skip/revert, note updates, completion persistence, duration correction persistence, history list retrieval, and detailed session retrieval with grouped logged sets. Preserve current account-scope guardrail patterns (`requireAccountScope`, `buildAccountScopedWhere`, ownership assertions).</action>
  <verify>
    <automated>pnpm test tests/program/program-dal.test.ts --runInBand</automated>
  </verify>
  <done>DAL exposes complete Phase 4 read/write primitives with account isolation and deterministic mutation semantics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement session-logging service invariants (timer start, completion lock, correction window)</name>
  <files>src/server/services/session-logging.ts, tests/program/program-dal.test.ts</files>
  <behavior>
    - Test 1: First successful logged set sets `startedAt`; later set edits keep original start time.
    - Test 2: Completing a session computes `effectiveDurationSec` from `startedAt` to completion time.
    - Test 3: Duration correction is accepted up to `completedAt + 24h` and rejected afterward.
    - Test 4: Service blocks set/skip/note mutations after completion while still allowing valid duration correction.
  </behavior>
  <action>Add `createSessionLoggingService` that coordinates DAL calls and enforces cross-entity temporal rules with injected `now` for deterministic testing. Keep route-agnostic interfaces and explicit result/error mapping so handlers can return stable 4xx statuses without business logic duplication.</action>
  <verify>
    <automated>pnpm test tests/program/program-dal.test.ts --runInBand</automated>
  </verify>
  <done>All lifecycle rules are enforced in one service layer and covered by deterministic tests.</done>
</task>

</tasks>

<verification>
Run DAL tests after implementation to verify account scope, mutation lifecycles, timer semantics, and correction-window enforcement together.
</verification>

<success_criteria>
- DAL/service layer can fully power LOG-01/02/03 logging workflows and LOG-04 history retrieval without route-level business logic.
- Timer and completion behavior follows locked decisions (first set starts, explicit complete stops, 24h correction).
- Post-completion mutation guardrails are enforced consistently.
</success_criteria>

<output>
After completion, create `.planning/phases/04-session-logging-and-history/04-02-SUMMARY.md`.
</output>
