---
phase: 04-session-logging-and-history
plan: 04-05
type: execute
wave: 4
depends_on:
  - 04-03
files_modified:
  - src/lib/program/select-today-session.ts
  - src/app/api/program/sessions/[sessionId]/route.ts
  - tests/program/program-session-logging-route.test.ts
autonomous: true
gap_closure: true
requirements:
  - LOG-01
  - LOG-02
  - LOG-03
  - LOG-04
must_haves:
  truths:
    - "Authenticated owners requesting session detail receive grouped logged sets plus exercise skip metadata, note, and completion feedback fields needed by the dashboard drilldown."
    - "Session detail requests for non-owned or missing sessions remain masked as not found and do not leak ownership."
    - "Dashboard history drilldown behavior remains unchanged after the refactor, with the same session-detail payload shape available to clients."
  artifacts:
    - path: src/lib/program/select-today-session.ts
      provides: "Projection helper contract for session detail payload consumed by the session detail route."
      exports:
        - buildSessionDetailProjection
    - path: src/app/api/program/sessions/[sessionId]/route.ts
      provides: "Authenticated session detail route delegating response shaping to shared projection helper."
      exports:
        - GET
    - path: tests/program/program-session-logging-route.test.ts
      provides: "Route behavior coverage ensuring enriched session detail payload remains stable after projection refactor."
  key_links:
    - from: src/app/api/program/sessions/[sessionId]/route.ts
      to: src/lib/program/select-today-session.ts
      via: "shared session detail projection helper invocation"
      pattern: "buildSessionDetailProjection"
---

<objective>
Close verification gap `04-03 key_link mismatch` by moving session detail response shaping from route-local inline mapping to the shared projection helper.

Purpose: Re-establish the planned architecture link so projection logic stays centralized and less prone to route-level drift.
Output: Session detail route uses `buildSessionDetailProjection` while preserving current enriched payload behavior.
</objective>

<execution_context>
@/home/flo/.codex/get-shit-done/workflows/execute-plan.md
@/home/flo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-session-logging-and-history/04-03-PLAN.md
@.planning/phases/04-session-logging-and-history/04-03-SUMMARY.md
@.planning/phases/04-session-logging-and-history/04-VERIFICATION.md
@src/lib/program/select-today-session.ts
@src/app/api/program/sessions/[sessionId]/route.ts
@tests/program/program-session-logging-route.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Refactor session detail projection flow to use buildSessionDetailProjection</name>
  <files>src/lib/program/select-today-session.ts, src/app/api/program/sessions/[sessionId]/route.ts, tests/program/program-session-logging-route.test.ts</files>
  <behavior>
    - Test 1: Existing session-detail route tests continue passing with enriched logged-set/skip/feedback fields unchanged.
    - Test 2: Route source includes direct use of `buildSessionDetailProjection` for response shaping.
  </behavior>
  <action>Extend `buildSessionDetailProjection` so it can represent the enriched session detail payload currently assembled inline in the route. Update the route handler to delegate payload shaping to that helper after ownership filtering. Keep auth, not-found masking, and DAL lookup behavior unchanged.</action>
  <verify>
    <automated>pnpm test tests/program/program-session-logging-route.test.ts --runInBand && rg "buildSessionDetailProjection" src/app/api/program/sessions/\[sessionId\]/route.ts</automated>
  </verify>
  <done>Verification gap `04-03 key_link mismatch` is resolved and the session detail route now follows the planned projection helper linkage.</done>
</task>

</tasks>

<verification>
Run the logging route test suite and confirm the route directly references `buildSessionDetailProjection` so the key-link is structurally present.
</verification>

<success_criteria>
- `buildSessionDetailProjection` is the canonical shaping path used by `GET /api/program/sessions/:sessionId`.
- Session detail response contract remains backward-compatible for the Phase 4 dashboard history drilldown.
- Verification evidence directly closes the `04-03 key_link mismatch` item from `04-VERIFICATION.md`.
</success_criteria>

<output>
After completion, create `.planning/phases/04-session-logging-and-history/04-05-SUMMARY.md`.
</output>
