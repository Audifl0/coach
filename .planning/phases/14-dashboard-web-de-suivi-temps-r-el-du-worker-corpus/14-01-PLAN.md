---
phase: 14-dashboard-web-de-suivi-temps-r-el-du-worker-corpus
plan: 14-01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/program/contracts.ts
  - src/server/dashboard/worker-dashboard.ts
  - src/app/(private)/dashboard/worker-corpus/loaders/overview.ts
  - tests/program/worker-corpus-dashboard.test.ts
autonomous: true
requirements:
  - DASH-02
  - PLAT-02
  - SAFE-03
user_setup: []
must_haves:
  truths:
    - "Le dashboard worker lit les artefacts reels du corpus via une projection serveur dediee, sans lecture directe des JSON depuis l'UI."
    - "Les payloads overview/run/snapshot sont valides par schemas partages et distinguent clairement `ready`, `empty` et `error`."
    - "Les signaux critiques de lease, freshness, snapshot actif et rollback sont derivables cote serveur de maniere deterministe."
  artifacts:
    - path: src/server/dashboard/worker-dashboard.ts
      provides: "Lecture defensive des artefacts worker, calcul des severites et projections overview/recent runs/snapshot."
    - path: src/lib/program/contracts.ts
      provides: "Schemas Zod et types partages pour le dashboard worker et ses endpoints detail."
    - path: src/app/(private)/dashboard/worker-corpus/loaders/overview.ts
      provides: "Loader SSR minces relies au service dashboard worker."
  key_links:
    - from: src/server/dashboard/worker-dashboard.ts
      to: scripts/adaptive-knowledge/worker-state.ts
      via: "Le dashboard derive l'etat live depuis `worker-state.json` et ses statuts de lease/heartbeat."
      pattern: "started|heartbeat|completed|failed|blocked-by-lease|stale"
    - from: src/server/dashboard/worker-dashboard.ts
      to: src/lib/program/contracts.ts
      via: "Les projections worker sont parsees et exposees via des schemas partageables stables."
      pattern: "worker-corpus|overview|snapshot|run"
---

<objective>
Poser le socle serveur et contractuel du dashboard worker corpus.

Purpose: normaliser la lecture des artefacts reels du worker en projections serveur stables, testables et reutilisables par la page overview et les routes detail.
Output: contrats partages, service `worker-dashboard` et loader overview capables d'exposer l'etat operateur sans self-fetch ni parsing disperse.
</objective>

<execution_context>
@/home/flo/.codex/get-shit-done/workflows/execute-plan.md
@/home/flo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-dashboard-web-de-suivi-temps-r-el-du-worker-corpus/14-CONTEXT.md
@.planning/phases/14-dashboard-web-de-suivi-temps-r-el-du-worker-corpus/14-RESEARCH.md
@src/lib/program/contracts.ts
@src/server/dashboard/program-dashboard.ts
@src/app/(private)/dashboard/loaders/dashboard-access.ts
@scripts/adaptive-knowledge/worker-state.ts
@scripts/adaptive-knowledge/pipeline-run.ts
@scripts/adaptive-knowledge/publish.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Definir les contrats partages du dashboard worker</name>
  <files>src/lib/program/contracts.ts, tests/program/worker-corpus-dashboard.test.ts</files>
  <behavior>
    - Test 1: un payload overview valide parse statuts live, freshness, snapshot actif et rollback.
    - Test 2: un payload run detail invalide ou partiel est rejete explicitement par schema.
    - Test 3: l'absence de snapshot actif ou de rollback reste representable comme etat `empty` ou valeur `null`, pas comme exception implicite.
  </behavior>
  <action>Ajouter les schemas Zod et types necessaires pour `WorkerCorpusOverview`, `WorkerCorpusRunRow`, `WorkerCorpusRunDetail` et `WorkerCorpusSnapshotDetail`. Garder des payloads compacts et orientes UI, sans exposer les dumps JSON bruts du worker.</action>
  <verify>
    <automated>corepack pnpm test tests/program/worker-corpus-dashboard.test.ts --runInBand</automated>
  </verify>
  <done>Le dashboard worker a des contrats stables partageables entre loaders SSR, routes API et composants de refresh.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implementer la projection serveur des artefacts worker</name>
  <files>src/server/dashboard/worker-dashboard.ts, tests/program/worker-corpus-dashboard.test.ts</files>
  <behavior>
    - Test 1: le service lit `worker-state.json`, `active.json`, `rollback.json` et les snapshots valides en derivees `ready`.
    - Test 2: des artefacts absents, corrompus ou partiels retournent `empty` ou `error` de facon defensive, avec severite exploitable.
    - Test 3: les calculs `isHeartbeatStale`, `snapshotAgeHours` et `lastRunAgeHours` sont derives cote serveur de maniere deterministe.
  </behavior>
  <action>Creer `src/server/dashboard/worker-dashboard.ts` comme couche unique de lecture des artefacts sous `.planning/knowledge/adaptive-coaching`, avec tri des snapshots, mapping des run-reports et severites operateur (`healthy`, `degraded`, `critical`). Reprendre le pattern `ready/empty/error` du dashboard existant et journaliser les degradations sans crasher la page.</action>
  <verify>
    <automated>corepack pnpm test tests/program/worker-corpus-dashboard.test.ts --runInBand</automated>
  </verify>
  <done>Le projet dispose d'une frontiere serveur propre pour tout le dashboard worker.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Exposer un loader SSR overview mince et authentifiable</name>
  <files>src/app/(private)/dashboard/worker-corpus/loaders/overview.ts, src/server/dashboard/worker-dashboard.ts, tests/program/worker-corpus-dashboard.test.ts</files>
  <behavior>
    - Test 1: le loader renvoie `ready`, `empty` ou `error` sans dupliquer la logique de parse des artefacts.
    - Test 2: un etat degrade du worker reste chargeable par la page avec message operateur explicite.
    - Test 3: aucune lecture filesystem n'est faite depuis la page React elle-meme.
  </behavior>
  <action>Ajouter le loader `overview` du segment prive `dashboard/worker-corpus` pour encapsuler l'appel au service dashboard worker. Le loader doit rester server-first, composeable avec `resolveDashboardAccess()`, et preparer le terrain pour la page overview de 14-02.</action>
  <verify>
    <automated>corepack pnpm test tests/program/worker-corpus-dashboard.test.ts --runInBand</automated>
  </verify>
  <done>Le dashboard worker peut etre rendu cote serveur sans couplage direct aux artefacts dans les composants.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `corepack pnpm test tests/program/worker-corpus-dashboard.test.ts --runInBand`
- [ ] `rg -n "worker-corpus|ready|empty|error|stale|rollback|snapshotAgeHours" src/lib/program/contracts.ts src/server/dashboard/worker-dashboard.ts src/app/(private)/dashboard/worker-corpus/loaders/overview.ts`
- [ ] Verifier qu'aucun composant UI ou page React ne lit directement `.planning/knowledge/adaptive-coaching/*`.
- [ ] Verifier que les projections couvrent explicitement `worker-state.json`, `active.json` et `rollback.json`.
</verification>

<success_criteria>
- All tasks completed.
- All verification checks pass.
- Le socle serveur du dashboard worker est centralise, typé et robuste aux artefacts absents ou partiels.
- Les futurs ecrans peuvent consommer des contrats stables sans reparser les fichiers worker.
</success_criteria>

<output>
After completion, create `.planning/phases/14-dashboard-web-de-suivi-temps-r-el-du-worker-corpus/14-01-SUMMARY.md`
</output>
