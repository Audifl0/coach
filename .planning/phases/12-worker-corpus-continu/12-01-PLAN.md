---
phase: 12-worker-corpus-continu
plan: 12-01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/adaptive-knowledge/refresh-corpus.ts
  - scripts/adaptive-knowledge/pipeline-run.ts
  - scripts/adaptive-knowledge/worker-state.ts
  - infra/scripts/install-adaptive-corpus-cron.sh
  - tests/program/adaptive-knowledge-worker.test.ts
autonomous: true
requirements:
  - PROG-01
  - SAFE-03
must_haves:
  truths:
    - "Le worker corpus continu s'execute de maniere idempotente avec lease/lock explicite, sans chevauchement silencieux entre deux runs."
    - "Chaque run persiste un etat observable (started, heartbeat, completed, failed, stale) permettant reprise et diagnostic sans toucher au snapshot actif."
    - "Un run interrompt ou invalide degrade sans corrompre la bible active ni casser le fallback prudent des consommateurs."
  artifacts:
    - path: scripts/adaptive-knowledge/worker-state.ts
      provides: "Contrat de lock/lease, heartbeat et etat de run pour le worker corpus continu."
      exports:
        - acquireAdaptiveKnowledgeLease
        - heartbeatAdaptiveKnowledgeLease
        - releaseAdaptiveKnowledgeLease
    - path: scripts/adaptive-knowledge/pipeline-run.ts
      provides: "Orchestrateur capable d'etre pilote par un worker idempotent avec etat de run et statuts explicites."
      exports:
        - runAdaptiveKnowledgePipeline
    - path: infra/scripts/install-adaptive-corpus-cron.sh
      provides: "Installation ops d'une cadence nominale qui ne declenche que le worker garde-fou et non un refresh nu."
  key_links:
    - from: scripts/adaptive-knowledge/worker-state.ts
      to: scripts/adaptive-knowledge/pipeline-run.ts
      via: "Le worker encadre chaque run du pipeline par acquisition de lease et release finale."
      pattern: "lease|heartbeat|runState"
    - from: scripts/adaptive-knowledge/pipeline-run.ts
      to: infra/scripts/install-adaptive-corpus-cron.sh
      via: "Le flux ops appelle l'entree worker idempotente plutot qu'une orchestration fragile."
      pattern: "refresh-corpus|worker"
---

<objective>
Transformer le pipeline corpus existant en worker autonome et idempotent capable de tourner en continu sans collision de runs ni etat ambigu.

Purpose: etablir la base d'execution fiable du worker avant d'enrichir la decouverte et la curation IA.
Output: etat de worker/lease, orchestration de run gardee, et point d'entree ops/cron coherent.
</objective>

<execution_context>
@/home/flo/.codex/get-shit-done/workflows/execute-plan.md
@/home/flo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-worker-corpus-continu/12-CONTEXT.md
@.planning/phases/12-worker-corpus-continu/12-RESEARCH.md
@scripts/adaptive-knowledge/refresh-corpus.ts
@scripts/adaptive-knowledge/pipeline-run.ts
@infra/scripts/install-adaptive-corpus-cron.sh
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Ajouter un contrat de worker state avec lock, lease, heartbeat et reprise saine</name>
  <files>scripts/adaptive-knowledge/worker-state.ts, tests/program/adaptive-knowledge-worker.test.ts</files>
  <behavior>
    - Test 1: un second run concurrent est refuse tant que le lease actif reste valide.
    - Test 2: un lease stale peut etre repris explicitement sans ecraser un run sain.
    - Test 3: les transitions `started -> heartbeat -> completed|failed` sont persistables et deterministes.
  </behavior>
  <action>Introduire une petite couche de coordination du worker qui persiste l'etat du run, son expiration et ses heartbeats dans le repertoire corpus. Cette couche doit etre purement orientee orchestration et ne doit jamais muter le snapshot actif.</action>
  <verify>
    <automated>corepack pnpm test tests/program/adaptive-knowledge-worker.test.ts --runInBand</automated>
  </verify>
  <done>Le worker dispose d'une base de coordination fiable contre les collisions et les runs orphelins.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Faire piloter `pipeline-run` par le worker idempotent et exposer des statuts de run exploitables</name>
  <files>scripts/adaptive-knowledge/pipeline-run.ts, scripts/adaptive-knowledge/refresh-corpus.ts, tests/program/adaptive-knowledge-worker.test.ts</files>
  <behavior>
    - Test 1: l'entree worker encapsule un run complet en acquittant lease + release.
    - Test 2: une erreur d'ingestion/synthese marque le run comme `failed` sans corrompre `active.json`.
    - Test 3: l'entree CLI retourne des codes de sortie explicites pour `completed`, `skipped`, `blocked-by-lease` et `failed`.
  </behavior>
  <action>Refactorer l'entree `refresh-corpus.ts` pour passer par la couche worker et enrichir `pipeline-run.ts` avec les statuts et metadonnees necessaires a une execution continue observable.</action>
  <verify>
    <automated>corepack pnpm test tests/program/adaptive-knowledge-worker.test.ts tests/program/adaptive-knowledge-pipeline-run.test.ts --runInBand</automated>
  </verify>
  <done>Le pipeline corpus est executable comme vrai worker garde plutot que comme simple script ponctuel.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Durcir l'installation ops nominale autour du worker plutot que du refresh brut</name>
  <files>infra/scripts/install-adaptive-corpus-cron.sh, scripts/adaptive-knowledge/refresh-corpus.ts, tests/program/adaptive-evidence-corpus-loader.test.ts</files>
  <behavior>
    - Test 1: le script d'installation cron pointe vers l'entree worker nominale.
    - Test 2: le mode check/health reste disponible sans lancer de mutation de snapshot.
    - Test 3: la documentation d'installation embarquee dans le script signale les hypotheses de lease et les repertoires touches.
  </behavior>
  <action>Conserver une cadence ops simple, mais faire du script cron un front-end vers le worker protege. Le mode nominal doit rester automatisable, diagnosable et compatible avec un environnement VPS sans supervision complexe.</action>
  <verify>
    <automated>corepack pnpm test tests/program/adaptive-evidence-corpus-loader.test.ts tests/program/adaptive-knowledge-worker.test.ts --runInBand</automated>
  </verify>
  <done>Le declenchement ops standard passe bien par le worker controle et non par un script fragile.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `corepack pnpm test tests/program/adaptive-knowledge-worker.test.ts --runInBand`
- [ ] `corepack pnpm test tests/program/adaptive-knowledge-pipeline-run.test.ts --runInBand`
- [ ] `rg -n "lease|heartbeat|blocked-by-lease|stale" scripts/adaptive-knowledge scripts/adaptive-knowledge/refresh-corpus.ts`
- [ ] Verifier qu'aucune erreur de worker ne modifie `active.json` ni `rollback.json`.
</verification>

<success_criteria>
- All tasks completed.
- All verification checks pass.
- Le worker corpus continu ne peut pas lancer deux runs concurrents silencieux.
- Un echec de run laisse le snapshot actif intact et observable.
</success_criteria>

<output>
After completion, create `.planning/phases/12-worker-corpus-continu/12-01-SUMMARY.md`
</output>
