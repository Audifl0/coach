---
phase: 08-release-blockers-and-regression-restoration
plan: 08-06
type: execute
wave: 6
depends_on:
  - 08-05
files_modified:
  - next.config.ts
  - src/app/(private)/dashboard/page.tsx
  - tests/program/dashboard-today-surface.test.ts
  - tests/program/dashboard-trends-surface.test.ts
autonomous: true
gap_closure: true
requirements:
  - PLAT-01
  - PLAT-03
must_haves:
  truths:
    - "`corepack pnpm build` exits `0` at current HEAD without `Next.js build worker exited with code: 1 and signal: null`."
    - "Dashboard server rendering no longer depends on fragile same-origin HTTP self-fetch behavior during static generation."
    - "Dashboard today/trends projection behavior remains contract-compatible after the build-path fix."
  artifacts:
    - path: next.config.ts
      provides: "Next.js build configuration pinned to the project root and stable build-worker behavior."
      exports:
        - default
    - path: src/app/(private)/dashboard/page.tsx
      provides: "Dashboard server data-loading path that avoids build-time worker crashes while preserving authenticated route semantics."
      exports:
        - default
        - pickDashboardSession
        - loadProgramTrendsData
    - path: tests/program/dashboard-today-surface.test.ts
      provides: "Regression coverage for today-session projection behavior after dashboard data-loading/build-path adjustments."
  key_links:
    - from: src/app/(private)/dashboard/page.tsx
      to: src/app/api/program/today/route.ts
      via: "dashboard today-session data sourcing avoids build-time self-fetch crash path while preserving the existing response contract"
      pattern: "ProgramTodayResponse|selectTodayWorkoutProjection"
    - from: src/app/(private)/dashboard/page.tsx
      to: src/app/api/program/trends/route.ts
      via: "trends loading remains schema-validated and contract-compatible after build-path stabilization"
      pattern: "parseProgramTrendsSummaryResponse|period=30d"
    - from: tests/program/dashboard-trends-surface.test.ts
      to: src/app/(private)/dashboard/page.tsx
      via: "regression tests verify dashboard trends loader contract after crash remediation"
      pattern: "loadProgramTrendsData|resolveDashboardSectionOrder"
---

<objective>
Close the remaining Phase 08 verification gap by restoring a green production build at current HEAD.

Purpose: remove the static generation worker crash (`Next.js build worker exited with code: 1 and signal: null`) without widening scope beyond the build failure seam.
Output: build-path remediation in Next.js/dashboard server rendering plus regression checks that keep today/trends behavior stable.
</objective>

<execution_context>
@/home/flo/.codex/get-shit-done/workflows/execute-plan.md
@/home/flo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-release-blockers-and-regression-restoration/08-VERIFICATION.md
@.planning/phases/08-release-blockers-and-regression-restoration/08-05-SUMMARY.md
@next.config.ts
@src/app/(private)/dashboard/page.tsx
@src/app/api/program/today/route.ts
@src/app/api/program/trends/route.ts
@tests/program/dashboard-today-surface.test.ts
@tests/program/dashboard-trends-surface.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture deterministic build-crash evidence and isolate the failing static-generation seam</name>
  <files>next.config.ts, src/app/(private)/dashboard/page.tsx</files>
  <action>Reproduce the failure with both `corepack pnpm build` and `corepack pnpm next build --webpack`, capture stderr, and identify the smallest code/config seam that triggers the static-generation worker exit. Keep scope strictly on this crash diagnosis; do not start unrelated cleanup (middleware deprecation, broad refactors, or non-blocking warnings).</action>
  <verify>
    <automated>corepack pnpm build</automated>
  </verify>
  <done>The executor has a concrete, file-level crash seam to patch (not a generic "build failed" state).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Patch only the isolated build seam in Next config/dashboard server data loading</name>
  <files>next.config.ts, src/app/(private)/dashboard/page.tsx, tests/program/dashboard-today-surface.test.ts, tests/program/dashboard-trends-surface.test.ts</files>
  <behavior>
    - Test 1: dashboard today-session projection remains unchanged for today, next-session fallback, and empty-session states.
    - Test 2: dashboard trends loading still returns parsed data on success and `null` on non-OK responses.
    - Test 3: the production build no longer crashes during static generation.
  </behavior>
  <action>Apply the minimal fix identified in Task 1. Preferred order: (1) pin Next build root/workspace inference in `next.config.ts` when root drift is contributing to worker instability, then (2) remove the crash-prone dashboard server self-fetch seam by keeping data loading inside stable server boundaries while preserving existing response parsing and auth-route behavior. Avoid redesigning dashboard UX or changing API contracts.</action>
  <verify>
    <automated>corepack pnpm test -- tests/program/dashboard-today-surface.test.ts tests/program/dashboard-trends-surface.test.ts</automated>
  </verify>
  <done>Crash seam is remediated with targeted regression coverage and no contract drift on dashboard today/trends behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Re-run release gates after the build crash fix</name>
  <files>next.config.ts, src/app/(private)/dashboard/page.tsx</files>
  <action>After the fix is in place, run the release gates in order: build first (gap closure target), then typecheck, then full tests. If any non-build failure appears, record it explicitly in the summary as outside this gap-closure scope unless it is a direct consequence of the crash fix.</action>
  <verify>
    <automated>corepack pnpm build && corepack pnpm typecheck && corepack pnpm test</automated>
  </verify>
  <done>All three gates are green and Phase 08 verification no longer reports the static-generation worker crash gap.</done>
</task>

</tasks>

<verification>
Confirm the static-generation worker crash is removed, dashboard server data loading still matches existing today/trends contracts, and the repo-level build/typecheck/test gates are green.
</verification>

<success_criteria>
- `corepack pnpm build` exits `0` on current HEAD.
- `corepack pnpm typecheck` exits `0`.
- `corepack pnpm test` exits `0`.
- No new dashboard contract regressions in `dashboard-today-surface` and `dashboard-trends-surface` tests.
</success_criteria>

<output>
After completion, create `.planning/phases/08-release-blockers-and-regression-restoration/08-06-SUMMARY.md`.
</output>
