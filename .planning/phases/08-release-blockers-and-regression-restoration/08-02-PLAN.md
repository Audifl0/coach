---
phase: 08-release-blockers-and-regression-restoration
plan: 08-02
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - src/app/api/program/today/route.ts
  - src/app/(private)/dashboard/page.tsx
  - src/lib/program/contracts.ts
  - src/lib/program/select-today-session.ts
  - src/lib/profile/completeness.ts
  - src/server/services/program-generation.ts
  - src/server/services/adaptive-coaching.ts
  - src/server/llm/client.ts
  - src/server/llm/providers/openai-client.ts
  - src/server/llm/providers/anthropic-client.ts
  - tests/program/dashboard-today-surface.test.ts
  - tests/program/dashboard-adaptive-forecast.test.ts
  - tests/program/adaptive-coaching-provider-config.test.ts
  - tests/program/program-session-logging-route.test.ts
autonomous: true
gap_closure: true
requirements:
  - ADAP-01
  - ADAP-02
  - ADAP-03
  - SAFE-03
  - PLAT-01
must_haves:
  truths:
    - "RB-01 wave 2 is limited to the bounded post-build typecheck clusters identified in `08-RESEARCH.md`: dashboard/program contracts, profile completeness typing, provider/LLM adapter typing, and any test fixtures broken by those shared contract repairs."
    - "Shared contracts must be repaired at the source instead of papering over errors with new casts or route-local literal unions."
    - "This wave is complete only when `corepack pnpm typecheck` is green and the affected regression suites remain aligned with the repaired contracts."
  artifacts:
    - path: src/lib/program/contracts.ts
      provides: "Shared program/dashboard response contracts that route and UI code both consume."
    - path: src/lib/profile/completeness.ts
      provides: "Profile completeness guard that accepts the minimal validated shape it actually reads."
      exports:
        - isProfileComplete
    - path: src/server/llm/client.ts
      provides: "Provider-attempt typing that narrows retry/fallback metadata safely under current SDK types."
      exports:
        - createLlmProposalClient
  key_links:
    - from: src/app/api/program/today/route.ts
      to: src/lib/program/contracts.ts
      via: "today-route payload stays aligned with shared session/dashboard contracts"
      pattern: "SessionState|ProgramSession"
    - from: src/app/(private)/dashboard/page.tsx
      to: src/lib/profile/completeness.ts
      via: "dashboard profile gating uses the generalized completeness helper without cast-based suppression"
      pattern: "isProfileComplete"
    - from: src/server/services/adaptive-coaching.ts
      to: src/server/llm/client.ts
      via: "adaptive runtime consumes provider client outputs through narrowed shared LLM attempt types"
      pattern: "createLlmProposalClient|fallbackReason"
---

<objective>
Finish RB-01 by restoring a clean typecheck baseline across the remaining bounded source clusters that survive the build unblock.

Purpose: return the repo to trustworthy compile health without broadening phase 08 into general cleanup.
Output: repaired shared contracts and typings plus updated focused regression fixtures for the affected dashboard/program/profile/provider surfaces.
</objective>

<execution_context>
@/home/flo/.codex/get-shit-done/workflows/execute-plan.md
@/home/flo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/08-release-blockers-and-regression-restoration/08-CONTEXT.md
@.planning/phases/08-release-blockers-and-regression-restoration/08-RESEARCH.md
@src/app/api/program/today/route.ts
@src/app/(private)/dashboard/page.tsx
@src/app/(private)/dashboard/_components/today-workout-card.tsx
@src/lib/program/contracts.ts
@src/lib/program/types.ts
@src/lib/profile/completeness.ts
@src/server/services/adaptive-coaching.ts
@src/server/llm/client.ts
@src/server/llm/config.ts
@tests/program/dashboard-today-surface.test.ts
@tests/program/dashboard-adaptive-forecast.test.ts
@tests/program/adaptive-coaching-provider-config.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Repair dashboard and today-route contract drift at the shared type boundary</name>
  <files>src/app/api/program/today/route.ts, src/app/(private)/dashboard/page.tsx, src/lib/program/contracts.ts, src/lib/program/select-today-session.ts, tests/program/dashboard-today-surface.test.ts, tests/program/dashboard-adaptive-forecast.test.ts</files>
  <behavior>
    - Test 1: the today-workout/dashboard surface accepts the current shared session-state contract, including any `started` state that local literals had drifted away from.
    - Test 2: pending-confirmation banner data remains statically narrowed and does not widen action types to `string`.
    - Test 3: nullable today/next-session handling stays explicit instead of being suppressed with casts.
  </behavior>
  <action>Repair the shared program/dashboard contract drift at the source. Replace route-local or component-local literals with exported shared session types, tighten any banner projection helpers to return the shared contract directly, and fix nullable session handling explicitly instead of suppressing it.</action>
  <verify>
    <automated>corepack pnpm test -- tests/program/dashboard-today-surface.test.ts tests/program/dashboard-adaptive-forecast.test.ts</automated>
  </verify>
  <done>Dashboard and today-route code compile against one shared program contract without local drift.</done>
</task>

<task type="auto">
  <name>Task 2: Generalize profile completeness typing without changing profile behavior</name>
  <files>src/lib/profile/completeness.ts, src/server/services/program-generation.ts, src/app/(private)/dashboard/page.tsx</files>
  <action>Update `isProfileComplete(...)` to accept the minimal validated profile shape it actually reads, then remove the existing cast-based suppressions at dashboard and program-generation call sites. Keep all runtime profile completeness rules unchanged.</action>
  <verify>
    <automated>corepack pnpm test -- tests/program/dashboard-today-surface.test.ts</automated>
  </verify>
  <done>Profile completeness checks compile cleanly without `as never`-style call-site workarounds.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Reconcile provider and adaptive typing drift, then fix only the fallout fixtures</name>
  <files>src/server/services/adaptive-coaching.ts, src/server/llm/client.ts, src/server/llm/providers/openai-client.ts, src/server/llm/providers/anthropic-client.ts, tests/program/adaptive-coaching-provider-config.test.ts, tests/program/program-session-logging-route.test.ts</files>
  <behavior>
    - Test 1: provider config fixtures remain valid under the current `parseLlmRuntimeConfig(...)` boundary.
    - Test 2: LLM attempt/result typing narrows fallback metadata safely under the current SDK response shapes.
    - Test 3: any remaining test fixture repairs are limited to the shared-contract fallout from the source fixes above.
  </behavior>
  <action>Fix the bounded provider/adaptive typing drift called out in `08-RESEARCH.md`: narrow `LlmAttemptResult` before reading fallback metadata, normalize nullable request-id handling to the SDK surface, pass mutable schema arrays where SDKs require them, and keep provider-config fixtures realistic at the tested boundary. Only after those source fixes land, update the small set of tests that drifted because the shared contracts tightened.</action>
  <verify>
    <automated>corepack pnpm test -- tests/program/adaptive-coaching-provider-config.test.ts tests/program/program-session-logging-route.test.ts</automated>
  </verify>
  <done>The remaining RB-01 typing clusters are repaired without broad refactors or new type suppressions.</done>
</task>

<task type="auto">
  <name>Task 4: Close RB-01 with the repo-wide typecheck gate</name>
  <files>src/app/api/program/today/route.ts, src/lib/profile/completeness.ts, src/server/llm/client.ts</files>
  <action>Run the full repo typecheck only after the bounded source clusters above are repaired. If new failures appear outside the documented RB-01 clusters, stop and capture them instead of silently broadening this wave.</action>
  <verify>
    <automated>corepack pnpm typecheck</automated>
  </verify>
  <done>`corepack pnpm typecheck` exits `0` and RB-01 is fully restored.</done>
</task>

</tasks>

<verification>
Confirm the dashboard/program/profile/provider clusters compile cleanly, the affected regression suites pass, and `corepack pnpm typecheck` is green without widening phase scope.
</verification>

<success_criteria>
- `corepack pnpm typecheck` exits `0`.
- Shared dashboard/program contracts are the source of truth instead of route-local or component-local drift.
- Profile completeness and provider adapter typings compile cleanly without new cast-based suppressions.
</success_criteria>

<output>
After completion, create `.planning/phases/08-release-blockers-and-regression-restoration/08-02-SUMMARY.md`.
</output>
