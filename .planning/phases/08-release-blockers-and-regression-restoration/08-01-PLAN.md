---
phase: 08-release-blockers-and-regression-restoration
plan: 08-01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/auth/session-contract.ts
  - src/lib/auth/auth.ts
  - src/middleware.ts
autonomous: true
gap_closure: true
requirements:
  - AUTH-02
  - PLAT-01
must_haves:
  truths:
    - "Users can still sign in and stay signed in, while dashboard access no longer breaks because of Edge-runtime auth import errors."
    - "Private-route requests from authenticated users keep the same session-cookie behavior after the middleware/auth import split."
    - "The release build gate for this wave is observable as a green `corepack pnpm build` on the same application behavior."
  artifacts:
    - path: src/lib/auth/session-contract.ts
      provides: "Edge-safe auth session constants that can be imported from middleware without dragging Node-only auth code."
      exports:
        - SESSION_COOKIE_NAME
    - path: src/middleware.ts
      provides: "Middleware auth gate that depends only on the Edge-safe session contract."
      exports:
        - middleware
    - path: src/lib/auth/auth.ts
      provides: "Node-only auth service/runtime helpers that no longer leak into the Edge import path."
      exports:
        - createAuthService
        - hashSessionToken
  key_links:
    - from: src/middleware.ts
      to: src/lib/auth/session-contract.ts
      via: "middleware reads session cookie naming from an Edge-safe contract module"
      pattern: "SESSION_COOKIE_NAME"
    - from: src/lib/auth/auth.ts
      to: src/lib/auth/session-contract.ts
      via: "shared cookie constants remain centralized without importing Node-only password/session helpers into middleware"
      pattern: "SESSION_COOKIE_NAME"
---

<objective>
Restore the first release gate for RB-01 by removing the Edge-runtime auth import contamination and proving the production build is trustworthy again.

Purpose: unblock `corepack pnpm build` before broader typecheck repair so later remediation runs against a valid production build path.
Output: refreshed Prisma client state plus an Edge-safe auth constant split that keeps middleware out of the Node-only auth graph.
</objective>

<execution_context>
@/home/flo/.codex/get-shit-done/workflows/execute-plan.md
@/home/flo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-release-blockers-and-regression-restoration/08-CONTEXT.md
@.planning/phases/08-release-blockers-and-regression-restoration/08-RESEARCH.md
@src/middleware.ts
@src/lib/auth/auth.ts
@src/lib/auth/password.ts
@tests/auth/session-lifecycle.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refresh generated Prisma client state before touching RB-01 source fixes</name>
  <files>src/middleware.ts, src/lib/auth/auth.ts, src/lib/auth/password.ts</files>
  <action>Run `corepack pnpm prisma:generate` first, then re-check the remaining RB-01 failure clusters described in `08-RESEARCH.md`. Treat this as preflight only: do not expand scope into unrelated Prisma or DAL cleanup unless the stale generated-client error persists after regeneration.</action>
  <verify>
    <automated>corepack pnpm prisma:generate</automated>
  </verify>
  <done>Generated client state is refreshed so the remaining RB-01 work targets real source errors.</done>
</task>

<task type="auto">
  <name>Task 2: Split middleware-safe auth constants from Node-only auth runtime code</name>
  <files>src/lib/auth/session-contract.ts, src/lib/auth/auth.ts, src/middleware.ts</files>
  <action>Create a tiny Edge-safe auth contract module for `SESSION_COOKIE_NAME` and move middleware imports onto that seam. Keep `hashSessionToken`, password helpers, and any `node:crypto` usage behind the Node-only auth runtime boundary; do not redesign authentication behavior in this wave.</action>
  <verify>
    <automated>corepack pnpm build</automated>
  </verify>
  <done>`corepack pnpm build` succeeds without Edge-runtime errors from the auth import graph.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Re-run focused auth regression coverage after the import-graph fix</name>
  <files>tests/auth/session-lifecycle.test.ts</files>
  <behavior>
    - Test 1: login still issues the expected session cookie name and attributes.
    - Test 2: persisted-session and logout behavior remain unchanged after the constant split.
  </behavior>
  <action>Run the existing auth lifecycle suite after the middleware/auth seam change. Only add or update tests if the current assertions do not cover the cookie contract affected by the split.</action>
  <verify>
    <automated>corepack pnpm test -- tests/auth/session-lifecycle.test.ts</automated>
  </verify>
  <done>The build unblock does not regress session issuance or current-session logout behavior.</done>
</task>

</tasks>

<verification>
Confirm Prisma preflight succeeded, middleware no longer imports Node-only auth code, and `corepack pnpm build` is green with focused auth regressions still passing.
</verification>

<success_criteria>
- `corepack pnpm build` exits `0` after the auth seam fix.
- `src/middleware.ts` depends only on an Edge-safe session contract module.
- Auth session lifecycle behavior remains unchanged under targeted tests.
</success_criteria>

<output>
After completion, create `.planning/phases/08-release-blockers-and-regression-restoration/08-01-SUMMARY.md`.
</output>
