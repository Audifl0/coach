---
phase: 08-release-blockers-and-regression-restoration
plan: 08-05
type: execute
wave: 5
depends_on:
  - 08-04
files_modified:
  - src/server/services/adaptive-coaching.ts
  - src/lib/adaptive-coaching/orchestrator.ts
  - src/lib/adaptive-coaching/evidence.ts
  - src/lib/adaptive-coaching/evidence-corpus.ts
  - tests/program/adaptive-coaching-confirm-route.test.ts
  - tests/program/adaptive-coaching-service.test.ts
autonomous: true
gap_closure: true
requirements:
  - ADAP-01
  - ADAP-02
  - ADAP-03
  - SAFE-03
must_haves:
  truths:
    - "All adaptive proposal inputs, including the deterministic local proposal path, are sanitized before strict contract parsing so transport-only metadata cannot force a false SAFE-03 fallback."
    - "High-impact `deload` and `substitution` recommendations continue to land in `pending_confirmation` until the user explicitly confirms them."
    - "Deterministic evidence retrieval returns exactly `topK` references from the active runtime corpus when enough entries exist, filling remaining slots from the same corpus before any built-in fallback corpus is considered."
  artifacts:
    - path: src/server/services/adaptive-coaching.ts
      provides: "Adaptive generation/confirmation service with restored pending-confirmation lifecycle behavior."
      exports:
        - createAdaptiveCoachingService
    - path: src/lib/adaptive-coaching/evidence.ts
      provides: "Deterministic evidence retrieval that maintains the requested top-k contract against the active runtime corpus."
      exports:
        - retrieveAdaptiveEvidence
    - path: tests/program/adaptive-coaching-confirm-route.test.ts
      provides: "Regression coverage for confirmation-required recommendations staying in `pending_confirmation` until user action."
  key_links:
    - from: src/server/services/adaptive-coaching.ts
      to: src/lib/adaptive-coaching/orchestrator.ts
      via: "adaptive service sanitizes proposal input before strict parsing and status assignment"
      pattern: "modelConfidence|pending_confirmation"
    - from: src/lib/adaptive-coaching/evidence.ts
      to: src/lib/adaptive-coaching/evidence-corpus.ts
      via: "retrieval fills the requested top-k from the active corpus using deterministic ordering"
      pattern: "topK|active corpus"
    - from: tests/program/adaptive-coaching-service.test.ts
      to: src/lib/adaptive-coaching/evidence.ts
      via: "service regression tests assert the deterministic evidence-count contract"
      pattern: "top-k|3"
---

<objective>
Close RB-04 by restoring the two audited adaptive regression contracts after compile, build, and auth/security blockers are already green.

Purpose: re-establish trustworthy adaptive lifecycle behavior and deterministic evidence retrieval on the safety-sensitive recommendation surface.
Output: repaired adaptive proposal sanitization and evidence top-k behavior plus focused adaptive regression coverage and the final phase test gate.
</objective>

<execution_context>
@/home/flo/.codex/get-shit-done/workflows/execute-plan.md
@/home/flo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/08-release-blockers-and-regression-restoration/08-CONTEXT.md
@.planning/phases/08-release-blockers-and-regression-restoration/08-RESEARCH.md
@src/server/services/adaptive-coaching.ts
@src/lib/adaptive-coaching/orchestrator.ts
@src/lib/adaptive-coaching/evidence.ts
@src/lib/adaptive-coaching/evidence-corpus.ts
@tests/program/adaptive-coaching-confirm-route.test.ts
@tests/program/adaptive-coaching-service.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Restore pending-confirmation lifecycle behavior for high-impact adaptive recommendations</name>
  <files>src/server/services/adaptive-coaching.ts, src/lib/adaptive-coaching/orchestrator.ts, tests/program/adaptive-coaching-confirm-route.test.ts</files>
  <behavior>
    - Test 1: deterministic local proposals that recommend `deload` or `substitution` are stored as `pending_confirmation`, not `fallback_applied`.
    - Test 2: provider and non-provider proposal paths both strip transport-only fields before strict parsing.
    - Test 3: confirm/reject flows keep the existing expiration, ownership, and next-session checks intact.
  </behavior>
  <action>Sanitize every proposal input before strict adaptive contract parsing, not only provider proposals. Keep `modelConfidence` and any other transport metadata outside the parsed proposal contract, and preserve the existing rule that `deload` and `substitution` require explicit confirmation before application.</action>
  <verify>
    <automated>corepack pnpm test -- tests/program/adaptive-coaching-confirm-route.test.ts</automated>
  </verify>
  <done>High-impact adaptive recommendations reliably stay in `pending_confirmation` until the user acts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Restore deterministic evidence top-k behavior against the active runtime corpus</name>
  <files>src/lib/adaptive-coaching/evidence.ts, src/lib/adaptive-coaching/evidence-corpus.ts, tests/program/adaptive-coaching-service.test.ts</files>
  <behavior>
    - Test 1: `retrieveAdaptiveEvidence(..., topK: 3)` returns exactly three deterministic references when the active runtime corpus has at least three usable entries.
    - Test 2: when direct query-tag overlap yields fewer than `topK` hits, retrieval fills the remainder from the same active corpus in deterministic order before considering built-in fallback corpus data.
    - Test 3: empty or unusable runtime corpus still falls back conservatively without reopening the knowledge-pipeline scope.
  </behavior>
  <action>Change evidence retrieval to a deterministic two-pass strategy: rank direct matches first using the current score/source-priority rules, then fill any remaining slots from the same active corpus in a stable order. Keep fallback to the built-in default corpus only for empty or unusable runtime corpus cases.</action>
  <verify>
    <automated>corepack pnpm test -- tests/program/adaptive-coaching-service.test.ts</automated>
  </verify>
  <done>The adaptive evidence layer honors the current deterministic top-k contract against the runtime corpus.</done>
</task>

<task type="auto">
  <name>Task 3: Run the final adaptive and repo test gates for phase 08</name>
  <files>tests/program/adaptive-coaching-confirm-route.test.ts, tests/program/adaptive-coaching-service.test.ts</files>
  <action>After the two audited adaptive regressions are closed, run the focused adaptive suites first and then the full repo test gate. If failures remain outside RB-04, capture them explicitly instead of silently widening the phase scope.</action>
  <verify>
    <automated>corepack pnpm test</automated>
  </verify>
  <done>`corepack pnpm test` exits `0` and the final RB-04 validation gate is restored.</done>
</task>

</tasks>

<verification>
Confirm the adaptive confirmation lifecycle is back in contract, deterministic evidence retrieval returns the requested top-k references, and the full test suite is green.
</verification>

<success_criteria>
- `tests/program/adaptive-coaching-confirm-route.test.ts` passes with `pending_confirmation` behavior restored.
- `tests/program/adaptive-coaching-service.test.ts` passes with deterministic evidence top-k behavior restored.
- `corepack pnpm test` exits `0`.
</success_criteria>

<output>
After completion, create `.planning/phases/08-release-blockers-and-regression-restoration/08-05-SUMMARY.md`.
</output>
