---
phase: 08-release-blockers-and-regression-restoration
plan: 08-04
type: execute
wave: 4
depends_on:
  - 08-03
files_modified:
  - src/lib/auth/client-ip.ts
  - src/lib/auth/rate-limit.ts
  - src/lib/auth/auth-logger.ts
  - src/app/api/auth/login/route.ts
  - src/app/api/auth/signup/route.ts
  - tests/auth/auth-rate-limit.test.ts
  - tests/auth/session-lifecycle.test.ts
autonomous: true
gap_closure: true
requirements:
  - AUTH-01
  - AUTH-02
must_haves:
  truths:
    - "Brute-force protection is scoped only to the public login and signup routes in this phase; it must not turn into a generic abuse platform."
    - "Repeated login failures are throttled by normalized username plus client IP, repeated signup attempts are throttled by client IP, and limit hits return `429` with `Retry-After`."
    - "Operator-visible structured log events exist for throttle enforcement and repeated auth failures while normal `400|401|409` semantics remain unchanged below the threshold."
  artifacts:
    - path: src/lib/auth/rate-limit.ts
      provides: "Focused auth-route limiter primitives that can be injected into login/signup handlers for deterministic tests."
    - path: src/lib/auth/client-ip.ts
      provides: "Client identity extraction from forwarded headers supplied by the reverse proxy."
      exports:
        - resolveClientIp
    - path: tests/auth/auth-rate-limit.test.ts
      provides: "Targeted regression coverage for login/signup throttling, Retry-After headers, and structured log emission."
  key_links:
    - from: src/app/api/auth/login/route.ts
      to: src/lib/auth/rate-limit.ts
      via: "login handler uses the focused limiter before returning success or generic invalid-credentials responses"
      pattern: "429|Retry-After"
    - from: src/app/api/auth/signup/route.ts
      to: src/lib/auth/client-ip.ts
      via: "signup throttling keys abuse checks from forwarded client identity"
      pattern: "X-Forwarded-For|X-Real-IP"
    - from: src/app/api/auth/login/route.ts
      to: src/lib/auth/auth-logger.ts
      via: "throttle and repeated-failure events are emitted in an operator-visible structured format"
      pattern: "auth_throttle|auth_failure"
---

<objective>
Close RB-03 by adding runtime-enforced brute-force protection to the public auth boundary with minimal operator visibility.

Purpose: make login/signup abuse measurably enforceable before public exposure without broadening phase 08 into a full abuse-detection platform.
Output: focused auth limiter modules, route wiring, and route-level regression coverage for throttling behavior.
</objective>

<execution_context>
@/home/flo/.codex/get-shit-done/workflows/execute-plan.md
@/home/flo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-release-blockers-and-regression-restoration/08-CONTEXT.md
@.planning/phases/08-release-blockers-and-regression-restoration/08-RESEARCH.md
@src/app/api/auth/login/route.ts
@src/app/api/auth/signup/route.ts
@src/lib/auth/auth.ts
@tests/auth/session-lifecycle.test.ts
@infra/caddy/Caddyfile
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add focused auth limiter and client-identity helpers with deterministic tests</name>
  <files>src/lib/auth/client-ip.ts, src/lib/auth/rate-limit.ts, tests/auth/auth-rate-limit.test.ts</files>
  <behavior>
    - Test 1: login throttling keys repeated failures by normalized username plus client IP.
    - Test 2: signup throttling keys repeated attempts by client IP.
    - Test 3: limit enforcement returns a retry window that handlers can surface through `Retry-After`.
  </behavior>
  <action>Create a small, process-local limiter module and a forwarded-header client IP helper under `src/lib/auth`. Keep the API injectable so route handlers can be tested without proxy or database setup, and avoid any generic rate-limit framework work in this phase.</action>
  <verify>
    <automated>corepack pnpm test -- tests/auth/auth-rate-limit.test.ts</automated>
  </verify>
  <done>Focused auth throttling primitives exist and are covered by deterministic unit tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire login and signup handlers to enforce throttling without regressing existing auth semantics</name>
  <files>src/lib/auth/auth-logger.ts, src/app/api/auth/login/route.ts, src/app/api/auth/signup/route.ts, tests/auth/auth-rate-limit.test.ts, tests/auth/session-lifecycle.test.ts</files>
  <behavior>
    - Test 1: repeated failed login attempts return `429` with `Retry-After` after the configured threshold.
    - Test 2: repeated signup attempts return `429` after the configured threshold.
    - Test 3: sub-threshold failures still return the current generic `401` or `409` behavior, and successful login bypasses or clears the failure bucket.
    - Test 4: throttle events emit structured operator-visible log records.
  </behavior>
  <action>Inject limiter and logging dependencies into `createLoginHandler(...)` and `createSignupHandler(...)`, resolve client identity from forwarded headers, and enforce throttling only on the public auth routes. Preserve the existing generic invalid-credentials behavior for ordinary login failures and avoid Caddy/plugin changes unless route-layer enforcement proves impossible.</action>
  <verify>
    <automated>corepack pnpm test -- tests/auth/auth-rate-limit.test.ts tests/auth/session-lifecycle.test.ts</automated>
  </verify>
  <done>Public auth routes throttle repeated abuse while preserving current auth behavior for legitimate traffic and sub-threshold failures.</done>
</task>

<task type="auto">
  <name>Task 3: Run focused manual abuse proof against the auth endpoints</name>
  <files>src/app/api/auth/login/route.ts, src/app/api/auth/signup/route.ts</files>
  <action>Use repeated requests with a fixed forwarded client IP to prove `/api/auth/login` and `/api/auth/signup` return `429` after the configured threshold, then capture the expected log event names and retry-window behavior in the summary for operator reference.</action>
  <verify>
    <automated>bash -lc 'set -euo pipefail; BASE_URL="${BASE_URL:-http://127.0.0.1:3000}"; AUTH_LOG_FILE="${AUTH_LOG_FILE:-/tmp/coach-auth.log}"; : > /tmp/coach-auth-runtime-proof.log; for i in 1 2 3 4 5 6; do curl -sS -D - -o /dev/null -H "content-type: application/json" -H "x-forwarded-for: 203.0.113.10" -X POST "$BASE_URL/api/auth/login" --data "{\"username\":\"rb03-proof\",\"password\":\"wrong-password\"}" | tee -a /tmp/coach-auth-runtime-proof.log >/dev/null; done; for i in 1 2 3 4; do curl -sS -D - -o /dev/null -H "content-type: application/json" -H "x-forwarded-for: 203.0.113.10" -X POST "$BASE_URL/api/auth/signup" --data "{\"username\":\"rb03-proof-$i\",\"password\":\"password123\"}" | tee -a /tmp/coach-auth-runtime-proof.log >/dev/null; done; rg -n "HTTP/.* 429|[Rr]etry-[Aa]fter" /tmp/coach-auth-runtime-proof.log; rg -n "auth_failure|auth_throttle" "$AUTH_LOG_FILE"'</automated>
  </verify>
  <done>The phase has route-level proof and operator-facing evidence that public auth abuse is no longer unchecked.</done>
</task>

</tasks>

<verification>
Confirm repeated auth abuse is throttled on the public login/signup routes, responses include `429` plus `Retry-After`, and operators can see the throttle events.
</verification>

<success_criteria>
- Repeated failed login attempts are blocked after a bounded threshold.
- Repeated signup attempts are blocked after a bounded threshold.
- Existing auth success and ordinary failure behavior remain stable below the threshold.
</success_criteria>

<output>
After completion, create `.planning/phases/08-release-blockers-and-regression-restoration/08-04-SUMMARY.md`.
</output>
