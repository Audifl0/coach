---
phase: 09-security-runtime-and-release-proof-stabilization
plan: 09-04
type: execute
wave: 3
depends_on:
  - 09-01
  - 09-02
files_modified:
  - src/server/dal/program.ts
  - src/app/api/program/sessions/[sessionId]/route.ts
  - src/app/(private)/dashboard/_components/session-logger.tsx
  - src/app/(private)/dashboard/_components/today-workout-card.tsx
  - src/lib/program/select-today-session.ts
  - tests/program/program-dal.test.ts
  - tests/program/dashboard-today-surface.test.ts
  - tests/program/session-history-surface.test.ts
  - tests/program/program-session-logging-route.test.ts
autonomous: true
requirements:
  - PROG-02
  - LOG-01
  - LOG-04
  - DASH-01
must_haves:
  truths:
    - "The functional drift fixes stay narrowly scoped to AUD-09: UTC-safe today/next selection, archived-session drilldown parity, and deterministic workout-resume hydration after refresh or reopen."
    - "Current product rules remain intact unless AUD-09 requires correction: today still wins over next-session fallback, history remains account-scoped, and resume behavior rehydrates existing in-progress state instead of inventing new client-only workflow."
    - "Every corrected flow is backed by targeted regressions rather than relying on incidental coverage from broader program/dashboard tests."
  artifacts:
    - path: src/server/dal/program.ts
      provides: "UTC-safe today/next lookup and session-detail reads that match history/list expectations."
      exports:
        - getTodayOrNextSessionCandidates
        - getSessionById
    - path: src/app/api/program/sessions/[sessionId]/route.ts
      provides: "Session detail route that can serve archived completed sessions without breaking ownership masking."
      exports:
        - GET
    - path: src/app/(private)/dashboard/_components/session-logger.tsx
      provides: "Client resume/hydration behavior that restores existing in-progress workout state after refresh."
  key_links:
    - from: src/server/dal/program.ts
      to: src/lib/program/select-today-session.ts
      via: "UTC-safe candidate selection feeds the unchanged today/next projection contract"
      pattern: "getTodayOrNextSessionCandidates|selectTodayWorkoutProjection"
    - from: src/app/api/program/sessions/[sessionId]/route.ts
      to: src/server/dal/program.ts
      via: "history drilldown detail reuses account-scoped DAL access that now includes archived completed sessions"
      pattern: "getSessionById"
    - from: src/app/(private)/dashboard/_components/today-workout-card.tsx
      to: src/app/(private)/dashboard/_components/session-logger.tsx
      via: "today workout surface rehydrates saved in-progress session data after refresh/reopen"
      pattern: "startedAt|completedAt|logger"
---

<objective>
Execute `STAB-04` once dashboard trust and mutation consistency are stable by fixing the remaining high-impact flow drift from AUD-09.

Purpose: make dashboard today/history/resume behavior deterministic without redesigning the workout experience.
Output: UTC-safe selection logic, archived history drilldown parity, workout-resume hydration fixes, and targeted regressions.
</objective>

<execution_context>
@/home/flo/.codex/get-shit-done/workflows/execute-plan.md
@/home/flo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/09-security-runtime-and-release-proof-stabilization/09-CONTEXT.md
@.planning/phases/09-security-runtime-and-release-proof-stabilization/09-RESEARCH.md
@src/server/dal/program.ts
@src/app/api/program/sessions/[sessionId]/route.ts
@src/app/(private)/dashboard/_components/session-logger.tsx
@src/app/(private)/dashboard/_components/today-workout-card.tsx
@tests/program/program-dal.test.ts
@tests/program/session-history-surface.test.ts
@tests/program/program-session-logging-route.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix UTC-boundary today and next-session selection</name>
  <files>src/server/dal/program.ts, src/lib/program/select-today-session.ts, tests/program/program-dal.test.ts, tests/program/dashboard-today-surface.test.ts</files>
  <behavior>
    - Test 1: Sessions scheduled at the first instant of the next UTC day are included in next-session fallback instead of being skipped.
    - Test 2: Today selection uses the same UTC-day boundary semantics already used by trends/history helpers.
    - Test 3: Existing today-first behavior and projection contract remain unchanged for callers.
  </behavior>
  <action>Write the failing boundary regressions first, then rebase `getTodayOrNextSessionCandidates(...)` onto the existing UTC helper semantics already present in the DAL. Keep the route and projection contracts stable; only correct the date-boundary behavior required by AUD-09.</action>
  <verify>
    <automated>pnpm test tests/program/program-dal.test.ts tests/program/dashboard-today-surface.test.ts</automated>
  </verify>
  <done>Today/next workout selection is deterministic across midnight UTC boundaries.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Align history drilldown detail with archived completed sessions</name>
  <files>src/server/dal/program.ts, src/app/api/program/sessions/[sessionId]/route.ts, tests/program/session-history-surface.test.ts, tests/program/program-session-logging-route.test.ts</files>
  <behavior>
    - Test 1: Session detail route returns completed archived sessions that already appear in the history list.
    - Test 2: Cross-account access stays masked as not found.
    - Test 3: Existing detail payload fields and set ordering remain unchanged for active-plan sessions.
  </behavior>
  <action>Extend the DAL/session-detail route only as far as needed to serve archived completed sessions through the same projection contract. Preserve ownership masking and avoid introducing a separate history-detail endpoint.</action>
  <verify>
    <automated>pnpm test tests/program/session-history-surface.test.ts tests/program/program-session-logging-route.test.ts</automated>
  </verify>
  <done>History list and drilldown detail now describe the same set of completed sessions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Restore deterministic workout-resume hydration after refresh or reopen</name>
  <files>src/app/(private)/dashboard/_components/session-logger.tsx, src/app/(private)/dashboard/_components/today-workout-card.tsx, tests/program/dashboard-today-surface.test.ts, tests/program/program-session-logging-route.test.ts</files>
  <behavior>
    - Test 1: Reloading a started-but-not-completed session restores the saved timer/session state instead of presenting a fresh idle workout.
    - Test 2: Completed sessions do not rehydrate as resumable.
    - Test 3: Existing completion payload and duration-correction behavior stay unchanged.
  </behavior>
  <action>Use already-available server session detail/state to rehydrate the in-progress logger surface after refresh. Keep the current UI flow and completion semantics intact; only close the missing rehydration gap identified in AUD-09.</action>
  <verify>
    <automated>pnpm test tests/program/dashboard-today-surface.test.ts tests/program/program-session-logging-route.test.ts</automated>
  </verify>
  <done>Refreshing or reopening an in-progress workout returns the user to the correct session state deterministically.</done>
</task>

</tasks>

<verification>
Run the targeted today/history/session-logging regressions to prove the midnight boundary, archived drilldown, and refresh/reopen resume flows all behave deterministically.
</verification>

<success_criteria>
- Next-session fallback includes the first session on the next UTC day.
- History drilldown can open archived completed sessions already shown in history lists.
- Started workout state survives refresh/reopen without incorrectly reviving completed sessions.
</success_criteria>

<output>
After completion, create `.planning/phases/09-security-runtime-and-release-proof-stabilization/09-04-SUMMARY.md`.
</output>
