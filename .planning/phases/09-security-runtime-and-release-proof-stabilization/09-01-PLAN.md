---
phase: 09-security-runtime-and-release-proof-stabilization
plan: 09-01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(private)/dashboard/page.tsx
  - src/app/(private)/dashboard/_components/today-workout-card.tsx
  - src/app/(private)/dashboard/_components/trends-summary-card.tsx
  - src/lib/program/select-today-session.ts
  - src/server/dashboard/program-dashboard.ts
  - tests/program/dashboard-page-data.test.ts
  - tests/program/dashboard-today-surface.test.ts
  - tests/program/dashboard-trends-surface.test.ts
autonomous: true
requirements:
  - DASH-01
  - DASH-02
must_haves:
  truths:
    - "After phase 08 restores the green compile and auth-abuse baseline, dashboard SSR no longer derives a same-service origin from request headers or forwards cookies into internal HTTP fetches for today/trends data."
    - "Dashboard program sections expose explicit `ready`, `empty`, and `error` states so route/runtime failures cannot masquerade as a legitimate no-workout outcome or silently hide the trends block."
    - "Current business behavior stays intact unless required by AUD-05: today remains higher priority than next-session fallback, API routes stay available for client consumers, and section ordering stays today -> adaptive -> trends -> history."
  artifacts:
    - path: src/server/dashboard/program-dashboard.ts
      provides: "Server-only dashboard data composition for today and trends section states without same-service HTTP fetch hops."
      exports:
        - loadDashboardProgramTodaySection
        - loadDashboardTrendsSection
    - path: src/app/(private)/dashboard/page.tsx
      provides: "Dashboard page wiring that consumes explicit section states instead of nullable internal fetch results."
      exports:
        - pickDashboardSession
        - resolveDashboardSectionOrder
    - path: tests/program/dashboard-page-data.test.ts
      provides: "Regression coverage for direct server composition and explicit degraded-state handling."
  key_links:
    - from: src/app/(private)/dashboard/page.tsx
      to: src/server/dashboard/program-dashboard.ts
      via: "dashboard SSR reads today/trends data through server-only composition helpers"
      pattern: "loadDashboardProgramTodaySection|loadDashboardTrendsSection"
    - from: src/server/dashboard/program-dashboard.ts
      to: src/lib/program/select-today-session.ts
      via: "server loader reuses existing today projection logic instead of duplicating route-specific shaping"
      pattern: "selectTodayWorkoutProjection"
    - from: src/app/(private)/dashboard/_components/trends-summary-card.tsx
      to: src/app/(private)/dashboard/page.tsx
      via: "component receives explicit section state so outages render as errors rather than disappearing"
      pattern: "error|empty|ready"
---

<objective>
Execute `STAB-01` after phase 08 by removing request-derived dashboard trust and making degraded program reads explicit.

Purpose: stabilize the authenticated landing page without changing program-selection business rules.
Output: server-only dashboard data loaders, explicit today/trends section states, and deterministic regression coverage for AUD-05.
</objective>

<execution_context>
@/home/flo/.codex/get-shit-done/workflows/execute-plan.md
@/home/flo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/09-security-runtime-and-release-proof-stabilization/09-CONTEXT.md
@.planning/phases/09-security-runtime-and-release-proof-stabilization/09-RESEARCH.md
@.planning/phases/07-audit-technique-avanc-et-stabilisation-compl-te-de-l-application/07-REMEDIATION-BACKLOG.md
@src/app/(private)/dashboard/page.tsx
@src/app/api/program/today/route.ts
@src/app/api/program/trends/route.ts
@src/lib/program/select-today-session.ts
@tests/program/dashboard-today-surface.test.ts
@tests/program/dashboard-trends-surface.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add server-only dashboard loaders for today and trends section state</name>
  <files>src/server/dashboard/program-dashboard.ts, src/lib/program/select-today-session.ts, tests/program/dashboard-page-data.test.ts</files>
  <behavior>
    - Test 1: Today loader resolves account-scoped data directly from server code and never depends on `host` or `x-forwarded-host` headers.
    - Test 2: Today and trends loaders return explicit `ready`, `empty`, or `error` states instead of `null`.
    - Test 3: Trends loader preserves the existing strict 30d summary contract while surfacing failures as `error`.
  </behavior>
  <action>Write failing loader-state tests first, then introduce a server-only dashboard helper module that composes DAL/projection logic directly for today/next workout and trends summary. Reuse existing route projection helpers where possible so this is a trust/remediation change, not a contract rewrite.</action>
  <verify>
    <automated>pnpm test tests/program/dashboard-page-data.test.ts</automated>
  </verify>
  <done>Dashboard server composition is direct, account-scoped, and explicit about degraded states.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewire dashboard rendering to preserve business-empty behavior and expose real errors</name>
  <files>src/app/(private)/dashboard/page.tsx, src/app/(private)/dashboard/_components/today-workout-card.tsx, src/app/(private)/dashboard/_components/trends-summary-card.tsx, tests/program/dashboard-today-surface.test.ts, tests/program/dashboard-trends-surface.test.ts</files>
  <behavior>
    - Test 1: Today workout UI still shows true empty-state behavior when no current or next workout exists, but shows a distinct error surface when server loading fails.
    - Test 2: Trends section remains in the same location in the dashboard order, but now renders an explicit degraded/error state instead of disappearing.
    - Test 3: `pickDashboardSession(...)` still prioritizes today over next-session fallback and leaves adaptive forecast wiring unchanged.
  </behavior>
  <action>Replace nullable internal-fetch code in `page.tsx` with direct loader-state consumption. Update the today and trends components only as much as needed to render explicit degraded states while preserving current labels, hierarchy, and section order.</action>
  <verify>
    <automated>pnpm test tests/program/dashboard-today-surface.test.ts tests/program/dashboard-trends-surface.test.ts tests/program/dashboard-page-data.test.ts</automated>
  </verify>
  <done>Dashboard rendering can distinguish empty business data from runtime failure without changing the intended UX flow.</done>
</task>

</tasks>

<verification>
Run the dashboard surface tests to prove that same-service SSR trust is gone, today/next selection still behaves the same, and degraded states are explicit for both today and trends sections.
</verification>

<success_criteria>
- Dashboard SSR no longer uses request-derived origins or cookie-forwarded internal HTTP calls for today/trends data.
- Today workout surface distinguishes `empty` from `error` without regressing current session-selection behavior.
- Trends section renders an explicit degraded state when loading fails instead of vanishing silently.
</success_criteria>

<output>
After completion, create `.planning/phases/09-security-runtime-and-release-proof-stabilization/09-01-SUMMARY.md`.
</output>
