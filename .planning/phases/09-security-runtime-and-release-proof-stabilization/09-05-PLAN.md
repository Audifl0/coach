---
phase: 09-security-runtime-and-release-proof-stabilization
plan: 09-05
type: execute
wave: 4
depends_on:
  - 09-01
  - 09-04
files_modified:
  - .env.example
  - src/server/env/ops-config.ts
  - src/server/observability/app-logger.ts
  - src/app/api/program/today/route.ts
  - src/app/api/program/trends/route.ts
  - src/app/api/program/history/route.ts
  - src/app/api/program/sessions/[sessionId]/route.ts
  - infra/scripts/smoke-authenticated-dashboard.mjs
  - infra/scripts/deploy.sh
  - infra/scripts/run-restore-drill.sh
  - docs/operations/vps-deploy.md
  - docs/operations/restore-drill-runbook.md
  - tests/ops/restore-drill.test.ts
  - tests/ops/authenticated-dashboard-smoke.test.ts
autonomous: true
requirements:
  - DASH-01
  - PLAT-02
user_setup:
  - "Populate the centralized ops env contract, including a dedicated authenticated smoke-test account for non-production verification."
must_haves:
  truths:
    - "Operator readiness stays narrow and release-focused: env expectations are centralized, authenticated smoke exists for deploy/restore verification, and critical server failures emit structured logs."
    - "Structured logging is server-only and allowlisted; it records critical failures and degraded-path events without turning this phase into a broad observability platform rewrite."
    - "Restore and deploy verification prove authenticated dashboard reachability with business-data checks, not only anonymous status codes."
  artifacts:
    - path: src/server/env/ops-config.ts
      provides: "Centralized parsing/validation for release-facing env inputs used by deploy, restore, and authenticated smoke flows."
      exports:
        - parseOpsRuntimeConfig
    - path: src/server/observability/app-logger.ts
      provides: "Minimal structured logger for critical server failures and explicit degraded-path events."
      exports:
        - appLogger
        - logRouteFailure
    - path: infra/scripts/smoke-authenticated-dashboard.mjs
      provides: "Node 22 authenticated smoke helper that logs in and validates account-scoped dashboard content for deploy/restore evidence."
  key_links:
    - from: infra/scripts/smoke-authenticated-dashboard.mjs
      to: src/app/api/program/today/route.ts
      via: "authenticated smoke verifies account-scoped dashboard data instead of anonymous reachability only"
      pattern: "/api/program/today|dashboard"
    - from: infra/scripts/run-restore-drill.sh
      to: infra/scripts/smoke-authenticated-dashboard.mjs
      via: "restore evidence deepens from anonymous status checks to authenticated product sanity"
      pattern: "smoke-authenticated-dashboard"
    - from: src/app/api/program/history/route.ts
      to: src/server/observability/app-logger.ts
      via: "critical unexpected failures emit structured server logs at route boundaries"
      pattern: "appLogger|logRouteFailure"
---

<objective>
Execute `STAB-05` after the critical dashboard/runtime fixes by deepening operator readiness just enough for credible release validation.

Purpose: centralize release-facing env inputs, add authenticated operational smoke, and introduce minimal structured app logging for critical failures.
Output: centralized ops env parser, authenticated dashboard smoke helper, route-level structured logging, and updated deploy/restore runbooks.
</objective>

<execution_context>
@/home/flo/.codex/get-shit-done/workflows/execute-plan.md
@/home/flo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/09-security-runtime-and-release-proof-stabilization/09-CONTEXT.md
@.planning/phases/09-security-runtime-and-release-proof-stabilization/09-RESEARCH.md
@.env.example
@docs/operations/vps-deploy.md
@docs/operations/restore-drill-runbook.md
@infra/scripts/deploy.sh
@infra/scripts/run-restore-drill.sh
@src/server/llm/observability.ts
@src/app/api/program/today/route.ts
@src/app/api/program/history/route.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Centralize the release-facing env contract and authenticated smoke configuration</name>
  <files>.env.example, src/server/env/ops-config.ts, docs/operations/vps-deploy.md, docs/operations/restore-drill-runbook.md, tests/ops/authenticated-dashboard-smoke.test.ts</files>
  <behavior>
    - Test 1: Ops env parsing fails clearly when required deploy/restore/authenticated-smoke inputs are missing or malformed.
    - Test 2: `.env.example` and the runbooks describe the same contract and naming.
    - Test 3: Existing provider/pipeline env contracts remain untouched; this task centralizes only the release-facing operator inputs needed for phase 09.
  </behavior>
  <action>Write failing config/documentation tests first, then add one centralized parser for deploy, restore, and authenticated-smoke env inputs. Update `.env.example` and both runbooks so operators have one canonical source of truth for the narrow phase-09 release workflow.</action>
  <verify>
    <automated>pnpm test tests/ops/authenticated-dashboard-smoke.test.ts</automated>
  </verify>
  <done>Operators have one centralized env contract for release-proof and restore validation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add authenticated dashboard smoke for deploy and restore evidence</name>
  <files>infra/scripts/smoke-authenticated-dashboard.mjs, infra/scripts/deploy.sh, infra/scripts/run-restore-drill.sh, tests/ops/authenticated-dashboard-smoke.test.ts, tests/ops/restore-drill.test.ts</files>
  <behavior>
    - Test 1: Smoke helper logs in with configured credentials, verifies authenticated dashboard reachability, and asserts business-data presence rather than anonymous status code only.
    - Test 2: Deploy flow can invoke the authenticated smoke after HTTPS reachability succeeds.
    - Test 3: Restore drill evidence includes authenticated verification markers in addition to the existing restore stages.
  </behavior>
  <action>Use Node 22 built-in `fetch` to implement one authenticated smoke helper script and wire it into deploy/restore flows. Keep it deterministic and light-weight; do not introduce Playwright or browser automation.</action>
  <verify>
    <automated>pnpm test tests/ops/authenticated-dashboard-smoke.test.ts tests/ops/restore-drill.test.ts && node --check infra/scripts/smoke-authenticated-dashboard.mjs</automated>
  </verify>
  <done>Deploy and restore verification now include authenticated product sanity, not only anonymous endpoint reachability.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add minimal structured logging for critical route and degraded-path failures</name>
  <files>src/server/observability/app-logger.ts, src/app/api/program/today/route.ts, src/app/api/program/trends/route.ts, src/app/api/program/history/route.ts, src/app/api/program/sessions/[sessionId]/route.ts, tests/ops/authenticated-dashboard-smoke.test.ts</files>
  <behavior>
    - Test 1: Unexpected route failures emit structured log records with allowlisted fields only.
    - Test 2: Explicit degraded-path events introduced by 09-01 can be logged without leaking request secrets or user payloads.
    - Test 3: Successful requests remain quiet; logging is for critical failures and degraded-path evidence only.
  </behavior>
  <action>Model the logger after the allowlisted provider observability pattern already in the repo. Add one server-only logger module and instrument only the critical route/degraded boundaries that operators need for release and restore diagnosis.</action>
  <verify>
    <automated>pnpm test tests/ops/authenticated-dashboard-smoke.test.ts</automated>
  </verify>
  <done>Critical runtime failures now have minimal structured server logs that operators can use during release validation and incident response.</done>
</task>

</tasks>

<verification>
Run ops smoke/config tests and script syntax checks to confirm the env contract is centralized, authenticated smoke works for deploy/restore flows, and structured logging is minimal and allowlisted.
</verification>

<success_criteria>
- One centralized env contract describes the release-facing operator inputs required by deploy, restore, and authenticated smoke.
- Deploy and restore verification include authenticated dashboard sanity checks with business-data evidence.
- Critical server failures and degraded-path events emit minimal structured logs without broad observability scope creep.
</success_criteria>

<output>
After completion, create `.planning/phases/09-security-runtime-and-release-proof-stabilization/09-05-SUMMARY.md`.
</output>
