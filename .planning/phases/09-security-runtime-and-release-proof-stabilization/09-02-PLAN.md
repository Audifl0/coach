---
phase: 09-security-runtime-and-release-proof-stabilization
plan: 09-02
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - prisma/schema.prisma
  - prisma/migrations/0006_runtime_consistency_guards/migration.sql
  - src/server/dal/program.ts
  - src/server/services/program-generation.ts
  - src/server/dal/adaptive-coaching.ts
  - src/server/services/adaptive-coaching.ts
  - src/server/services/session-logging.ts
  - src/app/api/program/generate/route.ts
  - src/app/api/program/adaptation/[recommendationId]/confirm/route.ts
  - src/app/api/program/adaptation/[recommendationId]/reject/route.ts
  - tests/program/program-dal.test.ts
  - tests/program/program-generate-route.test.ts
  - tests/program/program-session-logging-route.test.ts
  - tests/program/adaptive-coaching-service.test.ts
  - tests/program/adaptive-coaching-confirm-route.test.ts
autonomous: true
requirements:
  - PROG-01
  - LOG-01
  - LOG-02
  - LOG-03
  - ADAP-01
  - ADAP-03
  - SAFE-03
must_haves:
  truths:
    - "After phase 08 has restored the red gates, persistence owns the highest-risk runtime invariants called out by AUD-06: one active plan per user, no post-completion logging writes, and no partial reject-plus-fallback adaptive state."
    - "Duplicate submits and stale requests resolve deterministically as conflicts or idempotent outcomes instead of creating nondeterministic or partially-written state."
    - "Existing business rules remain unchanged: program generation still archives prior plans, session completion still derives duration from `startedAt`, and adaptive confirmation policy still requires `pending_confirmation`, unexpired state, and target-session match."
  artifacts:
    - path: prisma/migrations/0006_runtime_consistency_guards/migration.sql
      provides: "Manual PostgreSQL guardrails for phase-09 persistence invariants that Prisma schema alone cannot express."
    - path: src/server/dal/program.ts
      provides: "Program DAL writes with persistence-level protection for active-plan replacement and completion-sensitive session mutations."
      exports:
        - replaceActivePlan
        - completeSession
    - path: src/server/dal/adaptive-coaching.ts
      provides: "Typed stale-state conflict handling and atomic adaptive rejection/fallback persistence."
      exports:
        - updateAdaptiveRecommendationStatus
        - rejectRecommendationWithFallback
  key_links:
    - from: src/server/services/program-generation.ts
      to: src/server/dal/program.ts
      via: "service translates unique-active-plan persistence conflicts into deterministic generation behavior"
      pattern: "replaceActivePlan"
    - from: src/server/services/session-logging.ts
      to: src/server/dal/program.ts
      via: "session lifecycle checks remain in service while completion-sensitive write protection moves into the DAL transaction boundary"
      pattern: "completeSession|upsertLoggedSet|markExerciseSkipped|updateSessionNote"
    - from: src/server/services/adaptive-coaching.ts
      to: src/server/dal/adaptive-coaching.ts
      via: "confirm/reject routes receive typed stale-state outcomes and atomic fallback persistence"
      pattern: "updateAdaptiveRecommendationStatus|rejectRecommendationWithFallback"
---

<objective>
Execute `STAB-02` after the dashboard trust fix by hardening the three release-sensitive mutation clusters identified in AUD-06.

Purpose: make plan generation, session logging/completion, and adaptive decision transitions deterministic under duplicate submits and multi-tab races.
Output: persistence guards, typed conflict handling, and race-oriented regression coverage.
</objective>

<execution_context>
@/home/flo/.codex/get-shit-done/workflows/execute-plan.md
@/home/flo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/09-security-runtime-and-release-proof-stabilization/09-CONTEXT.md
@.planning/phases/09-security-runtime-and-release-proof-stabilization/09-RESEARCH.md
@.planning/phases/07-audit-technique-avanc-et-stabilisation-compl-te-de-l-application/07-AUDIT-REPORT.md
@prisma/schema.prisma
@src/server/dal/program.ts
@src/server/services/program-generation.ts
@src/server/services/session-logging.ts
@src/server/dal/adaptive-coaching.ts
@src/server/services/adaptive-coaching.ts
@tests/program/program-dal.test.ts
@tests/program/adaptive-coaching-confirm-route.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Enforce one active plan per user at the persistence boundary</name>
  <files>prisma/schema.prisma, prisma/migrations/0006_runtime_consistency_guards/migration.sql, src/server/dal/program.ts, src/server/services/program-generation.ts, src/app/api/program/generate/route.ts, tests/program/program-dal.test.ts, tests/program/program-generate-route.test.ts</files>
  <behavior>
    - Test 1: Concurrent or duplicate active-plan replacement attempts cannot leave two active plans for the same user.
    - Test 2: Service/route layers translate the persistence conflict into a deterministic conflict or retry-safe response rather than an opaque 500.
    - Test 3: Existing archive-then-create behavior still produces one active replacement plan with account scope intact.
  </behavior>
  <action>Write failing DAL/service tests first, then add the smallest persistence primitive that proves the invariant: a manual PostgreSQL partial unique index for active plans plus bounded service translation for unique/conflict failures. Keep current plan-generation rules unchanged.</action>
  <verify>
    <automated>pnpm test tests/program/program-dal.test.ts tests/program/program-generate-route.test.ts</automated>
  </verify>
  <done>Program generation cannot create multiple active plans for one account even under race conditions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Move session-completion-sensitive write protection into the DAL transaction boundary</name>
  <files>src/server/dal/program.ts, src/server/services/session-logging.ts, tests/program/program-dal.test.ts, tests/program/program-session-logging-route.test.ts</files>
  <behavior>
    - Test 1: Set autosave, skip/revert, and note updates fail deterministically once a concurrent completion has already won.
    - Test 2: Completing a session remains single-writer and preserves the current duration/fatigue/readiness business rules.
    - Test 3: Post-completion writes return stable, expected route/service behavior rather than stale read-then-write success.
  </behavior>
  <action>Introduce bounded DAL helpers that guard or lock the parent session row while performing completion-sensitive writes. Keep the session-logging service API stable; only shift stale-state enforcement to the persistence layer required by AUD-06.</action>
  <verify>
    <automated>pnpm test tests/program/program-dal.test.ts tests/program/program-session-logging-route.test.ts</automated>
  </verify>
  <done>Session logging behavior is deterministic under multi-tab completion races.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Make adaptive confirm/reject transitions typed, atomic, and route-deterministic</name>
  <files>src/server/dal/adaptive-coaching.ts, src/server/services/adaptive-coaching.ts, src/app/api/program/adaptation/[recommendationId]/confirm/route.ts, src/app/api/program/adaptation/[recommendationId]/reject/route.ts, tests/program/adaptive-coaching-service.test.ts, tests/program/adaptive-coaching-confirm-route.test.ts</files>
  <behavior>
    - Test 1: Expected-status mismatches surface as typed stale-state conflicts that routes return as `409`, not generic `500`.
    - Test 2: Rejecting a high-impact recommendation atomically persists the rejection, conservative fallback recommendation, and trace metadata.
    - Test 3: Existing policy checks for pending confirmation, expiration, and target-session match remain unchanged.
  </behavior>
  <action>Replace generic mismatch throws with a typed stale-state error path and add one DAL-level helper for reject-plus-fallback atomicity. Keep the current adaptive policy and forecast semantics intact; this task is about runtime consistency, not coaching-rule redesign.</action>
  <verify>
    <automated>pnpm test tests/program/adaptive-coaching-service.test.ts tests/program/adaptive-coaching-confirm-route.test.ts</automated>
  </verify>
  <done>Adaptive confirmation and rejection behave deterministically under stale requests without partial state.</done>
</task>

</tasks>

<verification>
Run DAL, service, and route tests covering active-plan replacement, session completion races, and adaptive stale-state handling to prove all three AUD-06 mutation clusters are deterministic.
</verification>

<success_criteria>
- Persistence guarantees prevent multiple active plans per user.
- Session logging writes cannot succeed after a concurrent completion wins.
- Adaptive confirm/reject routes surface stale-state conflicts deterministically and reject-plus-fallback is atomic.
</success_criteria>

<output>
After completion, create `.planning/phases/09-security-runtime-and-release-proof-stabilization/09-02-SUMMARY.md`.
</output>
