---
phase: 09-security-runtime-and-release-proof-stabilization
plan: 09-03
type: execute
wave: 5
depends_on:
  - 09-01
  - 09-02
  - 09-04
  - 09-05
files_modified:
  - package.json
  - infra/scripts/deploy.sh
  - infra/scripts/smoke-test-https.sh
  - infra/scripts/release-proof.sh
  - docs/operations/vps-deploy.md
  - docs/operations/release-proof.md
  - tests/ops/release-proof.test.ts
autonomous: true
requirements:
  - DASH-01
  - DASH-02
  - DASH-03
  - PLAT-02
user_setup:
  - "Provide non-production authenticated smoke credentials in the deploy env file or documented env source used by the release-proof script."
  - "Run this plan only after phase 08 outputs (`corepack pnpm typecheck`, `corepack pnpm test`, `corepack pnpm build`, secret-safe deploy flow, auth throttling) are already green on the target branch/environment."
must_haves:
  truths:
    - "Release proof remains intentionally narrow: one repeatable path covers compile, test, build, deploy smoke, and authenticated dashboard sanity without adding a browser E2E stack or broad CI platform rewrite."
    - "The authenticated proof exercises the stabilized dashboard/runtime surfaces from 09-01 through 09-05 rather than encoding pre-fix behavior."
    - "Operator workflows remain script-first and repo-native, reusing existing deploy and smoke primitives wherever possible."
  artifacts:
    - path: infra/scripts/release-proof.sh
      provides: "Single-command release gate that composes repo-native verification steps in deterministic order."
    - path: docs/operations/release-proof.md
      provides: "Operator-facing release checklist and evidence expectations for the narrow phase-09 gate."
    - path: tests/ops/release-proof.test.ts
      provides: "Regression coverage that keeps the release-proof orchestration aligned with deploy/smoke scripts and documented prerequisites."
  key_links:
    - from: infra/scripts/release-proof.sh
      to: infra/scripts/deploy.sh
      via: "release proof reuses the existing deploy entrypoint instead of duplicating Docker orchestration"
      pattern: "deploy.sh"
    - from: infra/scripts/release-proof.sh
      to: infra/scripts/smoke-test-https.sh
      via: "post-deploy proof still includes HTTPS reachability before authenticated sanity"
      pattern: "smoke-test-https.sh"
    - from: docs/operations/release-proof.md
      to: docs/operations/vps-deploy.md
      via: "release gate documentation layers on top of the existing deploy runbook rather than replacing it"
      pattern: "deploy|smoke|authenticated"
---

<objective>
Execute `STAB-03` last by composing the stabilized runtime into one narrow, repeatable release-proof workflow.

Purpose: produce a credible post-phase-08 verification path without expanding scope into full CI/E2E platform work.
Output: script-first release gate, release-proof runbook, and tests that keep the orchestration aligned with repo-native commands.
</objective>

<execution_context>
@/home/flo/.codex/get-shit-done/workflows/execute-plan.md
@/home/flo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/09-security-runtime-and-release-proof-stabilization/09-CONTEXT.md
@.planning/phases/09-security-runtime-and-release-proof-stabilization/09-RESEARCH.md
@package.json
@infra/scripts/deploy.sh
@infra/scripts/smoke-test-https.sh
@docs/operations/vps-deploy.md
@docs/operations/restore-drill-runbook.md
@tests/ops/restore-drill.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add a repo-native release-proof script that composes compile, test, build, deploy, and smoke gates</name>
  <files>package.json, infra/scripts/release-proof.sh, infra/scripts/deploy.sh, infra/scripts/smoke-test-https.sh, tests/ops/release-proof.test.ts</files>
  <behavior>
    - Test 1: Release-proof orchestration executes the expected stages in deterministic order and fails fast on the first failed gate.
    - Test 2: Existing deploy and HTTPS smoke scripts are reused rather than reimplemented.
    - Test 3: Authenticated smoke invocation is wired as a required post-deploy stage, not an optional note.
  </behavior>
  <action>Write failing ops tests first, then add a single release-proof wrapper that runs `typecheck`, `test`, `build`, deploy, HTTPS smoke, and authenticated sanity in order. Keep the wrapper minimal and shell-first; do not add CI-only assumptions or browser automation.</action>
  <verify>
    <automated>pnpm test tests/ops/release-proof.test.ts && bash -n infra/scripts/release-proof.sh infra/scripts/deploy.sh infra/scripts/smoke-test-https.sh</automated>
  </verify>
  <done>The repository has one narrow release-proof command that fails fast and reuses existing deploy primitives.</done>
</task>

<task type="auto">
  <name>Task 2: Publish the operator release-proof runbook and evidence contract</name>
  <files>docs/operations/release-proof.md, docs/operations/vps-deploy.md, tests/ops/release-proof.test.ts</files>
  <action>Document prerequisites, required env inputs, execution order, expected evidence, and stop conditions for the new release-proof path. Cross-link the deploy runbook so operators have one coherent deploy-plus-proof workflow instead of fragmented notes.</action>
  <verify>
    <automated>pnpm test tests/ops/release-proof.test.ts</automated>
  </verify>
  <done>Operators can run the release proof repeatably and know what evidence counts as a valid pass.</done>
</task>

</tasks>

<verification>
Run the release-proof script tests and shell syntax checks to confirm the wrapper is deterministic, fail-fast, and anchored to existing deploy/smoke tooling rather than duplicating it.
</verification>

<success_criteria>
- One documented command covers compile, tests, build, deploy, HTTPS smoke, and authenticated sanity.
- Release-proof orchestration fails fast and reuses existing repo-native deploy/smoke primitives.
- The runbook makes the post-phase-08 gate credible without introducing broad CI or browser E2E scope.
</success_criteria>

<output>
After completion, create `.planning/phases/09-security-runtime-and-release-proof-stabilization/09-03-SUMMARY.md`.
</output>
