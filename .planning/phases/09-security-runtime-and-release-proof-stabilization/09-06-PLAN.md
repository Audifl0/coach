---
phase: 09-security-runtime-and-release-proof-stabilization
plan: 09-06
type: execute
wave: 6
depends_on:
  - 09-02
  - 09-03
  - 09-05
files_modified:
  - src/server/dal/adaptive-coaching.ts
  - infra/scripts/smoke-authenticated-dashboard.d.ts
  - tests/ops/authenticated-dashboard-smoke.test.ts
autonomous: true
gap_closure: true
requirements:
  - ADAP-01
  - DASH-01
  - PLAT-02
must_haves:
  truths:
    - "This gap closure stays inside the already-approved phase-09 stabilization tracks: it removes TypeScript drift exposed while validating the existing STAB-02 adaptive path plus the STAB-03/STAB-05 release-proof and authenticated-smoke surfaces."
    - "Adaptive rejection persistence preserves the existing stale-state and atomic reject-plus-fallback behavior while replacing the typing seam that currently blocks release validation."
    - "Authenticated smoke remains the operator-facing dashboard/business-data proof primitive, and its TypeScript import surface stays explicit and strict rather than falling back to broad `any` declarations."
  artifacts:
    - path: src/server/dal/adaptive-coaching.ts
      provides: "Type-safe adaptive reject-plus-fallback persistence that preserves stale-state conflict detection without an optional `updateMany` branch."
      exports:
        - rejectRecommendationWithFallback
    - path: infra/scripts/smoke-authenticated-dashboard.d.ts
      provides: "Explicit TypeScript module declaration for the authenticated smoke helper imported from tests."
      exports:
        - runAuthenticatedDashboardSmoke
    - path: tests/ops/authenticated-dashboard-smoke.test.ts
      provides: "Strictly typed ops smoke coverage with an explicit callback parameter type instead of implicit `any`."
  key_links:
    - from: tests/ops/authenticated-dashboard-smoke.test.ts
      to: infra/scripts/smoke-authenticated-dashboard.d.ts
      via: "test import resolves the `.mjs` helper through an explicit declaration file compatible with the current tsconfig include rules"
      pattern: "runAuthenticatedDashboardSmoke"
    - from: src/server/dal/adaptive-coaching.ts
      to: tests/program/adaptive-coaching-service.test.ts
      via: "adaptive DAL mutation path stays compatible with the existing stale-state and atomic reject fallback contract"
      pattern: "rejectRecommendationWithFallback|AdaptiveRecommendationStaleStateError"
---

<objective>
Close the remaining phase-09 verification blockers as a narrow continuation of the existing stabilization work.

Purpose: unblock the release-proof entry path by fixing the current `typecheck` / `build` failures only, not by broadening phase scope.
Output: typed adaptive DAL mutation path, explicit `.mjs` declaration for the authenticated smoke helper, strict test callback typing, and final gate re-verification.
</objective>

<execution_context>
@/home/flo/.codex/get-shit-done/workflows/execute-plan.md
@/home/flo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-security-runtime-and-release-proof-stabilization/09-CONTEXT.md
@.planning/phases/09-security-runtime-and-release-proof-stabilization/09-VERIFICATION.md
@.planning/phases/09-security-runtime-and-release-proof-stabilization/09-02-SUMMARY.md
@.planning/phases/09-security-runtime-and-release-proof-stabilization/09-03-SUMMARY.md
@.planning/phases/09-security-runtime-and-release-proof-stabilization/09-05-SUMMARY.md
@src/server/dal/adaptive-coaching.ts
@infra/scripts/smoke-authenticated-dashboard.mjs
@tests/ops/authenticated-dashboard-smoke.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Remove the optional Prisma delegate branch from adaptive reject fallback</name>
  <files>src/server/dal/adaptive-coaching.ts, tests/program/adaptive-coaching-service.test.ts, tests/program/adaptive-coaching-confirm-route.test.ts</files>
  <behavior>
    - Test 1: `rejectRecommendationWithFallback(...)` still returns `null` for a missing account-scoped recommendation.
    - Test 2: expected-status mismatches still surface as `AdaptiveRecommendationStaleStateError`.
    - Test 3: rejection plus fallback creation remains atomic and preserves the current decision-log semantics.
  </behavior>
  <action>Keep the existing phase-09 adaptive behavior, but rewrite the `tx.adaptiveRecommendation.updateMany` path so TypeScript no longer sees an optional delegate method. Use one valid, deterministic Prisma mutation strategy for the transactional rejection update, then keep the stale-state re-read and fallback creation semantics unchanged.</action>
  <verify>
    <automated>corepack pnpm test tests/program/adaptive-coaching-service.test.ts tests/program/adaptive-coaching-confirm-route.test.ts</automated>
  </verify>
  <done>The adaptive DAL compiles cleanly under strict TypeScript while preserving the current stale-state and atomic fallback guarantees.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add strict TypeScript coverage for the authenticated smoke helper import</name>
  <files>infra/scripts/smoke-authenticated-dashboard.d.ts, tests/ops/authenticated-dashboard-smoke.test.ts</files>
  <behavior>
    - Test 1: the ops smoke test imports `../../infra/scripts/smoke-authenticated-dashboard.mjs` without a missing-declaration error.
    - Test 2: the `log` callback passed to `runAuthenticatedDashboardSmoke(...)` has an explicit `string` parameter type.
    - Test 3: the declaration only covers the exported helper surface used by tests; it does not introduce broad `any` module fallbacks.
  </behavior>
  <action>Create a narrow declaration file adjacent to the `.mjs` script that describes the exported smoke helper signature used by the tests. Update `tests/ops/authenticated-dashboard-smoke.test.ts` only as needed to remove the implicit-`any` callback parameter while preserving the current test assertions and fixture behavior.</action>
  <verify>
    <automated>corepack pnpm test tests/ops/authenticated-dashboard-smoke.test.ts && corepack pnpm typecheck</automated>
  </verify>
  <done>The authenticated smoke test stays strict-TypeScript compatible without weakening module typing or changing smoke behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Re-run the release-entry TypeScript gates and stop at the first remaining failure</name>
  <files>src/server/dal/adaptive-coaching.ts, infra/scripts/smoke-authenticated-dashboard.d.ts, tests/ops/authenticated-dashboard-smoke.test.ts</files>
  <action>After Tasks 1 and 2 land, run the repo gates in the order the verification report calls out: `corepack pnpm typecheck` first, then `corepack pnpm build`. If either command still fails, record the exact failing file and error in the summary and do not expand scope beyond direct fallout from these two seams.</action>
  <verify>
    <automated>corepack pnpm typecheck && corepack pnpm build</automated>
  </verify>
  <done>The current HEAD has a green TypeScript gate for both standalone typecheck and the build-time TypeScript pass.</done>
</task>

</tasks>

<verification>
Confirm the adaptive DAL, authenticated smoke test import, and strict callback typing compile cleanly, then prove the phase exits `gaps_found` by rerunning `corepack pnpm typecheck` and `corepack pnpm build`.
</verification>

<success_criteria>
- `src/server/dal/adaptive-coaching.ts` no longer fails on a possibly undefined `updateMany`.
- `tests/ops/authenticated-dashboard-smoke.test.ts` resolves the `.mjs` helper through an explicit declaration and contains no implicit `any`.
- `corepack pnpm typecheck` exits `0`.
- `corepack pnpm build` exits `0`.
</success_criteria>

<output>
After completion, create `.planning/phases/09-security-runtime-and-release-proof-stabilization/09-06-SUMMARY.md`.
</output>
