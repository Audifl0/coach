---
phase: 10-maintainability-cleanup-and-operational-hardening
plan: 10-03
type: execute
wave: 3
depends_on:
  - 10-02
files_modified:
  - infra/caddy/Caddyfile
  - infra/scripts/backup.sh
  - infra/scripts/restore.sh
  - scripts/admin-reset-password.ts
  - scripts/lib/secret-prompt.ts
  - docs/operations/vps-deploy.md
  - docs/operations/data-protection.md
  - docs/operations/restore-drill-runbook.md
  - tests/ops/restore-drill.test.ts
  - tests/ops/caddy-header-policy.test.ts
  - tests/auth/admin-reset.test.ts
  - tests/auth/admin-reset-cli.test.ts
autonomous: true
requirements:
  - AUTH-03
  - PLAT-01
  - PLAT-02
  - PLAT-03
user_setup:
  - "Reload Caddy on the target VPS after deploying the updated response-header policy."
  - "Run the phase-09 authenticated release-proof workflow against the deployed HTTPS environment and capture the header-check evidence alongside the existing smoke proof."
must_haves:
  truths:
    - "Admin password reset no longer echoes the new password to the terminal while preserving the existing generic completion and confirmation semantics."
    - "Backup and restore keep encrypted-at-rest behavior while eliminating plaintext SQL files from the normal backup and restore paths."
    - "The HTTPS reverse-proxy layer emits low-risk browser hardening headers without breaking the already-stabilized Next.js runtime or the phase-09 release-proof path."
  artifacts:
    - path: scripts/admin-reset-password.ts
      provides: "Admin reset CLI that reads secret input without terminal echo and still delegates business logic to the existing reset service."
    - path: infra/scripts/backup.sh
      provides: "Streaming encrypted backup flow that avoids plaintext dump residue on disk."
    - path: infra/caddy/Caddyfile
      provides: "Response-header hardening policy for the deployed HTTPS entrypoint."
  key_links:
    - from: scripts/admin-reset-password.ts
      to: tests/auth/admin-reset-cli.test.ts
      via: "CLI prompt handling is verified independently from service semantics so secret-entry behavior can change without weakening reset logic"
      pattern: "prompt|password|RESET"
    - from: infra/scripts/backup.sh
      to: infra/scripts/restore.sh
      via: "backup and restore remain symmetric encrypted streaming operations under the existing restore-target guardrails"
      pattern: "openssl|pg_dump|psql|RESTORE_TARGET_DB"
    - from: infra/caddy/Caddyfile
      to: docs/operations/vps-deploy.md
      via: "documented deployment verification includes the new response-header policy and reload procedure"
      pattern: "X-Content-Type-Options|Referrer-Policy|Permissions-Policy|frame-ancestors|X-Frame-Options"
---

<objective>
Complete `CLEAN-03` with bounded browser hardening and operator hardening that build on the green phase-09 release-proof baseline.

Purpose: close the remaining moderate-risk security and operations hardening gaps without expanding into a CSP redesign, platform rewrite, or new release gate.
Output: non-echo admin reset prompting, streaming encrypted backup/restore flows, low-risk proxy header policy, and documentation/tests aligned to the hardened paths.
</objective>

<execution_context>
@/home/flo/.codex/get-shit-done/workflows/execute-plan.md
@/home/flo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-maintainability-cleanup-and-operational-hardening/10-CONTEXT.md
@.planning/phases/10-maintainability-cleanup-and-operational-hardening/10-RESEARCH.md
@.planning/phases/07-audit-technique-avanc-et-stabilisation-compl-te-de-l-application/07-REMEDIATION-BACKLOG.md
@.planning/phases/07-audit-technique-avanc-et-stabilisation-compl-te-de-l-application/07-AUDIT-REPORT.md
@infra/caddy/Caddyfile
@infra/scripts/backup.sh
@infra/scripts/restore.sh
@scripts/admin-reset-password.ts
@tests/ops/restore-drill.test.ts
@tests/auth/admin-reset.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add non-echo secret prompting to the admin reset CLI without changing reset semantics</name>
  <files>scripts/admin-reset-password.ts, scripts/lib/secret-prompt.ts, tests/auth/admin-reset.test.ts, tests/auth/admin-reset-cli.test.ts</files>
  <behavior>
    - Test 1: The CLI still requires username, new password, and `RESET {username}` confirmation before executing.
    - Test 2: The password prompt path no longer echoes the entered secret to stdout/stderr.
    - Test 3: Generic success behavior for existing and missing usernames remains delegated to the current admin-reset service.
  </behavior>
  <action>Extract a tiny reusable secret-prompt helper for the CLI and keep `src/lib/auth/admin-reset.ts` untouched. Do not change generic response wording, reset confirmation rules, or service behavior; only harden terminal input handling and add narrow CLI-level coverage for it.</action>
  <verify>
    <automated>corepack pnpm test -- tests/auth/admin-reset.test.ts tests/auth/admin-reset-cli.test.ts</automated>
  </verify>
  <done>The reset CLI hides typed passwords while preserving all current reset semantics and service-level guarantees.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Convert backup and restore to encrypted streaming flows with the existing guardrails intact</name>
  <files>infra/scripts/backup.sh, infra/scripts/restore.sh, docs/operations/data-protection.md, docs/operations/restore-drill-runbook.md, tests/ops/restore-drill.test.ts</files>
  <behavior>
    - Test 1: Backup no longer writes a plaintext `.sql` file before encryption.
    - Test 2: Restore decrypts and replays SQL as a stream while keeping `RESTORE_TARGET_DB`, `ON_ERROR_STOP`, and single-transaction protections.
    - Test 3: Existing drill docs and tests stay aligned with the streaming contract and failure semantics.
  </behavior>
  <action>Replace file-based plaintext handling with streaming `pg_dump | openssl` and `openssl -d | psql` pipelines while preserving `set -euo pipefail`, current env-file parameterization, and restore-target safety checks. Update the runbook and data-protection docs only as needed to describe the new no-plaintext-window behavior.</action>
  <verify>
    <automated>corepack pnpm test -- tests/ops/restore-drill.test.ts && bash -n infra/scripts/backup.sh infra/scripts/restore.sh</automated>
  </verify>
  <done>Backup and restore remain encrypted and fail-fast but no longer create normal-path plaintext SQL artifacts on disk.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add low-risk browser hardening headers to the Caddy entrypoint and document deployment verification</name>
  <files>infra/caddy/Caddyfile, docs/operations/vps-deploy.md, tests/ops/caddy-header-policy.test.ts</files>
  <behavior>
    - Test 1: The proxy emits `X-Content-Type-Options`, `Referrer-Policy`, `Permissions-Policy`, and clickjacking protection headers.
    - Test 2: The plan does not introduce a strict blocking CSP-first rollout that could break the stabilized Next.js app.
    - Test 3: Deployment documentation tells operators how to reload Caddy and verify the headers through the existing HTTPS smoke/release-proof path.
  </behavior>
  <action>Add only low-risk response headers at the proxy layer first. If CSP is touched at all, keep it report-only or narrowly scoped and prove compatibility through the existing smoke path; do not turn this into a nonce-based CSP redesign. Update deploy docs with the exact reload and verification steps that accompany the phase-09 proof path.</action>
  <verify>
    <automated>corepack pnpm test -- tests/ops/caddy-header-policy.test.ts && bash -n infra/scripts/backup.sh infra/scripts/restore.sh</automated>
  </verify>
  <done>The deployed HTTPS entrypoint has pragmatic header hardening plus documented verification steps, without breaking the app or inventing a new release gate.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `corepack pnpm test -- tests/auth/admin-reset.test.ts tests/auth/admin-reset-cli.test.ts`
- [ ] `corepack pnpm test -- tests/ops/restore-drill.test.ts tests/ops/caddy-header-policy.test.ts`
- [ ] `bash -n infra/scripts/backup.sh infra/scripts/restore.sh`
- [ ] `corepack pnpm typecheck`
- [ ] `corepack pnpm test`
- [ ] `corepack pnpm build`
- [ ] Run the phase-09 authenticated release-proof path against the deployed HTTPS environment and confirm the new header checks, backup/restore flow, and admin reset hardening all preserve the same green release proof.
</verification>

<success_criteria>
- All tasks completed.
- All verification checks pass.
- AUTH-03, PLAT-01, PLAT-02, and PLAT-03 remain satisfied while the residual browser and operator hardening gaps from phase 07 are closed.
- No strict-CSP-first rollout, no plaintext backup residue in the normal path, and no reset-password terminal echo remain after the plan completes.
</success_criteria>

<output>
After completion, create `.planning/phases/10-maintainability-cleanup-and-operational-hardening/10-03-SUMMARY.md`
</output>
