---
phase: 10-maintainability-cleanup-and-operational-hardening
plan: 10-02
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - src/server/dal/program.ts
  - src/server/dal/program/plan-lifecycle.ts
  - src/server/dal/program/session-logging.ts
  - src/server/dal/program/trends-read-model.ts
  - src/server/dal/program/history-read-model.ts
  - src/app/(private)/dashboard/page.tsx
  - src/app/(private)/dashboard/loaders/dashboard-access.ts
  - src/app/(private)/dashboard/loaders/today-workout.ts
  - src/app/(private)/dashboard/loaders/adaptive-forecast.ts
  - src/app/(private)/dashboard/loaders/trends-summary.ts
  - src/app/(private)/dashboard/_components/session-logger.tsx
  - src/app/(private)/dashboard/_components/session-logger-state.ts
  - src/app/(private)/dashboard/_components/session-logger-client.ts
  - tests/program/program-dal.test.ts
  - tests/program/program-dal-trends.test.ts
  - tests/program/dashboard-today-surface.test.ts
  - tests/program/dashboard-trends-surface.test.ts
  - tests/program/dashboard-adaptive-forecast.test.ts
  - tests/program/session-history-surface.test.ts
  - tests/program/program-session-logging-route.test.ts
  - tests/program/dashboard-page-loaders.test.ts
  - tests/program/session-logger-surface.test.ts
autonomous: true
requirements:
  - PLAT-01
  - PLAT-03
must_haves:
  truths:
    - "The public `createProgramDal(...)` surface and all current ownership/account-scope rules remain intact while internals are split by concern."
    - "Dashboard redirects, section ordering, and post-phase-09 direct-server loading semantics remain stable after loader extraction."
    - "Session logger endpoint URLs, request payloads, error messages, and completion/duration flows stay behavior-equivalent while state and mutation helpers move behind internal seams."
  artifacts:
    - path: src/server/dal/program.ts
      provides: "Stable public DAL factory that composes smaller internal modules without changing route/service call sites."
      exports:
        - createProgramDal
    - path: src/app/(private)/dashboard/page.tsx
      provides: "Thin dashboard orchestrator that keeps auth/profile redirects and JSX composition while delegating data loading to extracted server helpers."
      exports:
        - default
        - resolveDashboardSectionOrder
    - path: src/app/(private)/dashboard/_components/session-logger.tsx
      provides: "Stable session logger component shell that preserves endpoint and message semantics while delegating internal state and mutation details."
      exports:
        - SessionLogger
  key_links:
    - from: src/server/dal/program.ts
      to: src/server/dal/program/session-logging.ts
      via: "public DAL methods are re-composed from smaller concern modules instead of exposing new APIs"
      pattern: "createProgramDal"
    - from: src/app/(private)/dashboard/page.tsx
      to: src/app/(private)/dashboard/loaders/today-workout.ts
      via: "page keeps routing/orchestration responsibilities while extracted loaders provide post-phase-09 server data composition"
      pattern: "load|resolveDashboard"
    - from: src/app/(private)/dashboard/_components/session-logger.tsx
      to: src/app/(private)/dashboard/_components/session-logger-client.ts
      via: "component preserves current fetch URLs and payload shapes while mutation plumbing moves behind an internal helper layer"
      pattern: "/api/program/sessions/|saveSet|submitSkip|completeSession"
---

<objective>
Execute `CLEAN-02` by decomposing the largest coupled modules behind stable exports, with explicit characterization coverage guarding every extraction seam.

Purpose: make the post-release-safe codebase easier to change without widening the blast radius of `program` DAL, dashboard orchestration, or session logger behavior.
Output: concern-specific internal modules, extracted dashboard loaders, and session-logger helper seams protected by characterization tests.
</objective>

<execution_context>
@/home/flo/.codex/get-shit-done/workflows/execute-plan.md
@/home/flo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-maintainability-cleanup-and-operational-hardening/10-CONTEXT.md
@.planning/phases/10-maintainability-cleanup-and-operational-hardening/10-RESEARCH.md
@.planning/phases/10-maintainability-cleanup-and-operational-hardening/10-01-SUMMARY.md
@src/server/dal/program.ts
@src/app/(private)/dashboard/page.tsx
@src/app/(private)/dashboard/_components/session-logger.tsx
@tests/program/program-dal.test.ts
@tests/program/program-dal-trends.test.ts
@tests/program/program-session-logging-route.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add characterization coverage for dashboard composition and session-logger request flow before extraction</name>
  <files>tests/program/dashboard-page-loaders.test.ts, tests/program/session-logger-surface.test.ts, tests/program/dashboard-today-surface.test.ts, tests/program/dashboard-trends-surface.test.ts, tests/program/dashboard-adaptive-forecast.test.ts, tests/program/program-session-logging-route.test.ts</files>
  <behavior>
    - Test 1: The post-phase-09 dashboard composition path preserves redirect logic, section ordering, and explicit ready/empty/error section outcomes.
    - Test 2: Session logger still starts the timer on first saved set, preserves skip/revert endpoint semantics, and enters duration-correction mode after completion.
    - Test 3: Current user-visible error messages stay mapped to the same failed request classes during extraction.
  </behavior>
  <action>Add characterization tests first, not after the refactor. Reuse existing helper seams and route tests where possible; do not introduce a new browser E2E stack just to cover this cleanup phase. If a small pure helper extraction is needed to test current behavior, keep it internal and behavior-preserving.</action>
  <verify>
    <automated>corepack pnpm test -- tests/program/dashboard-page-loaders.test.ts tests/program/session-logger-surface.test.ts tests/program/dashboard-today-surface.test.ts tests/program/dashboard-trends-surface.test.ts tests/program/dashboard-adaptive-forecast.test.ts tests/program/program-session-logging-route.test.ts</automated>
  </verify>
  <done>Refactor-sensitive dashboard and session-logger behavior is pinned by characterization coverage before any structural extraction begins.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Decompose `program` DAL internals behind the existing `createProgramDal(...)` entrypoint</name>
  <files>src/server/dal/program.ts, src/server/dal/program/plan-lifecycle.ts, src/server/dal/program/session-logging.ts, src/server/dal/program/trends-read-model.ts, src/server/dal/program/history-read-model.ts, tests/program/program-dal.test.ts, tests/program/program-dal-trends.test.ts, tests/program/session-history-surface.test.ts, tests/program/program-session-logging-route.test.ts</files>
  <behavior>
    - Test 1: Active-plan replacement, session lookup, and logging lifecycle methods keep their existing ownership and state semantics.
    - Test 2: Trends and history reads stay deterministic and account-scoped after internal extraction.
    - Test 3: Routes and services continue importing only `createProgramDal(...)`, not the new internal modules directly.
  </behavior>
  <action>Split `src/server/dal/program.ts` by concern exactly as the research recommends: plan/session lookup, session logging mutations, trends reads, and history reads. Keep every public method name and ownership rule stable. Do not redesign the DAL API, introduce repository abstractions, or move business rules out of the current stable contracts.</action>
  <verify>
    <automated>corepack pnpm test -- tests/program/program-dal.test.ts tests/program/program-dal-trends.test.ts tests/program/session-history-surface.test.ts tests/program/program-session-logging-route.test.ts && corepack pnpm typecheck</automated>
  </verify>
  <done>The program DAL is internally decomposed by concern while external callers continue using the unchanged public factory and methods.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extract dashboard loaders and session-logger internals without changing public behavior</name>
  <files>src/app/(private)/dashboard/page.tsx, src/app/(private)/dashboard/loaders/dashboard-access.ts, src/app/(private)/dashboard/loaders/today-workout.ts, src/app/(private)/dashboard/loaders/adaptive-forecast.ts, src/app/(private)/dashboard/loaders/trends-summary.ts, src/app/(private)/dashboard/_components/session-logger.tsx, src/app/(private)/dashboard/_components/session-logger-state.ts, src/app/(private)/dashboard/_components/session-logger-client.ts, tests/program/dashboard-page-loaders.test.ts, tests/program/session-logger-surface.test.ts, tests/program/dashboard-today-surface.test.ts, tests/program/dashboard-trends-surface.test.ts, tests/program/dashboard-adaptive-forecast.test.ts</files>
  <behavior>
    - Test 1: `page.tsx` keeps redirect decisions and final JSX composition while loaders own concern-specific data assembly.
    - Test 2: Session logger continues calling the exact same endpoints with the same payloads and success/error messaging.
    - Test 3: No extraction reintroduces host-derived trust, hidden degraded states, or a new client-side state model.
  </behavior>
  <action>After the characterization coverage exists, split the dashboard page into server loaders/view-model builders by concern and split the session logger into internal state and mutation helpers. Keep the page responsible for routing/orchestration, keep the component shell stable, and move pure helpers first, client mutation helpers second, and presentational decomposition last. Do not change route URLs, payload contracts, or render ordering.</action>
  <verify>
    <automated>corepack pnpm test -- tests/program/dashboard-page-loaders.test.ts tests/program/session-logger-surface.test.ts tests/program/dashboard-today-surface.test.ts tests/program/dashboard-trends-surface.test.ts tests/program/dashboard-adaptive-forecast.test.ts && corepack pnpm typecheck</automated>
  </verify>
  <done>Dashboard and session logger are decomposed behind stable public seams, with verified behavior parity for redirects, loaders, endpoint calls, and visible messages.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `corepack pnpm test -- tests/program/dashboard-page-loaders.test.ts tests/program/session-logger-surface.test.ts`
- [ ] `corepack pnpm test -- tests/program/program-dal.test.ts tests/program/program-dal-trends.test.ts tests/program/dashboard-today-surface.test.ts tests/program/dashboard-trends-surface.test.ts tests/program/dashboard-adaptive-forecast.test.ts tests/program/session-history-surface.test.ts tests/program/program-session-logging-route.test.ts`
- [ ] `corepack pnpm typecheck`
- [ ] `corepack pnpm test`
- [ ] `corepack pnpm build`
- [ ] Re-run the phase-09 authenticated release-proof path and confirm the decomposed dashboard/session flows still satisfy the same scripted verification.
</verification>

<success_criteria>
- All tasks completed.
- All verification checks pass.
- `createProgramDal(...)`, dashboard route behavior, and session-logger external contracts remain stable while internal modules become smaller and easier to change safely.
- No extraction broadens scope into new abstractions, new test stacks, or behavior changes outside the bounded `CLEAN-02` seams.
</success_criteria>

<output>
After completion, create `.planning/phases/10-maintainability-cleanup-and-operational-hardening/10-02-SUMMARY.md`
</output>
