---
phase: 10-maintainability-cleanup-and-operational-hardening
plan: 10-01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/server/dal/profile.ts
  - src/app/api/profile/route.ts
  - src/app/(private)/dashboard/page.tsx
  - src/app/api/program/today/route.ts
  - src/app/api/program/history/route.ts
  - src/app/api/program/trends/route.ts
  - src/app/api/program/trends/[exerciseKey]/route.ts
  - src/app/api/program/exercises/[plannedExerciseId]/substitutions/route.ts
  - src/app/api/program/exercises/[plannedExerciseId]/substitute/route.ts
  - src/app/api/program/sessions/[sessionId]/route.ts
  - src/app/api/program/sessions/[sessionId]/note/route.ts
  - src/app/api/program/sessions/[sessionId]/duration/route.ts
  - src/app/api/program/sessions/[sessionId]/complete/route.ts
  - src/app/api/program/sessions/[sessionId]/exercises/[plannedExerciseId]/sets/route.ts
  - src/app/api/program/sessions/[sessionId]/exercises/[plannedExerciseId]/skip/route.ts
  - src/server/services/adaptive-coaching.ts
  - tests/profile/profile-route.test.ts
  - tests/profile/onboarding-gate.test.ts
  - tests/program/dashboard-today-surface.test.ts
  - tests/program/dashboard-trends-surface.test.ts
  - tests/program/dashboard-adaptive-forecast.test.ts
  - tests/program/program-trends-route.test.ts
  - tests/program/session-history-surface.test.ts
  - tests/program/program-session-logging-route.test.ts
  - tests/program/adaptive-coaching-service.test.ts
autonomous: true
requirements:
  - PLAT-01
  - PLAT-03
must_haves:
  truths:
    - "Phase 10 starts only after phases 08 and 09 leave `corepack pnpm typecheck`, `corepack pnpm test`, `corepack pnpm build`, and the phase-09 authenticated release-proof path green."
    - "Profile, program, and adaptive default dependency builders stop spreading `unknown` and repeated `as never` casts across route and service boundaries while preserving current route payloads and behavior."
    - "Read-only dashboard and program surfaces keep the post-phase-09 trust model intact and do not reintroduce request-derived same-service fetch assumptions."
  artifacts:
    - path: src/server/dal/profile.ts
      provides: "Typed profile DAL records and Prisma-backed construction helpers that route handlers can consume without `unknown` payload plumbing."
      exports:
        - createProfileDal
    - path: src/app/api/profile/route.ts
      provides: "Profile handlers whose dependency contract uses real profile DTOs for GET, onboarding save, and edit patch flows."
      exports:
        - createProfileGetHandler
        - createProfilePutHandler
        - GET
        - PUT
    - path: src/server/services/adaptive-coaching.ts
      provides: "Adaptive service bootstrapping that consumes shared typed DAL adapters instead of repeating constructor-level casts at each entry point."
  key_links:
    - from: src/app/api/profile/route.ts
      to: src/server/dal/profile.ts
      via: "default route deps are built from typed profile DAL helpers rather than `unknown` pass-through callbacks"
      pattern: "createProfileDal|createProfileGetHandler|createProfilePutHandler"
    - from: src/app/(private)/dashboard/page.tsx
      to: src/app/api/program/today/route.ts
      via: "dashboard helpers stay aligned with the stabilized today-session contract without reintroducing host-derived fetch trust"
      pattern: "pickDashboardSession|parseProgramTodayResponse"
    - from: src/server/services/adaptive-coaching.ts
      to: src/app/api/program/sessions/[sessionId]/complete/route.ts
      via: "typed DAL builder seams remain compatible with existing adaptive and session-mutation entry points"
      pattern: "createProgramDal|createAdaptiveCoachingDal"
---

<objective>
Shrink the highest-friction type-boundary erosion identified by `CLEAN-01` without changing user-visible behavior.

Purpose: make post-phase-09 runtime and route seams easier to change safely by replacing repeated constructor casts and `unknown` route contracts with typed adapters.
Output: typed profile/program/adaptive dependency builders plus characterization-preserving test updates for the affected route and surface seams.
</objective>

<execution_context>
@/home/flo/.codex/get-shit-done/workflows/execute-plan.md
@/home/flo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-maintainability-cleanup-and-operational-hardening/10-CONTEXT.md
@.planning/phases/10-maintainability-cleanup-and-operational-hardening/10-RESEARCH.md
@.planning/phases/07-audit-technique-avanc-et-stabilisation-compl-te-de-l-application/07-REMEDIATION-BACKLOG.md
@.planning/phases/07-audit-technique-avanc-et-stabilisation-compl-te-de-l-application/07-AUDIT-REPORT.md
@src/server/dal/profile.ts
@src/app/api/profile/route.ts
@src/app/(private)/dashboard/page.tsx
@src/server/services/adaptive-coaching.ts
@src/app/api/program/today/route.ts
@src/app/api/program/history/route.ts
@src/app/api/program/trends/route.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Remove `unknown` and cast drift from the profile route seam first</name>
  <files>src/server/dal/profile.ts, src/app/api/profile/route.ts, tests/profile/profile-route.test.ts, tests/profile/onboarding-gate.test.ts</files>
  <behavior>
    - Test 1: GET and PUT profile handlers keep the existing 401, 200, and 400 behaviors.
    - Test 2: Edit mode still preserves untouched profile sections when patching one field.
    - Test 3: Onboarding completeness checks consume the same minimal typed profile shape across route and dashboard callers.
  </behavior>
  <action>Start with the smallest end-to-end seam from the research: tighten `createProfileDal(...)` and the route dependency contract so profile reads and writes use exported profile record/input types instead of `unknown` and `as never`. If a temporary Prisma compatibility cast is still needed, move it behind a DAL-local helper and keep it out of the route layer.</action>
  <verify>
    <automated>corepack pnpm test -- tests/profile/profile-route.test.ts tests/profile/onboarding-gate.test.ts</automated>
  </verify>
  <done>Profile routes and completeness checks are typed end to end with existing behavior preserved under targeted tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Apply the same typed builder pattern to read-only program and dashboard surfaces</name>
  <files>src/app/(private)/dashboard/page.tsx, src/app/api/program/today/route.ts, src/app/api/program/history/route.ts, src/app/api/program/trends/route.ts, src/app/api/program/trends/[exerciseKey]/route.ts, tests/program/dashboard-today-surface.test.ts, tests/program/dashboard-trends-surface.test.ts, tests/program/dashboard-adaptive-forecast.test.ts, tests/program/program-trends-route.test.ts, tests/program/session-history-surface.test.ts</files>
  <behavior>
    - Test 1: Today/next selection stays deterministic and aligned with the shared program contracts.
    - Test 2: Trends parsing and dashboard forecast helpers keep their existing payload and fallback semantics.
    - Test 3: History and trends routes keep their authenticated and ownership-scoped behavior while default deps move onto typed builders.
  </behavior>
  <action>Move next to the lower-risk read surfaces only: today, history, trends, and the dashboard helpers that consume them. Reuse the post-phase-09 trust path and do not add new same-service fetches or broaden dashboard orchestration. The goal is to centralize constructor compatibility inside DAL-local builders, not to redesign the page or route contracts.</action>
  <verify>
    <automated>corepack pnpm test -- tests/program/dashboard-today-surface.test.ts tests/program/dashboard-trends-surface.test.ts tests/program/dashboard-adaptive-forecast.test.ts tests/program/program-trends-route.test.ts tests/program/session-history-surface.test.ts</automated>
  </verify>
  <done>Read-only program surfaces consume stable typed builders and remain behavior-equivalent under existing route and helper tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Sweep remaining adaptive and session-mutation bootstraps onto shared DAL adapters</name>
  <files>src/server/services/adaptive-coaching.ts, src/app/api/program/exercises/[plannedExerciseId]/substitutions/route.ts, src/app/api/program/exercises/[plannedExerciseId]/substitute/route.ts, src/app/api/program/sessions/[sessionId]/route.ts, src/app/api/program/sessions/[sessionId]/note/route.ts, src/app/api/program/sessions/[sessionId]/duration/route.ts, src/app/api/program/sessions/[sessionId]/complete/route.ts, src/app/api/program/sessions/[sessionId]/exercises/[plannedExerciseId]/sets/route.ts, src/app/api/program/sessions/[sessionId]/exercises/[plannedExerciseId]/skip/route.ts, tests/program/program-session-logging-route.test.ts, tests/program/adaptive-coaching-service.test.ts</files>
  <behavior>
    - Test 1: Session logging routes keep current payload validation and deterministic 4xx/2xx behavior.
    - Test 2: Adaptive service behavior stays unchanged while constructor casts move behind typed bootstrapping seams.
    - Test 3: No new route-local `unknown` plumbing or repeated constructor casts are introduced during the sweep.
  </behavior>
  <action>Finish `CLEAN-01` by routing the remaining adaptive and session-mutation entry points through the same typed DAL constructors established above. Keep payload schemas, ownership checks, and status handling untouched; this task is only about shrinking cast entry points and aligning service/route bootstrap types with the actual DAL contracts.</action>
  <verify>
    <automated>corepack pnpm test -- tests/program/program-session-logging-route.test.ts tests/program/adaptive-coaching-service.test.ts && corepack pnpm typecheck</automated>
  </verify>
  <done>The remaining adaptive and session-mutation call sites share typed DAL bootstrapping seams and compile without route-local cast drift.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `corepack pnpm test -- tests/profile/profile-route.test.ts tests/profile/onboarding-gate.test.ts`
- [ ] `corepack pnpm test -- tests/program/dashboard-today-surface.test.ts tests/program/dashboard-trends-surface.test.ts tests/program/dashboard-adaptive-forecast.test.ts tests/program/program-trends-route.test.ts tests/program/session-history-surface.test.ts tests/program/program-session-logging-route.test.ts tests/program/adaptive-coaching-service.test.ts`
- [ ] `corepack pnpm typecheck`
- [ ] `corepack pnpm test`
- [ ] `corepack pnpm build`
- [ ] Re-run the authenticated release-proof path established in phase 09 and confirm these typed refactors do not change the scripted outcome.
</verification>

<success_criteria>
- All tasks completed.
- All verification checks pass.
- The remaining constructor compatibility shims are centralized behind DAL-local helpers instead of repeated across routes and services.
- Dashboard, profile, program, and adaptive flows keep their existing behavior and contracts while `CLEAN-01` removes the highest-friction type-boundary drift.
</success_criteria>

<output>
After completion, create `.planning/phases/10-maintainability-cleanup-and-operational-hardening/10-01-SUMMARY.md`
</output>
