---
phase: 05.1-llm-provider-r-el-openai-anthropic-env-model-key-structured-output-strict-tests-safe-03
plan: 05.1-01
type: execute
wave: 0
depends_on: []
files_modified:
  - src/server/llm/contracts.ts
  - src/server/llm/schema.ts
  - src/server/llm/config.ts
  - tests/program/adaptive-coaching-provider-contracts.test.ts
  - tests/program/adaptive-coaching-provider-config.test.ts
autonomous: true
requirements:
  - ADAP-01
  - ADAP-02
  - ADAP-03
  - SAFE-03
must_haves:
  truths:
    - "When real-provider mode is disabled, adaptation remains available through the existing deterministic local proposal path."
    - "When real-provider mode is enabled with incomplete env configuration, provider runtime fails fast instead of producing partial/unsafe behavior."
    - "Provider responses missing required structured fields (including `evidenceTags`) are rejected and cannot be surfaced as valid recommendations."
  artifacts:
    - path: src/server/llm/contracts.ts
      provides: "Typed provider metadata/results and strict failure taxonomy used by all adapters."
      exports:
        - LlmProvider
        - LlmAttemptResult
        - LlmFailureReason
    - path: src/server/llm/schema.ts
      provides: "Canonical strict JSON schema definition for adaptive recommendation proposal output."
      exports:
        - ADAPTIVE_PROPOSAL_JSON_SCHEMA
    - path: src/server/llm/config.ts
      provides: "Zod-validated env parser with strict timeout/retry policy and feature flag gate."
      exports:
        - parseLlmRuntimeConfig
        - isRealProviderEnabled
key_links:
  - from: src/server/llm/schema.ts
    to: src/lib/adaptive-coaching/contracts.ts
    via: "provider payload must parse through domain parser"
    pattern: "parseAdaptiveRecommendationProposal"
  - from: src/server/llm/config.ts
    to: src/server/services/adaptive-coaching.ts
    via: "feature flag + runtime policy gate"
    pattern: "LLM_REAL_PROVIDER_ENABLED|parseLlmRuntimeConfig"
---

<objective>
Define the strict LLM integration boundary before SDK implementation.

Purpose: lock contracts/config so executor work is deterministic and SAFE-03 fallback remains authoritative.
Output: provider contracts, canonical strict schema, env parser, and red/green tests for boundary behavior.
</objective>

<execution_context>
@/home/flo/.codex/get-shit-done/workflows/execute-plan.md
@/home/flo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05.1-llm-provider-r-el-openai-anthropic-env-model-key-structured-output-strict-tests-safe-03/05.1-CONTEXT.md
@.planning/phases/05.1-llm-provider-r-el-openai-anthropic-env-model-key-structured-output-strict-tests-safe-03/05.1-RESEARCH.md
@src/lib/adaptive-coaching/contracts.ts
@src/server/services/adaptive-coaching.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create strict provider-boundary contracts and canonical JSON schema</name>
  <files>src/server/llm/contracts.ts, src/server/llm/schema.ts, tests/program/adaptive-coaching-provider-contracts.test.ts</files>
  <behavior>
    - Test 1: Canonical schema requires all fields and disallows additional properties.
    - Test 2: Missing `evidenceTags` or empty `evidenceTags` is treated as invalid output.
    - Test 3: Model-provided `status` is excluded from accepted provider payload contract.
  </behavior>
  <action>Write failing tests first, then add provider-domain types (`provider`, `model`, `latencyMs`, `requestId`, `failureReason`) and one strict JSON schema constant aligned to `parseAdaptiveRecommendationProposal`. Keep `status` out of accepted provider payload shape so status assignment stays server-owned.</action>
  <verify>
    <automated>pnpm test tests/program/adaptive-coaching-provider-contracts.test.ts</automated>
  </verify>
  <done>Adapters can only emit schema-conforming payloads that pass domain parser and cannot inject lifecycle status.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add runtime env contract and feature-flag gate for real provider activation</name>
  <files>src/server/llm/config.ts, tests/program/adaptive-coaching-provider-config.test.ts</files>
  <behavior>
    - Test 1: `LLM_REAL_PROVIDER_ENABLED=false` keeps real-provider config optional.
    - Test 2: `LLM_REAL_PROVIDER_ENABLED=true` requires full primary+fallback contract (`LLM_PROVIDER_PRIMARY`, `LLM_PROVIDER_FALLBACK`, both model IDs, both API keys, timeout/retry values).
    - Test 3: Missing `LLM_GLOBAL_MAX_LATENCY_MS` or invalid timeout/retry values fail parse with deterministic error.
  </behavior>
  <action>Implement Zod-based runtime parser exposing explicit config for OpenAI primary + Anthropic fallback, strict timeout defaults, one retry max on primary, and one fallback attempt max. Ensure parser is fail-fast when feature flag is enabled and required envs are missing.</action>
  <verify>
    <automated>pnpm test tests/program/adaptive-coaching-provider-config.test.ts</automated>
  </verify>
  <done>Runtime config is deterministic, validated, and explicitly gated by feature flag.</done>
</task>

</tasks>

<verification>
Run contract/config tests to prove structured output and env gating rules are enforced before provider SDK wiring.
</verification>

<success_criteria>
- Canonical provider output schema is strict and domain-validated.
- `evidenceTags` presence is mandatory and tested.
- Real provider activation cannot run with incomplete env configuration.
</success_criteria>

<output>
After completion, create `.planning/phases/05.1-llm-provider-r-el-openai-anthropic-env-model-key-structured-output-strict-tests-safe-03/05.1-01-SUMMARY.md`.
</output>
