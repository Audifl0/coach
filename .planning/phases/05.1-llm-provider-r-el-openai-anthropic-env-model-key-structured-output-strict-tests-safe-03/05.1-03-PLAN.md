---
phase: 05.1-llm-provider-r-el-openai-anthropic-env-model-key-structured-output-strict-tests-safe-03
plan: 05.1-03
type: execute
wave: 2
depends_on:
  - 05.1-01
  - 05.1-02
files_modified:
  - src/server/services/adaptive-coaching.ts
  - src/app/api/program/adaptation/route.ts
  - tests/program/adaptive-coaching-service.test.ts
  - tests/program/adaptive-coaching-provider-integration.test.ts
  - tests/program/adaptive-coaching-fallback.test.ts
autonomous: true
requirements:
  - ADAP-01
  - ADAP-02
  - ADAP-03
  - SAFE-03
must_haves:
  truths:
    - "Feature flag controls real-provider activation; disabled mode preserves current deterministic local fallback behavior."
    - "Server computes final recommendation status after guardrails; model payload cannot force applied/pending states."
    - "SAFE-03 fallback behavior remains deterministic when provider chain fails or returns invalid structured output."
  artifacts:
    - path: src/server/services/adaptive-coaching.ts
      provides: "Service-level integration of provider client into existing parse->policy->SAFE pipeline."
      exports:
        - createAdaptiveCoachingService
    - path: tests/program/adaptive-coaching-provider-integration.test.ts
      provides: "End-to-end service scenarios validating real-provider path, status ownership, and SAFE-03 handoff."
key_links:
  - from: src/server/llm/client.ts
    to: src/server/services/adaptive-coaching.ts
    via: "`proposeRecommendation` injection point"
    pattern: "createLlmProposalClient|generate"
  - from: src/server/services/adaptive-coaching.ts
    to: src/lib/adaptive-coaching/orchestrator.ts
    via: "raw proposal source feeds fixed policy ordering"
    pattern: "generateAdaptiveRecommendation"
  - from: src/app/api/program/adaptation/route.ts
    to: src/server/services/adaptive-coaching.ts
    via: "route consumes integrated provider-backed service"
    pattern: "createAdaptiveCoachingService|generate"
---

<objective>
Wire provider runtime into adaptation generation and lock deterministic SAFE-03 behavior with integration tests.

Purpose: complete phase 05.1 scope by activating real providers via env flag while preserving server authority and conservative fallback guarantees.
Output: integrated service wiring and deterministic integration tests for valid, invalid, retry, and full-failure scenarios.
</objective>

<execution_context>
@/home/flo/.codex/get-shit-done/workflows/execute-plan.md
@/home/flo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05.1-llm-provider-r-el-openai-anthropic-env-model-key-structured-output-strict-tests-safe-03/05.1-02-PLAN.md
@src/server/services/adaptive-coaching.ts
@src/lib/adaptive-coaching/orchestrator.ts
@tests/program/adaptive-coaching-service.test.ts
@tests/program/adaptive-coaching-fallback.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Integrate provider client behind feature flag into adaptive coaching service</name>
  <files>src/server/services/adaptive-coaching.ts, tests/program/adaptive-coaching-service.test.ts</files>
  <behavior>
    - Test 1: With `LLM_REAL_PROVIDER_ENABLED=false`, service still uses deterministic default proposal behavior.
    - Test 2: With feature flag enabled and valid provider payload, service uses provider proposal.
    - Test 3: Provider payload `status` field is ignored and final status is computed by server policy path.
  </behavior>
  <action>Inject runtime provider client/config into `createAdaptiveCoachingService` and use it as `proposeRecommendation` source only when feature flag is enabled. Preserve existing local fallback behavior for disabled flag and keep policy/orchestrator as the authority for status assignment.</action>
  <verify>
    <automated>pnpm test tests/program/adaptive-coaching-service.test.ts</automated>
  </verify>
  <done>Service activation path is env-gated and does not allow provider output to bypass server-side status rules.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add integration tests for strict-output failures and SAFE-03 deterministic fallback</name>
  <files>tests/program/adaptive-coaching-provider-integration.test.ts, tests/program/adaptive-coaching-fallback.test.ts</files>
  <behavior>
    - Test 1: Primary invalid schema/missing `evidenceTags` then fallback valid schema yields accepted recommendation.
    - Test 2: Primary invalid schema triggers one primary retry; only after second invalid/failure does fallback provider run.
    - Test 3: Primary + fallback invalid/failure yields conservative SAFE-03 fallback with deterministic reason code.
  </behavior>
  <action>Create integration tests that stub provider adapter responses and assert end-state recommendation fields (`fallbackApplied`, `fallbackReason`, bounded progression deltas) plus trace ordering. Ensure SAFE-03 outcome is deterministic for full-chain failure cases.</action>
  <verify>
    <automated>pnpm test tests/program/adaptive-coaching-provider-integration.test.ts tests/program/adaptive-coaching-fallback.test.ts</automated>
  </verify>
  <done>SAFE-03 behavior remains deterministic across timeout/schema/refusal/failure paths in real-provider mode.</done>
</task>

<task type="auto">
  <name>Task 3: Confirm route-level behavior remains stable with provider-enabled service</name>
  <files>src/app/api/program/adaptation/route.ts, tests/program/adaptive-coaching-provider-integration.test.ts</files>
  <action>Update adaptation route wiring only as needed for provider-enabled service dependencies and ensure response contract remains unchanged for clients. Preserve account scoping, not-found masking, and existing dashboard consumer expectations.</action>
  <verify>
    <automated>pnpm test tests/program/adaptive-coaching-provider-integration.test.ts</automated>
  </verify>
  <done>Route remains contract-stable while sourcing recommendations from the integrated real-provider service path.</done>
</task>

</tasks>

<verification>
Run service and provider integration tests to verify real-provider activation, status authority, and SAFE-03 deterministic fallback.
</verification>

<success_criteria>
- Feature flag safely toggles real-provider runtime path.
- Provider responses cannot set lifecycle `status`; server policy remains authoritative.
- Full failure chain deterministically lands on conservative SAFE-03 outcome.
</success_criteria>

<output>
After completion, create `.planning/phases/05.1-llm-provider-r-el-openai-anthropic-env-model-key-structured-output-strict-tests-safe-03/05.1-03-SUMMARY.md`.
</output>
