---
phase: 05.1-llm-provider-r-el-openai-anthropic-env-model-key-structured-output-strict-tests-safe-03
plan: 05.1-02
type: execute
wave: 1
depends_on:
  - 05.1-01
files_modified:
  - package.json
  - pnpm-lock.yaml
  - src/server/llm/client.ts
  - src/server/llm/observability.ts
  - src/server/llm/providers/openai-client.ts
  - src/server/llm/providers/anthropic-client.ts
  - tests/program/adaptive-coaching-provider-fallback.test.ts
  - tests/program/adaptive-coaching-provider-retry.test.ts
  - tests/program/adaptive-coaching-provider-observability.test.ts
autonomous: true
requirements:
  - ADAP-01
  - ADAP-02
  - SAFE-03
must_haves:
  truths:
    - "On provider-side failure, recommendation generation follows a deterministic order: OpenAI attempt, bounded retry, then Anthropic fallback."
    - "If both providers fail or return invalid structured payloads, the client returns no candidate so SAFE-03 can apply a conservative outcome."
    - "Provider runtime emits auditable metadata (provider/model/latency/parse/fallback reason) without exposing prompt or user-sensitive content."
  artifacts:
    - path: src/server/llm/providers/openai-client.ts
      provides: "OpenAI adapter using strict schema response formatting and bounded timeout/retry behavior."
      exports:
        - createOpenAiProposalClient
    - path: src/server/llm/providers/anthropic-client.ts
      provides: "Anthropic fallback adapter using forced schema-shaped tool output."
      exports:
        - createAnthropicProposalClient
    - path: src/server/llm/client.ts
      provides: "Primary->retry->fallback orchestration that returns normalized success/failure metadata."
      exports:
        - createLlmProposalClient
key_links:
  - from: src/server/llm/observability.ts
    to: tests/program/adaptive-coaching-provider-observability.test.ts
    via: "allowlist-based audit envelope assertions"
    pattern: "provider|model|latencyMs|parseStatus|fallbackReason"
---

<objective>
Implement the real provider clients and deterministic fallback chain without weakening SAFE policy guarantees.

Purpose: deliver OpenAI primary + Anthropic fallback with strict output parsing, strict latency policy, and non-PII audit events.
Output: provider adapters, orchestration client, observability utilities, and integration tests for retry/fallback behavior.
</objective>

<execution_context>
@/home/flo/.codex/get-shit-done/workflows/execute-plan.md
@/home/flo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05.1-llm-provider-r-el-openai-anthropic-env-model-key-structured-output-strict-tests-safe-03/05.1-01-PLAN.md
@.planning/phases/05.1-llm-provider-r-el-openai-anthropic-env-model-key-structured-output-strict-tests-safe-03/05.1-RESEARCH.md
@src/server/services/adaptive-coaching.ts
@src/lib/adaptive-coaching/contracts.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install provider SDK dependencies and implement OpenAI strict-output adapter</name>
  <files>package.json, pnpm-lock.yaml, src/server/llm/providers/openai-client.ts</files>
  <action>Add `openai` and `@anthropic-ai/sdk` dependencies, then build OpenAI adapter that sends canonical strict JSON schema output request, enforces timeout from env config, captures request ID/latency metadata, and normalizes provider errors into failure reasons used by fallback orchestration.</action>
  <verify>
    <automated>pnpm test tests/program/adaptive-coaching-provider-retry.test.ts</automated>
  </verify>
  <done>Primary provider adapter is implemented with strict schema output and normalized failure metadata.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Anthropic fallback adapter and deterministic primary-retry-fallback client</name>
  <files>src/server/llm/providers/anthropic-client.ts, src/server/llm/client.ts, tests/program/adaptive-coaching-provider-fallback.test.ts, tests/program/adaptive-coaching-provider-retry.test.ts</files>
  <behavior>
    - Test 1: Primary timeout/error triggers one bounded primary retry.
    - Test 2: Primary schema-invalid payload (including missing `evidenceTags`) triggers one bounded primary retry before fallback-provider attempt.
    - Test 3: If retry and fallback both fail or remain schema-invalid, client returns null candidate and explicit fallback reason for SAFE-03.
  </behavior>
  <action>Write failing retry/fallback tests first, then implement orchestration order exactly: OpenAI attempt -> one OpenAI retry on retryable failures -> one Anthropic attempt -> null candidate. Enforce schema parse and mandatory `evidenceTags` after each provider response before accepting payload.</action>
  <verify>
    <automated>pnpm test tests/program/adaptive-coaching-provider-fallback.test.ts tests/program/adaptive-coaching-provider-retry.test.ts</automated>
  </verify>
  <done>Provider orchestration has deterministic ordering and produces SAFE-03-compatible null handoff when no valid proposal exists.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add non-PII observability envelope for provider attempts</name>
  <files>src/server/llm/observability.ts, src/server/llm/client.ts, tests/program/adaptive-coaching-provider-observability.test.ts</files>
  <behavior>
    - Test 1: Attempt logs include provider/model/latency/parse status/fallback reason/request ID.
    - Test 2: Log payload excludes prompt body, profile fields, reasons text, and other user-specific inputs.
  </behavior>
  <action>Create an allowlist-based audit builder and wire it into each attempt/rollup event from the fallback client. Log only operational metadata needed for debugging and postmortem analysis.</action>
  <verify>
    <automated>pnpm test tests/program/adaptive-coaching-provider-observability.test.ts</automated>
  </verify>
  <done>Provider runtime is observable with strict non-PII metadata and traceable fallback reasons.</done>
</task>

</tasks>

<verification>
Run fallback/retry/observability integration tests to confirm deterministic provider chain and safe audit logging behavior.
</verification>

<success_criteria>
- OpenAI primary and Anthropic fallback are both implemented behind one normalized client.
- Retry and fallback ordering is deterministic and bounded.
- Provider events are operationally useful while remaining non-PII.
</success_criteria>

<output>
After completion, create `.planning/phases/05.1-llm-provider-r-el-openai-anthropic-env-model-key-structured-output-strict-tests-safe-03/05.1-02-SUMMARY.md`.
</output>
